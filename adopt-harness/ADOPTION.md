# 接上 design-system —— 進行中的工作怎麼接手

這份寫給**沒有任何上下文的人**。`DECISIONS.md`(repo 根目錄)記的是已經定案的決定,
這份記的是「還沒做完的事、工具怎麼用、以及前面幾隻踩過哪些坑」。

工作內容:把 `tokyo-trip/index.html` 的樣式**逐塊**換成 `../design-system/` 的組件。
Lulu 要的是「網站真的變成 retro-modern 的樣子」,不只是色調統一。

---

## 這份文件怎麼讀

**趕時間的話,前三節就夠開工了:**「現在做到哪」→「工具」→「怎麼挑一塊」。
其餘是踩過的坑,遇到症狀再回來查 —— 但**「驗收規則」在動手之前一定要看過一次**。

| | |
| --- | --- |
| [現在做到哪](#現在做到哪) | 哪幾塊做完了、下一塊是什麼。**開工先跑那三行指令問線上狀態** |
| [工具](#工具adopt-harness這份文件的同一個資料夾) | `shoot.sh` / `probe.sh` / `pngdiff.py` / `crop.py` 怎麼用。另有 `table-vs-apis.js`(人工表 vs 三家地名查詢,**Google 那一欄要錢**,見 `table-vs-apis.md`)|
| [這份文件為什麼住在 `adopt-harness/`](#這份文件為什麼住在-adopt-harness) | 掃描範圍會把你寫的字編成 CSS。**工程文件不要放 `tokyo-trip/`** |
| [工具的陷阱](#工具的陷阱全部是踩出來的) | 十四條,全部是踩出來的。截到錯的東西、量到假的數字都在這裡。
**第 10 條是「斷言和截圖不是同一趟」;第 12 條是「迴歸斷言的期望值不要從現況抄」;
第 14 條專門在「檢查應該失敗」的時候讓檢查失效 —— 寫負向對照之前先看它** |
| [最貴的一個教訓](#最貴的一個教訓) | 差異式檢查對「兩邊都是空的」無感。正向斷言為什麼存在 |
| [基準:哪幾組還能用](#基準哪幾組還能用) | **基準名寫在那一節裡,不寫在這張索引上** —— 這一行曾經寫著 `shots/expform`,而那一節早就換成 `shots/gx-after` 了,害下一個人查了四趟截圖。**別人遞給你的基準要自己驗一次** |
| [驗收規則](#驗收規則) | 兩套,看那一輪該不該改變外觀。**動手前先讀** |
| [怎麼挑一塊](#怎麼挑一塊) | 四個判準 + **「要刪的規則整條盤完」** + **第六塊是 `.who`** 的理由 |
| [約束](#約束) | opacity modifier、`<link>` 順序、`:root` 不要進層、改 markup 要重編 |
| [已登記的盲區](#已登記的盲區) | 那 8 個沒人在用的 utility,以及文件把自己編進 CSS 的故事 |
| [走鐘防護](#走鐘防護) | `index.html` 裡那段自檢 |
| [多 session 協調規則](#多-session-協調規則) | 共用工作樹。**含「一塊一個人」的換手規則** |

---

## 現在做到哪

**這張表只記「哪一塊做完了」,不記「哪一塊上線了」。** 理由見下一節。

> **截圖現在是 12 張。** 這份文件裡很多地方寫著「九張」或「11 張」——
> 那些句子講的是它們發生的那一輪,是對的,不要回頭改。
> 但**你這一輪要比的是 12 張**(`01`–`09` + `10-exp-form` + `11-exp-edit-dialog`
> + `12-place-search`)。
>
> **12 那張的樁裝在 `shoot.sh` 裡**,所以它不碰網路 —— 直接查 Nominatim 的話,
> 它會變成「非決定性來源」那張表上的新一列。

| 狀態 | |
| --- | --- |
| ✅ 移除深色模式 | `ef364ce` |
| ✅ 309 條 CSS 包進 `@layer components` | `e890cf0`,視覺零改變 |
| ✅ 接線(載入 design-system) | `b43a2f7`,視覺零改變 |
| ✅ 第一塊:`.chip` → `.rm-chip` | `c43340e`,只有花費頁變 |
| ✅ 正向斷言驗完 + 工具修正 | 見「補救:正向斷言」 |
| ✅ `--font-mono` 的 CJK fallback | 補在 **tokyo-trip 自己那個 `--font-mono`** 上(不是 design-system)。九張全變,逐項對帳見 `block-02-mono.md` |
| ✅ 第三塊:`.fld label` → `rm-label` | 七張零差異、08/09 變,對帳命中 7 落空 0 清單外 0。連帶修好 harness 兩個缺口、新增兩支工具。見 `block-03-label.md` |
| ✅ 建置時的檢查 | design-system 那邊加了 token 撞名檢查和產出落後掃描,兩個都會在建置時出聲(含**成功時也出聲**)。消費端這邊配合重編過一次指紋 |
| ✅ 第四塊:`.fld input/select/textarea` → `rm-input` | 七張零差異、08/09 變,對帳命中 13 落空 0 **清單外 3**(都處理了)。Lulu 的決定:保留手機 16px 覆寫、刪桌機基底規則。連帶修好 harness 兩個缺口。見 `block-04-input.md`。**iOS 放大行為工具驗不到,但 Lulu 已在 iPhone 上實測過,沒有放大,見下** |
| ✅ 第五塊:補第 10 / 11 張截圖 | **一個樣式都沒改**,九張逐位元組相同。`#exp-form` / `#exp-edit-overlay` 第一次被打開:16 個欄位、10 個成員膠囊、10 個核取方塊都有畫面了。**回頭驗過第三、四塊凍住的東西,守住了**。連帶修好 harness 兩個坑、給 `pngdiff` / `crop` / `probe` 各加一個開關。見 `block-05-expform-shot.md` |
| ✅ 修回歸:`via` 在寫進快取那一刻掉了 | Lulu 真機回報「桃園按了只說沒標上去」。**人工表那句話只有標題那條路講得出來**,關鍵字寫在地點欄的(TPE／NRT／箱根…)全部講不出來,而且「再查一次」該收起來卻沒收。連帶清掉一顆會插在東京車站的幽靈 pin。探針 114 → **126**(修改前跑,10 條掛)。見 `geocode-via-in-cache.md` |
| ✅ 人工表 vs 開放 API 的量測 | Lulu 問「可以都用 google map 嗎」。20 組 × 3 家量完:**表留著**,而且理由跟準確度無關(三家的錯不重疊、同名地點與非地名字串是結構上答不出來的、Google 是唯一會硬給答案的)。順帶把 `富士` 拆成 `富士山`。探針 126 → **133**。見 `table-vs-apis.md` |
| ✅ 修抽屜的四檔(`fix/drawer-steps`) | Lulu 回報「沒有固定在四個尺寸會亂跑」。量到**視窗改 4px 抽屜砍一半**,而那個懸崖**不是四捨五入寫錯**:用相對比例反推絕對預設值在某處必然斷掉。四檔改成固定像素。只有 07 變。見 `drawer-steps.md` |
| ✅ 兩欄合併(「想去哪」＋「地點」→ 一欄) | Lulu 的決定:**只有挑過的才上地圖**、兩個表單都合併。支點是「**只改誰去填 `place`**」—— 沒挑就是空字串,而「沒有 place 就不查」是本來就在的規則,所以地圖那條路一行都沒動。只有 05 / 08 / 09 / 12 變。`geofix` 八處打字的測法整個改寫。見 `merge-place-field.md` |
| ✅ 地點搜尋(新功能,不是接線的一塊) | Lulu 要「像 Google Maps 那樣打關鍵字、看候選、選一個」。**先用免費的 Nominatim 做介面**,資料來源之後換只動 `seekPlace()` 一個函式。三個地點欄都有。只有 08 / 09 變,**新增第 12 張**讓候選清單第一次有畫面。`probes/seek.js` 25 條。見 `place-search.md` |
| ✅ 第六塊:`.who` → `rm-chip` | 只有 10 / 11 變,其餘九張逐位元組相同。核取方塊 **27.531→13px**、膠囊 **64.781→41px**(名字不再折行)。留下 `cursor:pointer`(是功能不是外觀)和手機覆寫(觸控目標)。量到第四塊補回的 `font-size:14px` **從補回去那天就是死的**。斷言收緊成 `label.rm-chip`,負向對照兩張都紅。見 `block-06-who.md` |
| ⏸ 字型 | **排最後**,見下 |

### 第四塊留下一件要真人做的事

這一塊整個決定(保留 `:521` 的 16px)就是為了防 iOS Safari 對焦放大,
而**九張截圖和探針全部是 macOS 上的 headless Chrome,驗不到它**。
量到的只有「390px 下 29 個控制項改完仍然全部是 16px」——
那是**觸發條件不成立**的證據,不是**行為沒發生**的證據。

> **要一個真人拿 iPhone 打開網站、點進「加行程」的任何一個欄位,看版面會不會跳。**
> 那五個人就是在 iPhone 上記帳的。

第五塊新增的 10 / 11 兩張圖**一點忙都幫不上**:多了 14 個畫得出來的欄位,
一個都不能回答 iOS 的問題,因為它們一樣是 macOS 上的 headless Chrome。
**工具能做的事和該做的事之間那道界線,五塊下來一步都沒有移動。**

#### ✅ 做了,而且過了 —— 但只涵蓋這麼多

| | |
| --- | --- |
| 誰 | Lulu |
| 何時 | 2026-09-20,第四塊推上線之後幾分鐘內 |
| 裝置 | 她自己的 iPhone(型號與 iOS 版本不詳,沒有問) |
| 做了什麼 | 開線上版本 → 點進表單 → 點一個輸入框 |
| 結果 | **版面沒有跳。對焦沒有觸發放大。`:521` 那條 16px 守住了。** |

**涵蓋到哪裡為止:** 只測了「對焦會不會放大」這一件事,沒有測任何其他 iOS 行為
(捲動、鍵盤遮蔽、time/date 原生控制項、Safari 以外的瀏覽器)。
**一台裝置、一個人、一次。**

> **「驗過了」和「驗到什麼程度」是兩件事。** 沒寫清楚的話,
> 下一個人會把它當成「iOS 全面驗過」,那比不寫還危險。

> **這一筆是這個專案到目前為止唯一一次非 headless 的證據**,
> 所以它值得被寫成一筆有出處的紀錄,而不是在狀態表上打一個勾。

> **為什麼這一段一度是錯的(值得記):** 這次實測發生在 2026-09-20,
> 而這份文件在那之後還寫著「沒有人做」「線上跑著一個沒驗過的假設」。
> 原因是**產生那個事實的事件發生在文件之外**(在協調者跟 Lulu 的對話裡),
> 沒有人把它帶回來。
> 這跟「狀態表過期兩次」是同一個形狀,只是方向相反 ——
> 那次是「文件說已經上線,其實還沒推」,這次是「文件說沒驗過,其實驗過了」。
> **而說「沒驗過」的過期比較危險:下一個人要嘛重做一次,要嘛把它當成待還的債繼續傳下去。**

### 線上現在是什麼:這份文件不回答這個問題

**以前這裡寫著答案,而那個答案過期了兩次。**

第一次寫成「還沒推的:接線和第一塊」,寫下之後又推了一次,沒回頭更新。
第二次寫成「`origin/main` = `9262c9a`、mono 尚未推送」,而 mono 那時早就上線,
**那五個人已經看得到了**。兩次都是同一個結構問題:
**把一個每次 push 都會變的事實,寫進一份不會自己更新的文件。**

而且第二次過期的時候,這一段下面就已經寫著「不要從未推送清單反推、要自己跑這三行」——
**文件自己知道答案會過期,卻還是把答案留在上面。** 警告擋不住一個結構上就會壞的東西。

所以現在這裡不留答案,只留產生答案的方法:

```sh
git rev-parse origin/main
git merge-base --is-ancestor <commit> origin/main && echo 已上線
curl -s https://playground-beta-liart.vercel.app/ | grep -o "rm-chip"
```

**每一輪開工前自己跑一次。** 如果你正想在這份文件裡寫下它們的輸出 —— 不要,
那就是前兩次過期的寫法。

**推不推是 Lulu 的決定,不是接手的人的。**

### 下一步的順序

1. **第六塊:`.who` 那一整組**(成員膠囊 `label` + 核取方塊 `input`)。
   它被第三塊和第四塊各凍了一次,理由都是「沒有截圖就驗不了」——
   **第五塊把那個理由拿掉了**。細節見「怎麼挑一塊」和 `block-05-expform-shot.md`
2. 字型**最後才做**。理由:字型一換,九張截圖全部不一樣;排在前面的話,後面每一塊
   都是在一個剛剛大幅變動過的基準上比對。排最後,前面每一塊都還能跟乾淨的基準比

---

## 工具:`adopt-harness/`(這份文件的同一個資料夾)

> 它在 repo 裡,但**不在 Vercel 的部署範圍、也不在 Tailwind 的掃描範圍**
> (兩者都是 `tokyo-trip/`)。
>
> 它原本放在 repo 外面。搬進來的理由:換手之後沒有人重建得出它 ——
> 裡面每一行幾乎都是踩到坑之後補的,從零寫一份不會長這樣。

```
shoot.sh        逐頁截 11 張圖(手機 + 桌機),每張附一條正向斷言
                **SRC=<某棵樹>/tokyo-trip ./shoot.sh ...** 截別的來源樹(見下)
probe.sh        在真實 DOM 上量東西,跑 probes/*.js,結果以 JSON 印出
                **WIDTH=390 ./probe.sh ...** 在手機寬度量(預設 1100,見下)
                **SRC=** 同上,兩支共用同一個開關
probes/         探針腳本。labels.js 是範本:盤點 + 逐項 computed 值 + 離群值
                inputs.js 會先用真實點擊把 08/09/花費那幾個表單打開再量
                who.js   .who 膠囊 + 核取方塊的逐項盤點(含小數點後三位的幾何)
                dayscroll.js 診斷 01 的雙穩態(非決定性來源 #3)
                geofix.js 地名那一整條的前端行為(退路、「沒填地點就不查」、按「對」)。
                         **把 fetch 換成樁**,所以不碰網路也不需要金鑰 —— 範本價值在這裡。
                         **160(1100)/ 161(390)條**(2026-09-21 實測;這個數字每輪都在長,
                         以量到的為準,不要信這一行)。兩個寬度都要跑。它也是
                         「送出當下那句話出現在哪裡」唯一的儀器(量的是 rect,不是有沒有出現)
                seek.js  地點搜尋那一整條 + 「改」出現在哪幾筆願望上。**56 條**,兩個寬度
                drawer.js 桌機地圖抽屜的四個吸附檔位。**登記的驗收寬度是 1100 和 641**,
                         390 下有 2 條恆紅(那個寬度撐不起 240px 的版面),**不是回歸**
                pwa.js 加到主畫面那一整套(head 標籤、manifest、圖示、`sw.js` 的 SHELL
                       逐個 fetch 對答案)。**PWA 壞掉的方式全部是無聲的**,
                       SHELL 少列一個檔線上完全正常,只有斷線的人看到半個網站。
                       `SNAP=1` 再多量一個狀態:離線 + 有副本(唯讀、狀態列要說出資料多舊)
fixture.py      固定的假資料 + 凍結時鐘
                **SNAP=1** 多灌一份離線副本(`tokyo5-snap`)。預設不灌 ——
                灌了每張圖都會變成「離線唯讀」那一套,十三張基準一次全紅
make-icons.sh   從 `tokyo-trip/icon.svg` 出 manifest 要的那幾個 PNG(渲染器是 Chrome,
                這台機器沒有 Pillow / ImageMagick / rsvg)。**產出要 commit**,
                部署時沒有人會重算。出圖前驗 XML、出圖後數顏色,兩道都是撞出來的
pngdiff.py      逐像素比對,回報差異位置與**列群**
                **--band y0 y1 [--x x0 x1] [--shift N] [--shiftx M]** 位移搜尋:
                回答「這一帶是被改掉了,還是只是被推走了」。
                `--shift` 垂直、`--shiftx` 水平、兩個一起給就搜二維。
                **只搜了一個方向而沒命中時,它會自己講出來沒搜哪一邊**
crop.py         把兩張圖的同一段 y 並排切出來,中間畫紅線 —— 用眼睛確認
                第六個參數 `dy` 把右邊那張往下位移再切(配 --shift 用)
wrap-layer.py   一次性的,把內嵌樣式包進 @layer components(已經用過了)
shots/          產出,不進版控(一組約 2.2MB,每輪重生)
block-01-chip.md  第一塊的預測清單 + 實際結果,可以當範本
block-02-mono.md  mono 中文後援那一輪(不是組件轉換,是修回歸)
block-03-label.md 第三塊,附兩個工具缺口的修法與驗證
block-04-input.md 第四塊,附 01 雙穩態的根因與兩個 harness bug
block-05-expform-shot.md 第五塊,補第 10/11 張截圖 + 回頭驗證兩輪的凍結
geocode-retry.md  **不是轉換塊**(所以不叫 block-NN):地名查詢加一條使用者
                  主動觸發的退路。驗收走「不該改變外觀」那一套
geocode-place-and-confirm.md  接在上一份後面的那兩段:**沒填地點就不查**(規則一)、
                  **不查的要在送出當下講**(規則二)、以及虛線圈按「對」變實線(規則三)。
                  **驗收是混合的**:06 / 07 預期會變(少兩顆 pin),其餘九張零差異。
                  三段是同一件事,不要拆散
geocode-outside-and-notice.md  **修上一份自己造成的兩個回歸**(Lulu 在真機上回報的):
                  規則一連帶把 `OUTSIDE` 人工表關掉了;規則二那句話講在 `<footer>` 裡。
                  **這一輪十一張零差異,而那是預期之內的** —— 該變的東西全部不在截圖上,
                  所以「該變的變了」整個靠探針。事前登記寫在那一份
api-geocode-test.js  `node adopt-harness/api-geocode-test.js` —— api/notion.js 的
                  geocode 分支 + 願望 PATCH 的夾制,fetch 換成樁,不需要金鑰也不碰網路。
                  **50 條**(2026-09-21 實測)。`SRC=<某個 notion.js>` 可以對它做負向對照
ADOPTION.md     這份
```

### `SRC=`:不動共用工作樹也能截某個 commit 的樣子

「工具的陷阱 1」教的是「要截某個 commit 就先 `git stash` 或 checkout」——
**那等於動共用工作樹裡的 `tokyo-trip/index.html`**,而這個 repo 同時有別的
session 在動它,一塊一個人的時候還常常被明確要求不准碰。

```sh
git worktree add /tmp/wt 8b4ee7c
ln -s <repo>/design-system/node_modules /tmp/wt/design-system/node_modules   # 少了它會重編失敗
SRC=/tmp/wt/tokyo-trip ./shoot.sh pre-label-expform
SRC=/tmp/wt/tokyo-trip WIDTH=390 ./probe.sh probes/who.js
```

出處紀錄會多印一行 `來源`,而且**不是預設來源時會大聲說**下面那幾行 HEAD／工作樹
統計是主樹的、跟這批圖無關,另外印來源樹自己的 HEAD。

**`shoot.sh` 和 `probe.sh` 共用一把鎖,同一時間只有一隻跑得動。** 見「工具的陷阱 6」。

`probe.sh` 跟 `shoot.sh` 站在同一個地基上(同樣先重編、同樣灌 `fixture.py`),
所以**探針量到的數字和截圖是同一個世界**。沒有這一點,兩邊對不上帳。

用法:

```sh
cd <repo>/adopt-harness          # 注意:是 repo 根目錄,不是 tokyo-trip/ 底下
./shoot.sh <標籤>                 # 截一組,放進 shots/<標籤>/
python3 pngdiff.py shots/a/01-plan-mobile.png shots/b/01-plan-mobile.png
```

`shoot.sh` 會**自動先重編** design-system 的產出,不用手動跑 build。

**`probe.sh` 預設在 1100px 量,而九張截圖裡有七張是 390px。**
`index.html` 有一整段 `@media (max-width:640px)` 的覆寫,在 1100 量到的是那七張
**根本看不到的那一套值**。`block-04` 差點因此下相反的結論(見那一份的「新踩到的陷阱 3」)。

```sh
WIDTH=390 ./probe.sh probes/inputs.js     # 08 / 09 實際的寬度
./probe.sh probes/inputs.js               # 1100,桌機那一套
```

**一塊如果出現在手機圖上,兩個寬度都要量。**

> **這是這個專案的招牌失敗模式,這次長在「量的寬度」上:**
> 工具量得到的寬度,跟你要回答的問題所在的寬度**不一樣** ——
> 而它**不會報錯,只會給你一個乾淨的錯答案**。
>
> `block-04` 要回答的是「`:521` 的 16px 有沒有守住」。在 1100px 量,
> 答案永遠是 `14px`×29,看起來完整、一致、可信,**而且是相反的結論**。
> 跟「canvas 量到的是系統字體」、「fixture 的 key 差一個字元」同形:
> **安靜地量到了別的東西。**

### 工具還缺什麼(已登記,還沒做)

`block-02-mono` 那一輪列的,協調者同意做但排在之後。寫在這裡免得再被重新發現一次:

1. ✅ **把「在真實 DOM 上量東西」的探針收進 `adopt-harness/`** —— `block-03-label`
   那一輪做了,就是 `probe.sh` + `probes/`。
   > 這一項是個**累犯**:至少兩輪各自寫過一支一次性探針,寫完都留在暫存目錄裡弄丟,
   > 下一個人又從零寫。收進 repo 才算修好。
   > 但「量字體的四個陷阱」仍然只有文件、沒有對應的探針 ——
   > 想量字體的人要自己照 `block-02-mono.md` 的做法寫一支 `probes/fonts.js`。
2. ✅ **`pngdiff.py` 要給區域 + 位移搜尋** —— `block-05` 做完了。
   除了原本的**列群**,現在有 `--band y0 y1`、`--x x0 x1`(x 方向的區域)
   和 `--shift N`(**垂直位移搜尋**)。
   最後一個回答的是那個真正要問的問題:**這一帶是被改掉了,還是只是被推走了?**
   —— 每一塊轉換都會把下面的東西整片往下推,而「被推走」和「被改掉」在
   「差了幾 %」那個數字上長得一模一樣。
   `crop.py` 跟著多一個 `dy` 參數,拿 `--shift` 找到的位移把兩張對齊再並排。
   > 仍然沒做的:全圖比對時**不會自動報 x 方向的群**,你得自己指定 `--x` 去縮。
   >
   > ✅ **水平位移搜尋做掉了**(`geocode-place-and-confirm` 那一輪,協調者定為必做)。
   > 現在是 `--shiftx M`,跟 `--shift N` 可以一起給(二維,依總位移量由小到大)。
   > 拿 01 那個已登記的雙穩態驗過:
   > `--band 460 640 --x 100 740 --shiftx 40` → **`dy=+0 dx=-27`,殘差 0** ——
   > 比手寫那次(`dx=27,殘差 2.81%`)還乾淨。
   > **`--x` 要框在捲動區的裡面**:框到不會跟著捲的邊緣,純位移也會看起來像被改掉。
   >
   > **而這一條真正的修法不只是補能力。** 以前只有垂直版的時候,它對水平位移
   > 回的是「沒有任何位移對得上,**這一帶是真的被改掉了**」—— 一個很有自信的錯答案。
   > 現在**只搜了一個方向而沒命中時,結論那一行會說自己沒搜哪一邊**;
   > 兩個方向都搜過,它才敢講「是真的被改掉了」。
   > **能力缺一半不可怕,不知道自己缺一半才可怕。**
3. ✅ **這份文件加目錄**,並把「這份文件為什麼住在 `adopt-harness/`」的故事
   移到「工具怎麼用」之後 —— `block-03-label` 那一輪做了。

4. ✅ **`shoot.sh` 沒有截到花費的兩張表單** —— `block-05` 做完了,
   現在是 `10-exp-form` 和 `11-exp-edit-dialog` 兩張(不是一張:
   `#exp-edit-overlay` 另外有 8 個欄位、5 顆膠囊,而且 app 會自己 focus `xe-title`)。

**剩下的缺口只有一個:沒有 `probes/fonts.js`** —— 「量字體的四個陷阱」仍然只有文件、
沒有對應的探針。想量字體的人要自己照 `block-02-mono.md` 的做法寫一支。
**下一個發現自己需要它的人就是該做的人。**

> 第 1 條那個「累犯」的形狀值得再看一次:`block-04` 為了診斷 01 的雙穩態
> 臨時寫了一段水平位移搜尋、用完就丟,並且自己寫下「它值得進 `pngdiff.py`」。
> `block-05` 需要垂直版的時候**沒有再寫一支一次性的**,直接進了 `pngdiff.py` ——
> 那條登記終於閉環了。**寫下「這應該進工具」跟真的放進去之間,隔了一整輪。**

---

## 這份文件為什麼住在 `adopt-harness/`

它原本在 `tokyo-trip/ADOPTION.md`。搬出來是因為**那個目錄同時是兩件事的範圍**,
而兩件事都被它咬到:

| | 範圍 | 症狀 |
| --- | --- | --- |
| Vercel | Root Directory = `tokyo-trip/` | 文件被部署到正式站,`GET /ADOPTION.md` 回 200 |
| Tailwind | `app.css` 的 `@source "./"` | 文件裡的字串被當成 class 編進產出 |

**第一個**:內部工作文件(失敗故事、協調規則、那五個人的相關描述)任何人打開網址就讀得到。
不是機密,但不該出現在使用者的網站上。

> 別用「README 沒事所以 .md 不會被服務」來推論 —— 實測 `GET /README.md` 是 **404**,
> 而 `GET /ADOPTION.md` 是 **200**。Vercel 對 README 有特別待遇,對別的 .md 沒有。

**第二個**:這份文件的「不要用 opacity modifier」那段字面寫著 `bg-paper/50`、`text-ink/70`,
「已登記的盲區」那段字面寫著 `backdrop-filter`。掃描器把**警告文字本身**撿去編成了 utility,
所以那三個名字真的出現在正式站的 CSS 裡(連同 `color-mix` 的 `@supports`)。
**一條叫大家不要用某個東西的規則,因為被寫下來而讓那個東西真的存在了。**

`adopt-harness/` 在版控裡但不在這兩個範圍內 —— 前一隻把工具放這裡就是為了第一個理由,
只是當時沒想到文件本身適用同一條。現在文件跟 `block-01-chip.md` 放在一起。

**推論:任何要寫進 repo 的工程文件都不要放 `tokyo-trip/` 底下。**

---

## 工具的陷阱(全部是踩出來的)

### 1. 它截的是「工作目錄現況」,不是某個 tag

**症狀:比對永遠通過。**

我做過一次:改完 `index.html` 之後才想到要截基準,結果截出來的「基準」是改動後的版本,
拿去跟改動後比,當然零差異。

**要截某個 commit 的樣子,先 `git stash` 你的改動。**

每組截圖旁邊有 `PROVENANCE.txt`,記了截圖當下的 HEAD、**整個 repo** 有幾個檔沒提交、
其中幾個會被烘進圖(`tokyo-trip/` 與 `design-system/`)、相依的 HEAD 與髒不髒、stash 幾筆。
事後回頭看得出這批是站在哪裡截的。

> **它以前只數 `tokyo-trip/`。** `git -C "$SRC" status --porcelain -- .` 的 `-- .`
> 把範圍限死在那個目錄,而 build 是從 `design-system/` 編出來的 ——
> 「一輪進行中 design-system 凍結」那條規則因此長期**沒有任何儀器在背後撐著**。
> `block-03-label` 那一輪修好,並且故意留一個髒改動驗過它真的會講出來。

### 2. 時鐘要凍,而且凍的方式本身會把 app 弄壞

出發倒數(「還有 N 天」)顯示在**每一頁**的標題列。跨過午夜,九張全部「有差異」。
我第一次撞到的時候,一度以為是別人改壞的。

`fixture.py` 把時鐘凍在 `2026-09-19T03:00:00Z`。

**但第一版的凍法把整個 app 弄壞了:**

```js
function K(){ return arguments.length ? new D(arguments[0], arguments[1], ... arguments[6]) : new D(F); }
```

固定傳七個參數,所以 `new Date(ms)` 變成 `new Date(ms, undefined, ...)` —— 那是多參數
建構式,結果是 Invalid Date。`todayISO()` 一掛,開機就停住。

**症狀:每一頁都一致地空白,而截圖比對照樣報零差異。**

正確的寫法是原樣轉交:`Reflect.construct(D, arguments)`。

> 這個壞法現在是**負向對照的標準做法** —— 要測「正向斷言還活著嗎」,就把這段改回
> 固定七個參數跑一輪,九張應該全部印 `✗`。

### 3. fixture 的 localStorage key 必須跟 app 一字不差

**症狀:看起來一切正常,但那一頁其實是網路的函數。**

`fixture.py` 灌的是 `tokyo5-pin`,而 `index.html` 讀的是 **`tokyo5-pin3`**
(第 1700 行,`const PINLS`;它的註解自己寫了「換過 key 兩次」)。

**差一個字元,不會報錯**,只是整份圖釘快取失效 → `pinFor()` 改走 Nominatim 線上查詢
(每筆之間還 sleep 1100ms 避開速率限制)→ 地圖上有幾個 pin、pin 在哪,
完全看網路那一刻多快、Nominatim 回什麼。

後果不是「少幾個 pin」,是 **06 / 07 兩張圖從來就不是決定性的**。
`block-01-chip.md` 寫「其他八張逐位元組相同」—— 那個結論在當時成立,
但**成立是運氣**(兩組截圖差一分鐘截的,Nominatim 剛好回一樣的東西)。
30 分鐘後再截,07 就跟 `block01` 差了 0.21% 的像素,全落在地圖區。

已修(key 改成 `tokyo5-pin3`)。修完的驗證方式是**加 `--host-resolver-rules` 把
localhost 以外的網路全部切斷重跑,確認數字一模一樣** —— 只看「數字變好了」不算驗證。

四個 key 現在的對照(改 app 的時候要一起看):

| fixture 灌的 | index.html 讀的 | |
| --- | --- | --- |
| `tokyo5-v1` | `LS` | ✅ |
| `tokyo5-wx` | `WXLS` | ✅ |
| `tokyo5-me` | `MELS` | ✅ |
| `tokyo5-pin3` | `PINLS` | ✅ 已修 |
| (沒灌) | `GEOLS` = `tokyo5-geo` | ⚠ 見下 |

### 4. 非決定性來源

| 來源 | 症狀 | 怎麼消掉的 |
| --- | --- | --- |
| 過場動畫 | 分頁切換的指示條被截在不同位置 | 截圖時注入 `transition:none;animation:none` |
| 地圖圖磚 | 地圖區大片差異 | 外部圖磚換成 1px 佔位 |
| 時鐘 | 見上 | 凍結 |
| **Nominatim(圖釘座標)** | **06/07 的 pin 數量和位置每次不同** | **`tokyo5-pin3` 灌對,見上一條。而許願地圖那條路以前還會另外發兩次(見下),現在是 0 次** |
| **open-meteo geocoding** | 目前沒有症狀,但沒被擋住 | **還沒處理,見下** |
| **網頁字體載入的時機(日期橫條的捲動位置)** | **01 在兩張圖之間跳**,差 2.1481%,`y=454..651`,1 群,水平位移 27 影像像素 | **沒有消掉,刻意的,見下** |
| **次像素相位(11 的左邊緣)** | **11 在兩張圖之間跳**,8 px,全在 `x=86` 那一行,6 個單列群 | **沒有消掉,刻意的,見下** |
| **日期橫條的捲動位置(05 也吃到了)** | **05 在兩張圖之間跳**,2.68%,`y=454..651`,1 群 —— 跟 01 同一個機制,不同張 | **沒有消掉,見下** |

#### 05-wishes-mobile 也是雙穩態的(`merge-place-field` 那一輪發現)

**2.68% / `y=454..651` / 1 群**,而且**位移搜尋兩個方向都對不上**。
連截三次得到兩個值(一次 A、兩次 B),01 在同一三趟裡穩定不動。

看圖就清楚:那一帶是**日期橫條**,兩趟捲到的水平位置差約 20 px ——
也就是**跟 01 登記的是同一個機制**(網頁字體載入的時機影響橫條捲到哪),
只是這一輪第一次在 05 上看到。

> **為什麼位移搜尋說「對不上」,而它其實就是位移。**
> 橫條捲動的時候,**邊緣會補進原本在畫面外的內容** —— 那一塊在另一張圖裡
> 根本不存在,所以整條帶永遠不可能整體對齊。
> **`--shift` / `--shiftx` 回報「沒有位移對得上」,對捲動容器不成立。**
> 那個工具是為「整片被往下推」設計的,不是為「視窗移動」。
> 遇到這種,眼睛比工具快:`crop.py` 切出來看一眼就知道。

> **判準**:05 的差異如果落在 `y=454..651`、1 群、2–3%,而且 `crop.py` 看起來
> 是日期橫條捲到不同位置,就是這件事,重截會跳回去。
> **任何其他形狀都要當成問題查。**

`01` 的那一條(下一節)寫著「水平位移 27 影像像素」—— **05 這一張對不上那個數字,
但那不代表它是別的東西。** 兩張圖的橫條寬度和選中的那一天都不同,位移量本來就不會一樣。
(這是「太緊的判準會讓下一個人把同一件事當成新問題查」的又一次。)

#### 11-exp-edit-dialog 也是雙穩態的(`geocode-retry` 那一輪發現)

**8 個像素 / 1881600(0.0004%),全部落在 `x=86` 那一行**,
分成 6 個單列的群(`y=1026 / 1199 / 1396 / 1538 / 1719 / 1837`),色差 3–5/255。

**兩個狀態都在沒有任何改動的樹上出現過** —— `shots/expform`(`aa72295`)是一邊,
`shots/expform-2`(`30c5b9c`)是另一邊,而這兩個 commit 之間只動了這份文件。
所以它跟程式碼無關,看起來是某個左邊緣落在次像素相位的兩側(工具陷阱 9 的形狀)。

> **判準**:11 的差異如果剛好是 **8 px / 6 個單列群 /
> 全部落在 `x=86` 或 `x=88`(同一個左邊緣)**,就是這件事,重截會跳回去。
> **任何其他形狀都要當成問題查。**
>
> (`x=88` 是 `geocode-place-and-confirm` 那一輪量出來的:8 個像素裡有 2 個在 `x=88`,
> 就是那兩群 2px 的,色差 1/255。**原本的簽章寫「全部在 x=86」太緊** ——
> 太緊的判準會讓下一個人把同一件事當成新問題查,那跟太鬆一樣是壞的,只是方向相反。)

沒有去消掉它,理由跟 01 一樣:代價大於收益。

#### 01-plan-mobile 是雙穩態的(`block-04` 發現)

**同一個乾淨工作樹連跑兩次 `shoot.sh`,01 會得到兩張不同的圖。** 根因量過了:

`renderDays()` 最後一行是 `cur.scrollIntoView({inline:"center"})`,它算出來的 `scrollLeft`
取決於**算的那一刻每張日卡多寬**,而日卡寬度取決於 Noto Sans TC 載完了沒。
沒載完 = 用後援字體的度量(卡片約 107px),載完之後卡片縮到 101px,
**但 `scrollLeft` 不會重算**。偶爾字體先贏那場賽跑,就得到另一張圖。

```
診斷: WIDTH=390 ./probe.sh probes/dayscroll.js
  實際 scrollLeft 102 / 以現在的度量重算 88 / 反推當初的卡寬 106.8(現在 101)
```

**沒有消掉,而且是刻意的。** 兩個狀態都是使用者真的會看到的(第一次造訪 vs 字體在快取裡),
要「消掉」就得讓 harness 在字體載完之後強制重捲一次 —— **那會改掉 01 這張圖本身,
前面每一組基準都不能再比**,代價遠大於收益。

> **遇到它的判準**:01 的差異如果剛好是 `2.1481%` / `y=454..651` / **1 群** /
> 水平位移 27,就是這件事,**重截一次會回到相同**。
>
> **這一題現在問得出來了:`pngdiff.py --shiftx`**(`geocode-place-and-confirm` 那一輪補的)。
> `--band 460 640 --x 100 740 --shiftx 40` → `dy=+0 dx=-27`,**殘差 0,逐位元組相同**。
> 以前只有垂直版的 `--shift`,對這一題會回「沒有任何位移對得上,這一帶是真的被改掉了」——
> **那句話在這裡是錯的,而且它講得很有自信**;現在沒搜過的方向它會自己講。
> **任何其他形狀的差異都要當成問題查** —— 不要把這一條當成「01 可以忽略」。
> (這份文件有前科:見「已登記的盲區」最後那一節,把訊號標成噪音是怎麼拆掉一道防線的。)

> 前面那句「前兩個都只影響『變化過程』和『外部資源』,不影響我要驗的版面」
> **對地圖不成立**。決定 pin 座標的是查詢結果,而 pin 座標就是版面 ——
> pin 一移動,`layoutLabels()` 的碰撞結果跟著變,標籤會改放到下面、或直接被藏起來。

> **許願地圖那條路以前每次打開都碰網路,現在不碰了。** `geocode-retry` 那一輪
> 順手量到「打開許願地圖會發出 2 次 Nominatim 查詢」並登記成一筆漏掉的非決定性來源。
> 成因在 `geocode-outside-and-notice` 那一輪查清楚了:航班報到那一筆的**地點欄裡
> 填的是航班備註**(`樂桃 MM626 · 建議起飛前 2.5 小時`),而規則一連帶讓
> `OUTSIDE` 人工表比對不到它 —— 於是它掉到線上查詢,還被拆成兩個字串各問一次。
> 人工表接回標題之後,**七顆 pin 全部來自 fixture 快取或人工表,一次網路都不碰**
> (`probes/geofix.js` 把 `fetch` 換成樁並計次,整趟的 URL 清單裡沒有任何一筆是
> app 自己發的)。07 的斷言門檻因此從 4 升回 7,見「補救:正向斷言」。

**`tokyo5-geo` 還沒灌。** 那是天氣用的地名查詢快取。實測「斷網 vs 連網」跑首頁,
六張日卡的天氣字串**完全相同**,所以現在不影響版面 —— 但那是因為目前的假資料裡
沒有東京都以外的地點(`resolvePlace()` 對東京都的結果一律 `null`,走預設座標)。
**哪天假資料加一筆箱根或橫濱,它就會變成第三個非決定性來源。** 事前登記在這裡。

### 5. 量字體的四個陷阱

**這一節原本不在這份文件裡。** 前面幾輪口耳相傳過三條,但沒有人寫下來 ——
`block-02-mono` 那一輪接手時是靠交接的人**口述**的,文件裡 grep 不到
`canvas`、`fonts.check`、`unicode-range` 任何一個字。現在補上,並且更正其中一條。

#### 5a. canvas 不會觸發網頁字體載入

**症狀:每個 stack 量出來的數字都不一樣,看起來很有意義,其實全是同一套字。**

`canvas.measureText()` 不會讓瀏覽器去下載網頁字體。所以你設了
`ctx.font = '14px "IBM Plex Mono"'`,量到的是**系統預設字體**,
換成別的 stack 再量,量到的還是同一套系統字體。兩邊數字有差,是因為字串不同,
不是因為字體不同。

**實際後果**:有一輪用 canvas 掃了十類字元,結論是「連全形數字和日文假名都沒動」,
而且那個結論被當成證據往上報過。`block-02-mono` 用真實 DOM 重量,
**全形數字差 2320px、全形拉丁差 1482px —— 它們確實變了**,前一個結論是假陰性。

**要用真實 DOM 渲染。** 做法見 `block-02-mono.md`:同一份 markup 用兩個 stack 各渲染一次,
每列固定高度 + `overflow:hidden`,截圖後用 `pngdiff.py` 逐像素比。

> 值得記的不只是結論。那一輪的人**讀過這份文件**,只是文件當時沒有這條 ——
> 而就算有,「知道陷阱」和「認出自己正踩在上面」也是兩件事:
> 他不覺得自己在「量字體」,他覺得自己在「掃字元」。

#### 5b. `document.fonts.check()` 要帶中文字,但帶了也還是會騙你

CJK 網頁字體按 `unicode-range` 分片送。`check()` 不帶第二參數時用的是拉丁測試字,
中文那一片還沒下載 —— **你會得到假陰性。所以一定要帶真的中文字當第二參數。**

**但這句建議只對了一半,另一半是這樣:**

```js
document.fonts.check('14px "IBM Plex Mono"', '景點')   // → true
// 而 IBM Plex Mono 一個中文字都沒有
```

`check()` 問的是「**要渲染這段文字所需要的 face 都載好了嗎**」。
當沒有任何 `@font-face` 宣告涵蓋那些碼位時,答案是「沒有東西要等」= `true`。

**它是『有沒有在等網路』的檢查,不是『涵蓋不涵蓋』的檢查。**
要知道涵蓋範圍,去讀 `@font-face` 的 `unicode-range`:

```js
document.fonts.forEach(f => console.log(f.family, f.status, f.unicodeRange))
```

#### 5c. `unicode-range` 只說得出網頁字體,說不出本機字體

讀完 `unicode-range` 也不能直接下結論。stack 裡的本機字體(`ui-monospace`/SF Mono、
`Menlo`、`PingFang TC`)**沒有 `unicode-range` 限制**,它們有沒有那個字形,
從 CSS 和 Google Fonts 的宣告裡完全看不出來。

實例:`→` U+2192 不在 IBM Plex Mono 的任何切片裡,照宣告推論「會換字體」——
**實測沒換**,因為 SF Mono 先把它接走了。

**推論的用途是「事前登記成不確定」,不是「事前下結論」。**
這也正是為什麼 CJK 後援要排在 `Menlo` **之後**:本機等寬字體沒有中文,接不住就往下落;
有些符號它們接得住,就輪不到 CJK 字體。

#### 5d. 比字體要比形狀,不要比位置

任何含座標的指標(`getBoundingClientRect().x`、元素寬度、文字換行位置)
都會把「第一個可用字體」的**度量差**算進去,也會把前面的字推走造成的位移算進去。
那量到的是版面,不是字形。

`block-02-mono` 的做法:每列固定高度 + `overflow:hidden`,
讓任何一列的差異都只代表那一列自己的字形變了。
對帳站上的差異時,也要分清楚「這個像素差是字形差,還是被前面的中文推走的」。

### 6. 這套工具自己不能兩隻同時跑

**症狀:截圖一半是你的、一半是別人的,而兩邊都沒有錯誤訊息。**

`shoot.sh` 碰的每一樣東西都是共用的:

```
PORT=8999                                  單一硬編碼的埠
rm -rf "$H/.work" && mkdir -p "$H/.work"   單一共用目錄,而且第一件事就是 rm -rf 它
build.sh → tokyo-trip/retro-modern.built.css   直接寫進共用工作樹裡被追蹤的檔
```

後跑的那隻會 `rm -rf` 掉前一隻**正在用的**工作目錄。前一隻於是對著半個目錄、
或別人的產出拍照。**這正是這整套工具存在的理由(無聲失敗),卻長在工具自己身上。**

已修:開頭用 `mkdir`(原子的)搶一把 `.lock`,搶不到就印出持有者並 `exit 2`。
`probe.sh` 共用同一把。

> **「持有 harness」是一把鎖,不只是一個習慣。** 它比「凍結 design-system」更基本 ——
> 凍結管的是相依,這條管的是工具本身。這個 repo 同時有好幾個 session 在動,
> 開跑之前先確認沒有別人在截。

#### 這個坑修了兩次,而第一次寫下的結論是錯的

```sh
trap 'rm -rf "$LOCK"; [ -n "${SRV:-}" ] && kill "$SRV" 2>/dev/null; true' EXIT INT TERM
```

**`set -e` 在 trap 裡面照樣有效**,而 `[ ] && kill` 是一個複合句 —— `kill` 失敗時整句失敗,
trap **在那裡中止**,末尾那個 `; true` 根本沒跑到。
`shoot.sh` 正常結束前會先 kill 一次 SRV,所以 trap 跑到時那個 pid **一定**是死的 ——
也就是說**它每一次成功都回傳 1**。沒人發現,是因為兩支的輸出一路都被 `| tail` 接走。

後果:`./shoot.sh a && ./shoot.sh b` 會在第一支之後停住,而訊息會說截圖失敗 —— 它明明成功了。
已改成 `if ... then kill ... || true; fi`,兩支都驗過退出碼是 0。

> **這一段的重點不是 bug,是那個「已修復」。**
>
> `block-03` 已經踩過這個坑一次 —— 在同一個檔案、距離三行的 PROVENANCE 區塊 ——
> 並且把結論寫成註解留了下來:「注意 `&&` 不能是這個區塊的最後一句」。
> 那一輪之後,這個坑在紀錄上是**修好的**。
>
> **兩件事都錯了:**
>
> 1. **範圍錯**:只修了踩到的那一句,**沒有回頭在同一個檔案裡 grep 一次同樣的形狀**。
>    三行外的 trap 裡就有一個,兩支腳本都有。
> 2. **結論錯**:那一輪留下的解法是「末尾補一個 `; true`」。
>    **實測擋不住** —— `set -e` 是在 `kill` 失敗的那一刻中止 trap 的,`true` 根本沒跑到。
>    真正有效的是把 `&&` 拆成 `if ... fi`。
>
> **一個錯的「已修復」比沒修更糟:它會讓下一個人不再看那裡。**
> 這個坑之所以活了一整輪,不是因為沒人知道它,是因為紀錄說它死了。
>
> **而它的訊號一直都在。** 退出碼從第一天起就是 1,只是兩支腳本的輸出
> 一路都被 `| tail` 之類的東西接走 —— **訊號活著,只是沒有人站在它的頻道上。**
> (這份文件裡至少有三個同形的故事:被當成噪音的 modified 產出檔、
> 永遠都在響所以沒人讀的落後警告、以及這一個。**檢查有沒有人在看,跟檢查有沒有輸出,是兩件事。**
> 後來還有兩個:被 `| grep` 濾掉的 build 錯誤,以及**發到頁尾去的使用者提示**,見第 7 條後面。)

### 7. 重編失敗會被 `| sed` 吞掉,腳本照樣往下跑、照樣說成功

**症狀:截圖用的是上一次留在樹裡的舊 `retro-modern.built.css`,而腳本回傳 0。**

```sh
( cd "$H/../design-system" && ./build.sh ... ) | sed 's/^/  build: /'
```

`set -e` 之下**管線的結果是最後一個指令(`sed`)的結果**,永遠是 0。
所以 `build.sh` 失敗時兩支腳本都會若無其事地往下跑。

`block-05` 是這樣撞到的:`SRC=` 指到 `git worktree`,那棵樹的 `design-system/`
沒有 `node_modules`,tailwind 解不到 `tailwindcss/theme.css` ——
**兩批歷史截圖全部是用舊產出截的,而腳本說成功。**

> 錯誤訊息**確實印出來了**,只是混在 build 的正常輸出裡,而那一段一路被
> `| grep`、`| tail` 接走。**訊號活著,沒有人站在它的頻道上** ——
> 這份文件裡第四個同形的故事。

#### 第五個,而且這一次那個沒人站的頻道是**使用者的**

前四個都長在工具或工程流程上。第五個長在產品裡,而且是自己剛加的功能:

```js
function flash(msg) { el("sync").textContent = msg; setTimeout(renderSync, 2800); }
```

`#sync` 在 **`<footer>`** 裡,是頁面最底下那行 10.5px 的等寬小字。
`geocode-place-and-confirm` 那一輪加了一句「這一筆沒填地點,不會出現在地圖上」,
規則二寫得很清楚:**送出的當下是唯一講得到的時機**。
訊息確實發了,而且是站上第 32 次用 `flash()` —— 程式完全正常,探針也綠。
**使用者在許願表單上按送出,眼睛在表單;那句話出現在頁尾。**

量出來的數字:按下送出的那一刻,`#sync` 在視線落點**下方 1017 像素**(390px 寬)。

> **這一次「頻道沒有人站」不是比喻,是可以量的。** 修法是把那三句改寫到
> 表單旁邊那個訊息槽(用站上現成的 `.note`,不是第三套機制),
> 而驗收的斷言也跟著從「flash 有沒有講」改成「**它出現在哪裡**」——
> `getBoundingClientRect()` 比對視線落點,不是比對「有沒有出現」。

**而最該記住的是那兩條斷言本身,不是那句提示搬到哪裡。**

上一輪的探針有兩條 `flashed(/沒填地點/)`,那一輪**全部通過**;
Lulu 在真機上回報的是「送出時沒有那句提示」。

> **兩邊都沒說謊:斷言問的是「有沒有發」,使用者問的是「看不看得到」。**

前四個同形的故事都是**訊號發了,但沒有人站在那個頻道上**。
這一個多了一層:**連探針都是綠的,因為探針站的是程式的頻道,不是人的。**
把斷言改成量 `getBoundingClientRect()`,等於**把探針搬到人的頻道上** ——
**那才是修好的部分,訊息換位置只是結果。**

推論,對之後每一輪都適用:**「這個行為有沒有發生」和「這個行為有沒有到達人」
是兩個斷言。** 只寫得出前者的時候,涵蓋率要照前者報,不要照後者報。
>
> **派工的人自己認的那一半更值得記:** 上一輪的指示是「站上已經有 `flash()`,
> 優先用它,不要長第三套」。那句話是對的,但**下指示的人沒有查它把字寫到哪裡**。
> 站上那 30 幾處 `flash()` 多半是「已刪除」「刪不掉」這種操作回饋,
> 看不看得到沒那麼要緊;而這一句是要人當下就讀到的。
> **用對了機制,用錯了位置** —— 而「這個站已經有這套東西」這種理由,
> 天生就會讓人跳過「它長在哪裡」這個問題。

**已修**:兩支都改成 `if ... > .build.log 2>&1; then ... else 印出來 + exit 3; fi`。
失敗訊息會告訴你 worktree 要怎麼接 `node_modules`。兩個方向都驗過。

> **而這一條被讀過之後照樣踩了一次。** `geocode-outside-and-notice` 那一輪要驗
> 「07 的門檻升回 7 之後,舊程式會不會真的失敗」,量退出碼寫的是
> `./shoot.sh ... 2>&1 | tail -14; echo "exit=$?"` —— `$?` 是 `tail` 的,印出 `0`,
> 而那一批畫面上明明就有一張 `✗`。不經過 pipe 重跑才看到 `4`。
> **那一輪的人讀過這一節,而且正是為了「兩個方向都要驗」才在量退出碼的。**
>
> **知道一條陷阱,跟在當下認出自己正踩在上面,是兩件事。**
> 這句話在這份文件裡已經被證實兩次了 —— 另一次是「量字體的四個陷阱」
> 那一節第 5a 條後面那句:「他不覺得自己在『量字體』,他覺得自己在『掃字元』」。
> **陷阱是按情境認出來的,不是按名字。** 所以每一條後面都要留下**症狀**,
> 不能只留下結論 —— 你下次碰到它的時候,手上只有症狀。

#### 同一輪裡這個形狀發作了第二次,而第二次是看的人自己造成的

`block-05` 把 `SRC=` 指錯目錄(指到 worktree 的根,不是它的 `tokyo-trip/`),
腳本在出處印完之後就因為找不到 `.work/index.html` 停住 ——
**而當時是用 `2>&1 | grep` 在看輸出的,traceback 被 grep 濾掉了**,
畫面上看起來像「它印完出處就沒事了」。`shots/` 底下只有一個 `PROVENANCE.txt`,一張圖都沒有。

> **用 `| grep` 看一支會失敗的腳本,等於自己把錯誤訊息關掉。**
>
> 這跟上面那個 `| sed` 吞掉退出碼是同一件事的兩面:
> **一邊是工具把訊號丟掉,一邊是看的人把頻道關掉。**
> 先看完整輸出,確定它會講話了,再開始過濾。

### 8. 正向斷言掛掉時,`shoot.sh` 以前回傳 **0**

`block-04` 修掉了「每一次成功都回傳 1」,**但沒有人檢查反過來的那一半** ——
在那之後不管幾張印 `✗`,它都還是回傳 0,只印一行警告。

也就是說 `./shoot.sh a && <比對>` 會在一批「有畫面是空的」的截圖上照樣往下跑,
**而差異式檢查對兩邊都空全綠。**「最貴的一個教訓」那一節在防的東西,出口一直開著。

**已修**:`FAILED` 時 `exit 4`。健康的一批 `exit=0`、有 `✗` 的一批 `exit=4`,都驗過。

> **「成功要回傳 0」和「失敗要回傳非零」是兩個斷言,驗一個不等於驗另一個。**
> 這是這個檔案裡第三次「修了踩到的那一半,沒有回頭問另一半」。

### 9. 位移搜尋找不到完全命中,**不一定**代表內容變了

元件的高度是小數時(例如 `.who` 的膠囊是 `64.781px`),被推到一個**小數距離**
就會換一個次像素相位,邊緣的反鋸齒跟著變,**任何整數位移都對不回去** ——
殘差長得像「有東西被改了」。

`block-05` 的對照:第三塊把 `.who` 推了 `39.344` CSS px(非整數)→ 殘差 2–3%;
第四塊推了整整 `12` CSS px(整數)→ **同一段逐位元組相同**。

**判準**:殘差集中在**邊緣**、比例零點幾到幾 %,而且同一次比對裡有另一塊完全命中
→ 那是相位。**要確定就去量幾何,不要繼續看像素** ——
`getBoundingClientRect()` 的小數點後三位一句話講完,而在像素上會繞很久。

### 10. 斷言和截圖**不是同一趟**,所以它們看的不是同一個時刻

**症狀:斷言說「只有 4 個」,而截圖裡明明有 7 個。**

`shoot.sh` 的每一張圖其實開兩次瀏覽器:一次拍照(virtual-time budget 25s 跑完的終態),
一次只為了問「畫面上真的有東西嗎」(動作之後 **900ms** 量)。
兩趟各自從零開始,所以**任何要等一次真實網路往返才會出現的東西,
可能在截圖裡有、在斷言那一刻還沒有。**

`geocode-place-and-confirm` 那一輪撞到:07 那三顆航班 pin 要等
`樂桃 MM626 · 建議起飛前 2.5 小時` 那兩次 Nominatim 回來才畫得出來(實測約 1.2s)。
以前不發作,是因為 fixture 快取裡的 pin 夠多、900ms 就過門檻了。

**處理原則:門檻只認「不靠網路就一定在」的那幾個。**
把門檻設成截圖上的數字,等於讓斷言變成網路的函數 —— 那就是「工具的陷阱 3」
(`tokyo5-pin3` 差一個字元)的形狀,只是這次長在時間上而不是 key 上。
數字因此調低的話,要**另外補一條不靠網路的斷言**把強度補回來(06 / 07 補的是 `.stop:3+`)。

> **這是這個專案招牌失敗模式的第三種長法:**
> 一次長在「量的寬度」(在 1100px 回答 390px 的問題)、
> 一次長在「量的 key」(`tokyo5-pin` vs `tokyo5-pin3`)、
> 這次長在「量的時刻」。**三次都不報錯,三次都給一個乾淨的答案。**

### 11. 量位置的時候,你量到的東西可能根本不在畫面上

**症狀:一個乾淨的數字,而它是從 0 算起的。**

`geocode-outside-and-notice` 那一輪要回答「那句提示出現在使用者看得到的地方嗎」,
而那是個幾何問題,所以用 `getBoundingClientRect()`。同一個判斷連續錯了三次,
三次都不報錯、三次都給出一個看起來很合理的數字:

| 量法 | 得到 | 為什麼是假的 |
| --- | --- | --- |
| 送出**之後**量送出鈕 | 到訊息 840 / 到頁尾 884 | 表單送出就 `hidden` 了,而 **`display:none` 的元素 rect 全是 0** —— 兩個數字都是從畫面頂端算起的絕對位置,不是距離 |
| 送出**之前**量按鈕、送出**之後**量訊息 | 到訊息 361 / 到頁尾 317 | 表單一收,底下整片往上移約一個表單的高度。**兩個位置不在同一個版面上就不能相減** —— 結論還會反過來:看起來訊息比頁尾還遠 |
| 兩個都對了,但容器是收起來的 `<details>` | 到訊息 0 / 到頁尾 **44** | 見下 |

第三個最陰:許願那一整塊住在 `<details class="wishbox">` 裡,而它預設是收起來的。
**現在的 Chrome 用 `::details-content` 的 `content-visibility:hidden` 實作收合,
不是 `display:none`** —— 裡面的元素照樣量得到一個 rect(而且有寬有高),
只是那個 rect 跟使用者看到的東西完全無關。`q("#wishbox").open` 是 `false`,
而 `getBoundingClientRect()` 給你 `{top: 839.9, width: 326, height: 58}`。

打開它之後,同一支探針量到的是 **0 / 1017** —— 訊息正好落在表單原本的位置,
而頁尾在一千像素之外。**那才是這一輪要回答的那個問題的答案。**

> **這是這個專案招牌失敗模式的第四種長法。** 前三種是量的寬度(在 1100px 回答
> 390px 的問題)、量的 key(`tokyo5-pin` vs `tokyo5-pin3`)、量的時刻(斷言和截圖
> 不是同一趟)。這一次是**量的版面** —— 而且它一次長了三種:
> 元素根本沒有盒子、兩個數字來自兩個版面、容器收起來但盒子還在。
>
> **判準**:任何 `getBoundingClientRect()` 出來的數字,先問三句 ——
> 這個元素現在畫得出來嗎?我相減的那兩個數字是同一個版面量的嗎?
> 它的祖先有沒有收起來的 `<details>` / `hidden` / `content-visibility`?
> **`rect` 是 0 不會報錯,它只會安靜地變成一個你信得過的距離。**

---

**附帶一條,很少見但會浪費你半小時:腳本執行中不要改腳本自己。**
sh 是按**位元組位移**邊讀邊執行的。我在第一隻還在跑的時候編輯了 `shoot.sh`,
位移一偏,那一隻就在半行的地方報 `syntax error near unexpected token`。
**檔案本身沒壞**,是跑到一半的那個行程被我改掉了 —— 看起來卻像是你剛剛把腳本改壞了。

---

---

---

### 12. 「順便確認沒弄壞」的斷言,期望值常常是從當下行為抄來的

`geocode-outside-and-notice` 那一輪的探針是 114 條全綠,其中兩條長這樣:

```js
ok("地點欄本身命中表的那幾筆沒有被弄壞(成田照樣標得出來)",
  await until(function () { return /不在這裡/.test(barText()); }), barText());
ok("成田那一筆的「再查一次」照樣畫得出來(它有 pin,使用者說不對還是能按)",
  q("#map-fix-go").hidden === false, null);
```

**那兩條斷言的內容就是 bug 的樣子。** 成田的地點欄命中人工表,照那一輪自己的決定
該講「用的是人工確認過的座標」而且收起「再查一次」—— 斷言卻要求它講「不在這裡?」、
按鈕還在。寫的人以為在做迴歸保護,實際上是**跑一次、看畫面現在長怎樣、抄下來**。

> **迴歸斷言的期望值要從「決定」推出來,不是從「現況」抄下來。**
> 從現況抄的那一條,在它保護的東西壞掉的時候照樣是綠的 —— 它保護的是現況,
> 而現況裡就有那個 bug。
>
> 判準:寫「順便確認 X 沒壞」的時候問自己 **「X 該是什麼樣子,我是從哪裡知道的?」**
> 答案如果是「我剛剛看到它是這樣」,那就還沒有斷言,只有一份快照。

同一輪還有第二個形狀:**那條路一條斷言都沒有。** 11 段測的全是「標題命中」,
`kp`(地點欄命中)那條路從頭到尾沒有被走過 —— 所以 114 條綠燈裡,
Lulu 撞到的那條路的覆蓋率是 0。**「幾條斷言」不回答「哪幾條路」。**

### 13. `until()` 等的字,上一次操作可能已經留在畫面上了

`geocode-via-in-cache` 那一輪,我寫的第一版是:

```js
mm.click();
ok("...", await until(function () { return /人工確認過的表裡/.test(barText()); }), barText());
```

**上一列(桃園機場報到)講的就是那句話**,所以 `until()` 在這一列重畫之前就通過了,
後面兩條斷言量到的是**上一列的字**。探針第一次跑就把它抓出來(那條「印的是地點欄
那幾個字」掛了),否則它會變成又一個乾淨的、錯的綠燈。

> **等待條件要用「這一列才有的字」**(這裡是 `/TPE 桃園 T1/`),不要用
> 「這個狀態會講的話」。畫面上留著的舊字會讓「等到了」跟「換好了」長得一模一樣。
>
> 這是陷阱 10(斷言和截圖不是同一趟)在單一支探針**內部**的版本:
> **兩次斷言之間,畫面也不是同一個時刻。**

### 14. 探針**同步**跑完的話,它的結論會被 `probe.sh` 自己的佔位值蓋掉

`probe.sh` 組出來的頁面長這樣:

```js
out = (function(){ …你的探針… })();
document.getElementById("r").textContent = JSON.stringify(out, null, 2);
```

非同步的探針(`geofix.js` / `seek.js` / `who.js` 都是)回傳一個佔位物件,
然後晚一點自己覆寫 `#r`。**但如果探針在第一個 `await` 之前就結束**,
它寫的 `#r` 發生在上面第二行**之前**,然後被佔位值蓋掉。

**症狀是探針回傳 `{說明:"非同步"}`** —— 看起來像「JS 在 iframe 裡就掛了」,
實際上它跑了、有結論、而結論被丟掉了。

#### 為什麼這一條比它看起來嚴重

**它只在探針「該變紅」的那棵樹上發作。**

探針會同步跑完,幾乎只有一個原因:**要量的東西不存在**,
第一個 `d.getElementById(…)` 就回 null 然後爆掉 —— 而那正是
**負向對照那一趟**(`SRC=` 指到改動之前的 commit)。

> 也就是說:**這是一個專門讓「檢查應該失敗」這件事失效的東西。**
> 負向對照會永遠看起來像「探針壞了」,而不是「這棵樹上沒有那個功能」。
> 然後下一個人會去修探針。
>
> 它跟「沒看過它失敗的檢查不算檢查」直接對撞:
> **如果那次「看它失敗」撞上這個,那次失敗本身就是假的。**

#### 修法

探針的第一件事是讓出一拍,不是量東西:

```js
(async function () {
  await sleep(0);          // 我的寫入一定在 probe.sh 那一行之後
  try { … } catch (e) { out.爆掉了 = String(e.stack || e); }
  document.getElementById("r").textContent = JSON.stringify(out, null, 2);
})();
```

順便加一道早退,讓負向對照在三條紅燈之後立刻交卷,不要每條 `until()` 各空轉 8 秒
(那會撐爆 `--virtual-time-budget`,結果同樣是拿不到結論):

```js
if (!q("[data-seek]")) { out.早退 = "這棵樹沒有這個功能,後面 N 條沒有東西可量"; throw new Error(out.早退); }
```

**`geofix.js` 和 `who.js` 是同樣的結構,只是目前不會撞上** —— 它們在第一個 `await`
之前不碰任何可能不存在的東西。下一個人加一條會同步爆的斷言就會踩到。
(`seek.js` 已經修了,可以照抄。)

### 15. zsh 會把 `git show $c:path` 裡的 `:a` / `:t` 當成歷史修飾符吃掉

**症狀:`fatal: ambiguous argument '…/97deecadopt-harness/…'` —— 看起來像那個 commit 不存在。**

```sh
for c in be3e0ef 97deeca; do git show $c:adopt-harness/api-geocode-test.js; done
#                                        ^^ 這個 a 被吃掉了
```

`:a`(絕對路徑)、`:t`(檔名)、`:h`、`:r`…… 都是 zsh 的參數修飾符,**雙引號也擋不住**
(`"$c:adopt-…"` 一樣會被吃)。`adopt-harness` 變成 `dopt-harness`,
而 git 的錯誤訊息講的是**改寫之後**那個東西,所以人會去查 commit 而不是查 shell。

修法:`"${c}":adopt-harness/…`。**判準:任何 `$var` 後面直接接 `:` 的地方,都加大括號。**

> **跟 `git worktree add` 因為分詞停在當前 HEAD 是同一個家族:
> shell 安靜地改寫了你給 git 的參數,而 git 回報的是改寫後的世界。**
> 兩個都不報「shell 有問題」,兩個都給一個關於 git 的、很有自信的錯答案 ——
> 這份文件裡那句「安靜地量到了別的東西」,這次長在**參數**上。

## 最貴的一個教訓

**差異式檢查對「兩邊都是空的」完全無感。**

工具壞掉的那次,每一頁都一致地空白,九張比對全綠。還跑了「同一狀態連跑兩次完全相同」
來證明工具是決定性的 —— **它確實是決定性的,只是決定性地壞掉。**

> **決定性是必要條件,不是充分條件。**

而且因為這個,假資料裡三筆花費**從來沒渲染出來過**,花費那頁在前幾輪一直是空白的。
接線那輪的結論沒有錯(其他八張是實的),但覆蓋率比當時報的低。
**結論對和驗證夠強是兩件事。**

### 補救:正向斷言

每張圖另外附一條「這一頁至少要有這些東西」,絕對式的,不依賴任何基準。
斷言寫在 `shoot.sh` 最底下每個 `shot` 呼叫的最後一個參數。

| 畫面 | 斷言 | 健康時實測 |
| --- | --- | --- |
| 01 / 02 行程 | `.stop:3+` | 3 |
| 03 花費 | `.exp:3+,.rm-chip:3+,.catrow:2+` | 3 / 3 / 2 |
| 04 分帳 | `.fp:5+` | 5 |
| 05 許願 | `.wish:3+` | 3 |
| 06 地圖(當日) | `.map .pin:3+,.stop:3+` | 3(截圖上也是 3)/ 3 |
| 07 地圖(許願) | `.map .pin:7+,.stop:3+` | 7 —— 一度降到 4,又升回來了,見下 |
| 08 加行程 | `#stop-form:not([hidden]) input:3+,.stop:3+` | 3 / 3 |
| 09 編輯 | `#edit-overlay:not([hidden]) #edit-form input:3+,.stop:3+` | 5 / 3 |
| 10 記一筆花費 | `#exp-form:not([hidden]) .rm-input:7+,#exp-form .who label:4+` | 7 / 5 |
| 11 改一筆花費 | `#exp-edit-overlay:not([hidden]) .rm-input:7+,#exp-edit-overlay .who label:4+` | 7 / 5 |

> **07 現在是 7,而它比 4 更強,不是更鬆。** `geocode-outside-and-notice` 那一輪
> 修好了「規則一連帶關掉人工表」之後,**許願地圖那條路一次網路都不碰** ——
> 七顆 pin 全部來自 fixture 快取或 `OUTSIDE` 人工表,900ms 就到齊。
> 判準(「只認不靠網路就一定在的那幾顆」)一個字都沒有放寬,是那條路真的不靠網路了。
> **兩個方向都驗過**:改完之後健康的一批 `exit=0`、07 印 `✓`;
> 拿 `SRC=` 指到修之前那個 commit 跑同一支,07 印
> `✗ .map .pin 只有 4 個(至少要 7)`,整支 `exit=4`。
> 哪天那條路又需要一次網路往返,這條斷言會先紅 —— 那是對的。
>
> **下面這一段是它降到 4 的那一輪寫的,留著,因為判準本身沒有變。**
>
> **06 / 07 的門檻在「沒填地點就不查」那一輪重設過,而且不是單純調低。**
> 那一輪之後,拿標題查出來的兩顆 pin(合羽橋道具街、teamLab)不再存在,
> 截圖上 06 是 3 顆、07 是 7 顆。但**斷言和截圖是兩趟不同的瀏覽器**:
> 截圖拍的是 virtual-time budget(25s)跑完的終態,斷言是在動作後 **900ms** 量的,
> 而 07 那三顆航班點要等一次真的 Nominatim 回應才畫得出來(約 1.2s)。
> 所以**門檻只認「不靠網路就一定在」的那幾顆**(07 是 4,不是 7)——
> 設成 7 等於讓斷言變成網路的函數,那正是 `tokyo5-pin3` 那個坑的形狀。
> 同時兩張各補一條 `.stop:3+`:純調低數字會削弱「app 死掉也照樣過」那道防線
> (07 以前就是門檻 2 太低而漏接過),而那三筆行程是 fixture 的資料,
> app 沒真的跑起來就不存在。**門檻降了,斷言反而更強。**
> 詳見「工具的陷阱 10」。

> **10 / 11 的第二條斷言故意挑 `.who label`,不挑 `input`。**
> `ef-who` / `xe-who` 在 markup 裡是**空的 `<div>`**,五顆膠囊是 JS 產生的
> (`index.html:2447` / `:2496`)—— 符合「斷言要挑只有 app 真的跑起來才會存在的東西」。
> 第一條的 `.rm-input` 則順便把第四塊綁進來:第四塊被回退,10 / 11 會印 `✗`。
> (副作用:拿 `SRC=` 去截**第四塊之前**的 commit 時,10 / 11 一定印 `✗`,
> 那是對的,不是工具壞了。)

**這條防線本身驗過了**,兩個方向都看過:
健康時九張全 `✓`,把第 2 條那個壞掉的 Date shim 放回去之後九張全 `✗`。

> **03 的斷言含 `.rm-chip:3+`,所以第一塊如果被回退,03 會印 `✗`。**
> 那是刻意的 —— 轉換開始之後要同時防「該變的沒變」,而這條順便把它綁進來了。
> 看到 03 `✗` 先確認第一塊還在,不要先當成工具壞了。

#### 收緊過的四條,以及為什麼

負向對照的第一次結果是**九張裡有三張漏接** —— app 已經死到標題列印 `NaN 天`、
行程花費許願全空,07 / 08 / 09 還是印 `✓`。原因各不相同:

- **08**:`#stop-form input` 量的是**靜態 markup**。那三個 input 本來就寫在 HTML 裡,
  app 開不開機都在;而且 `#stop-form` 預設帶 `hidden`,表單根本沒打開也算數
- **09**:同上,而且更嚴重 —— 09 的動作是 `d.querySelector('[data-edit-stop]').click()`。
  app 死掉時根本沒有 `[data-edit-stop]`,這行丟例外、被 `try/catch` 吃掉、
  **對話框從頭到尾沒開過,而斷言還是過。那條斷言從來沒有驗到它名義上要驗的東西。**
- **07**:門檻 2 太低。app 死掉時航班那兩個寫死的成田機場點照樣畫得出來
  (走 `OUTSIDE` 硬表,不經過 app 的資料層),剛好湊到 2

教訓:**斷言要挑「只有 app 真的跑起來才會存在」的東西。** 靜態 markup 不算。
量「容器開了沒」(`:not([hidden])`)比量「裡面有幾個 input」誠實。

#### 解析器自己的坑

斷言字串原本是 `spec.split(":")` 取 `p[1]` 當門檻。選擇器裡只要出現 `:not()`
這種偽類,門檻就會被切成字串,`parseInt` 得 `NaN`,而 **`n < NaN` 永遠是 false** ——
斷言不報錯,只是從此不再檢查任何東西。上面要加的 `:not([hidden])` 剛好會踩到。

已改成從**最後一個**冒號切,而且門檻讀不出數字時明確報錯。
**這個專案的招牌失敗模式,那次是埋在檢查器自己的字串解析裡。**

#### 涵蓋範圍到哪裡

負向對照只做了「app 完全沒開機」這一種壞法。**沒有**測「只有某一塊 CSS 沒生效」
那種局部壞法 —— 正向斷言本來也不是為那個設計的(那是差異式檢查的工作)。
全部是 headless Chrome,沒跑過真實裝置或真實 Safari。
(唯一的例外是第四塊那次由 Lulu 在 iPhone 上做的對焦測試,見上面那一節 ——
那是人做的,不是這套工具做的,而且只涵蓋「會不會放大」這一件事。)

> **而「沒跑過真實 iOS」這一句在第四塊變成了一個具體的欠款,後來由人還掉了。**
> 那一塊整個決定(保留 `:521` 的 16px)就是為了防 iOS Safari 對焦放大,
> **而這套工具在定義上驗不到它** —— 能證明的只有「觸發條件不成立」(font-size 是 16px),
> 不是「行為沒發生」。見最上面「第四塊留下一件要真人做的事」。
> **有人要拿 iPhone 點一下。**

#### 那九張截到了哪些狀態,又漏了哪些

**這一節是 `block-02-mono` 那一輪撞出來的。** 在那之前沒有人寫下來過,
而它造成的症狀非常像「該變的沒變」:

| 狀態 | 有沒有被截到 |
| --- | --- |
| 行程 / 花費 / 分帳三個分頁 | ✅ 01–04 |
| 許願清單(`#wishbox` 展開) | ✅ 05 |
| 地圖(當日 / 許願) | ✅ 06 / 07 |
| 新增行程表單、編輯對話框 | ✅ 08 / 09 |
| **記一筆花費 `#exp-form`、改一筆花費 `#exp-edit-overlay`** | ✅ **10 / 11**(`block-05` 補的)。16 個欄位、10 顆成員膠囊、10 個核取方塊都有畫面了 |
| **航班板 `<details class="board">`,手機寬度** | ❌ **從來沒有** |
| 手機寬度 + 任何 `<details>` 展開 | ❌ 沒有 |
| **桌機(1100px)打開任何表單** | ❌ **從來沒有**。02 / 07 是 1100px,但兩張都沒有表單是開的 —— 第四塊量到的那一整套桌機值仍然零截圖 |
| **`.who` 的「沒勾選」狀態** | ❌ 沒有。fixture 三筆花費都是全員分攤,10 個核取方塊在圖上全是勾起來的 |
| **JS 產生的 `rn-*` 改名欄位** | ❌ 沒有,而且探針也量不到 |
| **任何對焦以外的互動狀態**(hover / 展開 / 錯誤訊息) | ❌ 沒有。08 的第一個欄位和 11 的 `xe-title` 是對焦的,那是 app 自己 focus 的,不是刻意截的 |
| 深色 / 高對比 / 縮放設定 | ❌ 沒有 |
| Google Fonts 載入失敗(斷網)的樣子 | ❌ 沒有 |

**29 個靜態欄位控制項的截圖涵蓋率:9/29 → 23/29**(08 的 4 + 09 的 5 + 10 的 7 + 11 的 7)。

**`<details class="board">` 預設是關的。** 它在 02 / 07 兩張桌機圖裡是展開的,
在三張 390px 手機圖裡從來沒有。裡面有 **4 個吃 `--font-mono` 的中文元素**。

> **最容易被誤判的地方:關著的 `<details>` 裡的元素照樣有 computed style。**
> 你在 DOM 上盤點時它會被算進去、進了「預期會變」清單,然後在截圖裡永遠不出現 ——
> **看起來跟「該變的沒變」一模一樣,而那是驗收規則第 4 點要抓的東西。**
>
> 盤點完要再問一句:**這個元素在哪一張截圖裡真的畫得出來?**
> 答不出來的,就不要寫進那一輪的預期清單,改寫進「沒驗到的事」。

---

## 探針:各支現在幾條,以及在哪個寬度量的

> **條數要連寬度一起寫。** `sheet-grab` 用預設寬度跑會回一個空的
> `{說明: "非同步"}` —— 不是紅燈,是**沒有結論**,而那很容易被當成 0 條通過。
> 它的檔頭寫著 `WIDTH=390`,因為它量的是高度,那幾條在寬畫面下本來就不成立。

| 探針 | 條數 | 寬度 | 量的時候 |
| --- | --- | --- | --- |
| `seek` | 63 | 預設 | `026c0e1` |
| `geofix` | 78 | 預設 | `026c0e1` |
| `escalate` | 46 | 預設 | `026c0e1` |
| `drawer` | 14 | 預設 | `026c0e1` |
| `sheet-grab` | **12** | **`WIDTH=390`** | `026c0e1` |

`drawer` 和 `geofix` 在別的寬度下條數不一樣(`drawer` 在 1440 是 10、≤640 整支跳過;
`geofix` 在 390/1440 是 77)。**這張表只登記我自己量過的那一組**,
別的寬度要用的人自己跑一次 —— 這個 repo 已經因為抄別人的數字吃過虧。

## 基準:哪幾組還能用

`shots/` 不進版控,所以這張表講的是**如果它們還在**。被刪掉就重跑,
但重跑出來的是「現在的工作目錄」,不是當初那個 commit。

> **「最新的乾淨基準」這幾個字在下面出現了好幾次,而它們是不同輪各自寫的。**
> 每一輪都只加自己那一列、沒有把上一列的「最新」拿掉,所以表裡同時有三、四組
> 自稱最新。**真正要看的是最下面那一行「下一塊的基準用 ____」** ——
> 表格是歷史,那一行才是現在。

| 組 | 狀態 |
| --- | --- |
| `shots/base-6d0184d` | ✅ **最新的基準**(`6d0184d` = main,21 張,工作樹乾淨)。圖層鈕改名分組(`全部已排`·`Day n`｜`大家的許願`)、許願頁跟日期排斷開、兩顆地圖鈕刪掉之後拍的。**`05-wishes-mobile` 在這一組之前拍的都不是許願頁** —— 見下面 |
| `shots/base-33f8eb1` | 🔸 **上一組**(21 張,`33f8eb1`,窄桌機剛併進手機版面時)
| `shots/base-093867e` | 🔸 **上一組**(21 張,`093867e`,窄桌機還有抽屜的年代)
| `shots/base-16f0e43` | 🔸 **上一組**(21 張,`16f0e43`,合併今天那三條 PR 之前)。跟 `base-093867e` 比有 16 張變,絕大部分是地圖取景改了(行程頁預設只亮「這一天」那層)|
| `shots/base-123b588` | 🔸 **上一組**(20 張,`123b588`)。跟它比有 5 張變:02／07／21 是窄桌機的許願從「底部一條 bar」變成分頁,17／20 是那顆空的拖曳把手拿掉之後底下的東西整個往上移 —— **五張都是預期的,而且都有截圖可以看** |
| `shots/base-1616ffc` | 🔸 **上一組,沒有壞掉,只是少一張**(19 張,`1616ffc`)。拿它跟 `base-123b588` 比是 20 張裡 19 張一模一樣、1 張新增 —— **那正是「連拍兩次會拿到同一批圖」在跨 commit 上的第二次驗證** |
| `shots/base-bc0adba` | ⚠ **作廢**:18 張,沒有 `19-fly-mobile`,而且它是在圖磚還用「載進來再換掉」的年代截的 —— 那個做法會輸給網路,所以這一組裡有幾張拍到的是真的地圖、幾張是灰底,**它自己跟自己都對不起來** |
| `shots/b9-main` | ⚠ **作廢**:截於 `b9aa65c`,而在那之後手機版整個翻過來(地圖變底、清單變三段式 sheet、許願獨立成分頁)、頂列縮短、連線那一條收進漢堡選單、願望小卡的「改／刪掉」改成左滑。**18 張全部都不一樣** |
| `shots/adminwish-on-main` | ⚠ **作廢**:16 張,而完整一組現在是 18 張(`17-plan-three-cols`、`18-wish-add` 是後來加的),中間還隔了兩輪版面大改(機票獨立成「搭機」分頁、加行程和加願望變成對話框、許願在窄畫面是底部 sheet、「＋許願」搬到標頭)。拿它對帳會在**一堆正確的改動**上看到 13 張差異 |
| `shots/base18` | 🔸 **分支上截的,不是基準**(`026c0e1` = `fix/one-baseline-name` 疊在 `01e0575`,18 張全綠)。它描述的是「main 加上那一支文件改動」那個世界,而那一支對 `tokyo-trip/` 和 `design-system/` 零改動(兩邊各自量過)—— 所以它等同 `01e0575`。**留著只為了一件事**:`shots/b9-main` 拿它比出來的 13 張差異,就是 `01e0575 → b9aa65c` 之間畫面動的全部。**它不該被拿來當對帳的基準** —— 理由見下面那一則「基準的登記屬於誰」 |
| `shots/mapfix-retire` | ⚠ **不是 main 的基準,雖然它看起來最新**。它在 `feat/mapfix-retire` 上截的,而那條從 `ad57b82` 長出來 —— **PR #2(搜尋升級流程改版)不在裡面**,所以只有 14 張、沒有 15/16,而且 `14-seek-strong` 拍到的是改版**前**的樣子。拿它對帳會在一個正確的改動上看到 3% 的差異。它證明的是「`#map-fix` 拆掉之後版面沒破」,僅此而已 |
| `shots/adminwish` | ⚠ **作廢**:`53e09d9` 那個 commit 在 rebase 之後已經不存在,而且它是疊在 `#map-fix` 還在的世界上截的。它當時量到的東西(`05` 差 2.6% 是三筆願望都長出「改」、`13` 差 69 列是對話框**後面**那張模糊清單多了一顆「改」,對話框本身沒變)在 `shots/adminwish-on-main` 上重新量過,結論一樣 —— **所以撤回這一列不會弄丟任何結論**。這一列以前是 ✅「最新的乾淨基準」,而作廢那一列是後來加上去的,**兩列並存過一陣子** —— 見下面那一則 |
| `shots/esc-clean` | ✅ **最新的乾淨基準**(`7a8f42e`,搜尋三段式那一輪之後,**14 張**,第 14 張 `14-seek-strong` 是新的)。與 `shots/sync-post` 比:九張逐位元組相同,`11` 落在已登記雙穩態(8px / 6 列),`12` `13` 是這一輪改的畫面,**`08` 差 34 列是提示字從「淺草寺」換成「地名/車站/建築物/活動名等」** —— 預期內,但那一輪的筆記寫的是「只有 13 變」,漏了它 |
| `shots/esc` … `shots/esc5` | ⚠ **都不是基準**:五組全部在工作樹有 2–4 個檔未提交的狀態下截的,而且最新的 `esc5` 停在 `31b9bfd`(落後分支頂端三個 commit)、14 張。留著當過程紀錄。**「那一輪截過圖」不等於「那一輪留下了基準」** |
| `shots/sync-post` | ✅ **最新的乾淨基準**(`18b7525` = `origin/main` 併進 `feat/wish-edit` 之後,工作樹乾淨,**13 張**)。同步之後重截的一組,`13-wish-edit` 在裡面。**「快照模式下的『改』」那一輪拿它當基準,13 張逐位元組零差異** |
| `shots/sync-pre` | 🔸 同步**之前**那一組(`cf93f82`,13 張)。留著當「合併進來的東西改了什麼畫面」的左邊 |
| `shots/sync-mainonly` | ⚠ 負向對照,不是基準:`SRC=` 指到 `main`(`6fc932f`)的 worktree。**檔案有 13 個,但 `main` 上沒有許願編輯**(查過:`git show main:tokyo-trip/index.html | grep -c data-edit-wish` → `0`),所以第 13 張拍到的是一張沒有那張表的畫面,它那三條斷言在那一組上是 `✗`。**`shoot.sh` 永遠產 13 個檔,檔案存在不代表那一張有東西** —— 跟 `shots/expform-pre-*` 同一種 |
| `shots/snapguard` | ✅ 「改不可以編輯一份凍結的副本」那一輪之後,13 張,**與 `shots/sync-post` 逐位元組全部相同**(預期之內:快照模式不在任何一張截圖裡) |
| `shots/we-final` | ✅ 許願編輯那一輪之後的乾淨基準(13 張)。與 `shots/we-after2` 13 張逐位元組相同 |
| `shots/pwa-main` | ✅ **最新的乾淨基準**(`feat/pwa`,12 張)。與 `shots/main-base` **十一張逐位元組相同**,`01` 落在已登記雙穩態(`2.1481%` / `y=454..651` / 1 群 / `--shiftx` 搜到 `dx=-27` **殘差 0**,四項全中)。那一輪只動了 head 的標籤、離線那條路和頁尾那句話,畫面本來就不該變 |
| `shots/main-base` | ✅ 對照組:`SRC=` 指到 `main`(`d71b510`)的乾淨 worktree。**`main` 上沒有 `13-wish-edit`**(許願編輯還在 `feat/wish-edit`,沒合併),所以這一組只有 12 張 |
| `shots/pwa` / `shots/pwa2` | ⚠ **基準挑錯的紀錄,不是基準**。PWA 那一輪一開始疊在 `feat/wish-edit` 上截的,後來整批搬到從 `main` 開的 `feat/pwa`,所以這兩組的來源分支已經不是工作所在。`pwa` 的 `11` 落在雙穩態另一側、`pwa2` 跳回去,留著當「那條 8px 判準真的管用」的證據 |
| `shots/seek3` | ✅ **最新的乾淨基準**(地點搜尋之後,**12 張**)。連截兩次十二張逐位元組相同;與 `shots/who2` 只差 08 / 09(加上新的 12) |
| `shots/who2` | ✅ 上一個乾淨基準(第六塊之後,11 張)。連截兩次**十一張全部逐位元組相同**;與 `shots/fuji-split` 只差 10 / 11 兩張 |
| `shots/who-negctl` | ⚠ 負向對照,不是基準:`SRC=` 指到第六塊之前 + 收緊後的斷言,10 / 11 兩張印 `label.rm-chip 只有 0 個` |
| `shots/merge3` | ✅ **最新的乾淨基準**(兩欄合併之後,12 張)。`05` 和 `11` 各自落在已登記的雙穩態上(連截三次:05 得到兩個值、01 穩定) |
| `shots/seek3` | ✅ 上一個乾淨基準(地點搜尋之後,12 張) |
| `shots/fuji-split` | ⚠ 更早的基準(`table-vs-apis` 之後)。`01`–`10` 與 `shots/via-fix` 逐位元組相同,`11` 落在已登記雙穩態 |
| `shots/via-fix` | ✅ 上一個乾淨基準(`geocode-via-in-cache` 之後)。`01`–`10` 與 `shots/gx-after` 逐位元組相同,`11` 落在已登記雙穩態的狀態 A。連跑兩次,只有 `11` 在兩個狀態間跳 |
| `shots/psf-rep1` / `psf-rep2` / `prefix-samefixture` | ⚠ 負向對照,不是基準:`SRC=` 指到修之前那個 commit + **這一輪的新 fixture**,三趟雜湊完全相同(`cc553fca`),07 印的是 `行程 8`(多兩顆幽靈 pin)。留著當「修好之前真的不一樣、而且那個不一樣是決定性的」的證據 |
| `shots/gx-after` | ✅ **最新的乾淨基準**(`geocode-outside-and-notice` 之後,工作樹只有那一輪自己的檔)。**11 張全 `✓`**,連跑三次逐位元組相同;`01`–`10` 與 `shots/gx-base` / `gc-after` / `expform` 也逐位元組相同,`11` 落在已登記雙穩態的狀態 A(= `shots/expform` 那一邊) |
| `shots/gx-negctl` | ⚠ 負向對照,不是基準:用 `SRC=` 指到修之前那個 commit + **新的** 07 門檻(7)跑出來的,07 印 `✗ 只有 4 個`,整支 `exit=4`。留著當「門檻升回去是有東西撐著的」那句話的證據 |
| `shots/expform` | ✅ 上一個乾淨基準(`3b18468`,工作樹只有 `adopt-harness/`),**11 張全 `✓`**,連跑兩次逐位元組相同 |
| `shots/b5-base` | ✅ 同一個 HEAD 的九張,**與 `shots/input` 逐位元組相同** —— `block-05` 的基準複驗 |
| `shots/expform-pre-label` / `expform-pre-input` | 🔸 `8b4ee7c` / `957d829` 用 `SRC=` 截的歷史組,**10 / 11 的斷言會印 `✗`(`.rm-input` 那時不存在)**。只拿來比 `.who`,不是基準 |
| `shots/input` | ✅ 第四塊之後,九張全 `✓`(沒有 10 / 11) |
| `shots/pre-input-2` | ✅ 第四塊之前(`957d829`,工作樹乾淨),**與 `shots/label` 逐位元組相同** |
| `shots/pre-input` | ⚠ 同一個狀態的第一次,**01 落在雙穩態的另一側**。留著當那件事的證據,不要當基準 |
| `shots/label` | ✅ 第三塊之後,九張全 `✓` |
| `shots/pre-label` | ✅ 第三塊之前(`8b4ee7c`,工作樹乾淨),九張全 `✓` |
| `shots/pre-mono` / `mono-cjk` | 第二塊的前後,仍然有效 |
| `shots/final` | 第一塊之後的乾淨基準,九張全 `✓` |
| `shots/block01` | ⚠ **06 / 07 作廢**(網路產物),01–05、08、09 仍然有效 |
| `shots/wired` | ⚠ 同上 |
| `shots/negctl2` | 負向對照的證據,九張全 `✗`,不是基準 |

**下一塊的基準用 `shots/base-6d0184d`**(最新的一組,**21 張**,`6d0184d`,工作樹乾淨,從 main 截的)。
比對一律用 `./diff-shots.py 舊 新`,**不要用 `cmp`** —— 理由在下面那一則。

> **`05-wishes-mobile` 拍錯頁拍了好幾輪。** 它的腳本是「把 `#wishbox` 打開」——
> 那是許願還住在行程頁裡面的年代。許願變成分頁之後那一行什麼都沒做,
> 於是這張叫「許願頁」的圖,拍的一直是**行程頁**。
> 而它的正向斷言 `.wish:3+` **照樣過** —— 願望清單一直在 DOM 裡,只是那一欄沒露出來。
> 修法有兩半:腳本改成切到分頁,斷言改成數 `#wishbox` **裡面**的卡片。
> 教訓跟探針那邊是同一條:**斷言要綁在「使用者看得到的那一塊」上**,
> 不是「這份文件裡有沒有這個東西」。

> **「差幾個像素」這個判準本身錯過一次,值得記下來。**
> 窄桌機併進手機版面之後,同一個 commit 拍兩次有 12 張不一樣,看起來像災難。
> 換成「其中有幾個像素**看得出來**」(單一通道差超過 24)之後:
> **10 張的答案是 0 個** —— 整頁的字在次像素位置上飄一點,每個字的邊緣都被算進去,
> 於是 `03-cost-mobile` 堆出 31954 個「不同的像素」,而兩張圖並排看完全一樣。
>
> 真的在動的只有 `01-plan-mobile` 和 `05-wishes-mobile`,各約 8975 個看得出來的像素 ——
> **就是下面那一則講的地圖取景**,而且正好是地圖躲在清單後面的那兩頁。
>
> 教訓:**數量和強度是兩件事。** 一個真的移動是「像素不多,但每一個都差很多」;
> 次像素抖動是「像素很多,但每一個都差一點點」。只看數量,兩者長得一樣。

> **那個「拍兩次一樣」的性質,在 2026-09-23 合併之後又破了兩張。**
> `05-wishes-mobile` 差 17987 個像素、`08-addstop-form` 差 7967 個 —— 都不是反鋸齒。
> 我把兩張的差異區段切出來看過:**動的是地圖上那個點畫在哪裡**(「淺草寺參拜」
> 的標籤和圓點差幾像素),不是版面、不是文字。
>
> **原因**:新的取景邏輯會去量「清單那張 sheet 蓋住地圖多少」來決定畫面中心,
> 而截圖那一刻那個高度還沒安定,於是每次量到的值略有不同。
> 這跟 `sheet-grab` 那三條掛掉是同一個根源的兩面:那邊是高度沒到位,這邊是
> **讀到一個沒到位的高度**。
>
> **排除掉的**:日期那一排的水平捲動(量過三次都是 87,完全穩定 —— 我一開始
> 判斷是它,錯了);字體(三次都 `loaded`)。
> **試過但更糟的**:在截圖前補發一次 `resize` 事件叫程式重算 —— 不穩的張數
> 從 2 變成 7,已經退掉。
>
> **真機是好的 —— Lulu 在手機上拖過了(2026-09-23),「拖得動啊」。**
> 所以「高度永遠到不了」只發生在這個無頭環境裡,不是使用者會遇到的事。
> 這一條寫下來是為了**不要再查一次**:下一個看到 `sheet-grab` 把過場關掉的人,
> 會想知道那是在遮一個真的 bug 還是在繞過工具的限制 —— 答案是後者,問過真機了。
>
> **剩下的那件事只影響截圖的可比性**:取景應該讀那張 sheet 的**目標高度**(`--det`),
> 不是它當下正在變化中的高度。那段是 2026-09-23 從遠端併進來的,
> 這一輪的決定是「以遠端為準」,所以我沒有動它 —— 它是下一塊該做的事。

> **這一組是第一組「同一個 commit 連拍兩次會拿到同一批圖」的基準。**
> 之前不是。拿 `bc0adba` 連拍兩次,有 7 張不一樣 —— 而那 7 張的差異,
> 跟一個真的改動長得一模一樣。三個原因:
>
> 1. **日期那一排每次停在不同的位置。** 它在畫完的當下置中,而字體晚一步才到;
>    字體一到每張卡變寬,剛算好的置中就整排偏掉。**這一條是程式的 bug,不是截圖的** ——
>    網路慢的時候真的會看到選到的那一天半截在畫面外。修在 `document.fonts.ready`。
> 2. **擋圖磚的做法本身是一場賽跑**:等圖磚載進來再把 `src` 換掉,有時候會輸,
>    於是同一頁一次是灰底、一次是真的地圖,差 61 萬個像素。改成一條 CSS 讓它隱形,
>    不管什麼時候到都一樣 —— 順帶補上搜尋候選的縮圖,舊的選擇器沒涵蓋它們。
> 3. 剩下的是文字反鋸齒,02 差 21 個像素、04 差 1177 個,兩張圖並排看完全一樣。
>
> 所以比對從今以後用 `./diff-shots.py 舊 新` —— 它數像素、分三級,
> 不再回答「一不一樣」。`cmp` 在這一批圖上永遠會紅,而「反正那張本來就會紅」
> 正是一個真的迴歸可以躲進去的地方。
>
> **`shots/base-bc0adba` → `shots/base-1616ffc` 是 19 張裡 15 張變,那是預期的,
> 但原因不是版面改了** —— 是上面第 2 點:擋圖磚的方式換了,每一張有地圖的都整片變。
> 真正的版面改動只有兩件:機票卡進「搭機」就是攤開的,和多了 `19-fly-mobile` 這張新圖。

> **`shots/b9-main` → `shots/base-bc0adba` 是 18 張全變,那是預期的。**
> 這一輪動到**頂列**:兩行刪掉、高度 79 → 57、倒數改成「倒數 N 天」而 N 用 primary 色。
> **頂列在每一張圖上都有**,它一變,底下所有東西跟著位移 —— 所以沒有一張會相同。
> 看到 18/18 不要以為出事;要擔心的反而是**只有幾張變**,那才表示有東西沒套用到。
>
> **這一組重拍過一次,原因值得寫下來。** 第一次拍完之後我才 `git commit --amend`
> 改寫了那個 commit,於是 PROVENANCE 記的 `1f08536` **只活在我本機的 reflog、
> 遠端查不到**。圖是對的(兩棵樹 `git diff` 完全一樣),但**基準的價值在那張紙** ——
> 一組指向查不到的 commit 的圖,跟沒有登記差別不大。
> **判準:改寫歷史之前先確認沒有東西正在拍。**

> **`shots/base18` 和 `shots/b9-main` 之間有 13 張不同,那是正常的,不是誰弄壞了。**
> 變的:01 02 05 07 08 09 12 13 14 15 16 17 18;沒變的:03 04 06 10 11。
>
> **原因不是對話框的定位改了。** 第一次追這件事的人(我們兩個都是)會在 diff 裡看到
> `b9aa65c` 那條「對話框置中於扣掉抽屜之後那一塊」,覺得解釋得通就停在那裡 ——
> **而那條規則寫在 `@media (min-width:1280px)` 裡面,13 張裡有 10 張是 390 拍的。**
> 那個反證比結論有用:它讓下一個人有辦法自己推翻我們。
>
> 真正的原因是「＋許願」搬到標頭:那顆按鈕原本 `display:flex` 獨佔一整列、
> 底下還有 `margin-bottom:15px`,整條消失之後許願那一塊縮短,**底下每一張圖跟著位移**。
> 那八張對話框變的是**後面那張頁面**,不是對話框本身;13／15／18 超過 14%,
> 因為那三張畫面裡許願區佔的比例最大。
>
> 這 13 張是**兩個 session 各自截、各自比,得到同一批檔案** —— 不同的樹、不同的流程。
> **基準的登記屬於把東西送上 `main` 的那個人,不屬於旁邊那條分支。**
>
> 這一輪連續三次登記了自己截的基準,而三次都在 main 下一次動的時候就過期:
> `shots/adminwish` → `shots/adminwish-on-main` → `shots/base18`。
> **原因不是誰跑得不夠快,是位置錯了** —— 在一條還沒合併的分支上截的基準,
> 描述的是「main 加上我這一支」那個世界,而那個世界只在合併的瞬間存在過一次。
> main 一動它就作廢,而作廢的基準跟有效的基準**長得一模一樣**。
>
> 所以判準是:**誰把改動落在 main 上,誰重截、誰登記**,在同一個 commit 裡。
> 旁邊那條分支要驗自己的改動,截了自己用就好,**不要去動這張表**。
> (`shots/base18` 這一列是照舊制留下的,它寫明了對應 `01e0575`,所以仍然讀得懂 ——
>  但它不該是後面那些人拿來對帳的那一組。)

> **這一行原本在這裡寫了第二次。** 合併的時候兩邊各自更新了「下一塊的基準用 ___」——
> 一個指 `shots/b9-main`、一個指 `shots/base18`,而 git 沒有衝突,因為兩行離得夠遠。
> **整張表最重要的那一行,合併之後自相矛盾,而且沒有任何工具在看。**
> 撤回這一行,唯一的那一行在上面(`shots/b9-main`);`one-verdict.py` 加了一條檢查,
> 現在它會擋住第二行出現。
會動到地圖的話絕對不要用 `block01`。

> 這一行以前寫的是 `shots/merge3`(12 張),而在那之後至少又有四輪
> (`wish-edit`、`pwa`、`drawer-steps`、同步)產生過更新的乾淨基準,
> 沒有人回頭改它 —— **「唯一該寫基準名的地方」只有在真的有人去改的時候才成立。**
> 判準補一句:**改基準名跟截那組圖是同一件事的兩半**,不是兩件事。

> **同一個名字在這張表裡被判過兩次,一次作廢一次「最新」。** `shots/adminwish`
> 在 rebase 之後作廢,而我**加了一列作廢、沒有把原本那列拿掉** —— 於是表裡
> 相鄰兩列對同一組圖給出相反的結論,而下一個人會讀到哪一列取決於他從哪掃起。
> **加一列說「它不能用」不等於拿掉那列說「它是最新的」**:前者是補充,後者才是撤回。
> 判準:改一組圖的狀態時,搜的是那個**名字**出現過幾次,不是「我記得寫在哪」。

> **這一行是這份文件唯一該寫基準名的地方。** 最上面那張索引以前也寫了一個,
> 然後只更新了這裡 —— `geocode-via-in-cache` 那一輪照索引拿了 `shots/expform`,
> 06 差 2.3%、07 差 127px,花了四趟截圖才分清楚那是基準過期不是改壞。
> **同一個每輪都會變的事實,不要寫在兩個地方。**

> **別人遞給你的基準要自己驗一次。** `block-03-label` 那一輪接手時拿到
> `shots/pre-label`,做法是在同一個乾淨 HEAD 重截一組,用 `cmp` 確認九張逐位元組相同。
> 那不只證明「這個基準能用」,同時證明**harness 今天在這台機器上仍然是決定性的** ——
> 每一輪都該做,成本只有一次截圖。
>
> **`block-04` 那一輪這一步真的抓到東西了,而且第一直覺是錯的。** 重截出來 01 對不上,
> 看起來像「別人遞來的基準壞了」;再截第三次才發現**跳掉的是自己中間那一次**,
> 01 本來就是雙穩態的(見上面非決定性來源那一節)。
> **一次 `cmp` 不夠下結論,第三次才分得出是誰在跳。**

`shots/` 不進版控,被刪掉就重跑一次 —— 但重跑出來的是「現在的工作目錄」,
不是當初那個 commit,見「工具的陷阱 1」。

---

## 驗收規則

**兩套規則,看那一輪該不該改變外觀。**

### 不該改變外觀的輪(包 layer、接線)

零差異。**任何非零一律當成問題查,不准先歸因到任何已知效應。**

「帶著預期去驗收」會讓第一反應變成歸因而不是追查,那就毀掉驗收的意義。

### 該改變外觀的輪(每一塊轉換)

零差異反而代表沒生效。所以:

1. **動手前**先寫下預期會產生哪些視覺變化,逐項寫進 `block-NN-*.md`
2. 比對後,把實際差異逐一對到清單上
3. **清單外的差異是問題**,不管看起來多合理
4. **清單上有、但實際沒出現的也是問題** —— 那代表你以為會生效的東西沒生效
5. **「我驗不到這個」也要查證過再寫。** 把涵蓋率**報低**看起來保守無害,
   實際上是放棄一個其實有效的驗證管道
6. **逐條列完改動之後,再寫一行「這些合起來,使用者會看到什麼」。**
   前五點管的是「每一條改動對不對」,這一條管的是「它們湊在一起的那個結果有沒有人想過」

第 4 點容易被忽略。前面幾輪一直在防「不該變的變了」,轉換開始之後要同時防「該變的沒變」,
而後者在 CSS 裡一樣不報錯。

**第 6 點是連續兩輪同一種登記不足之後補的,不是個案。**

| 輪 | 逐條登記的 | 實際發生的 |
| --- | --- | --- |
| `geocode-place-and-confirm` | V5「剩下的 pin 座標不動,標籤可能讓位」 | 座標確實沒動,但**取景整個換了** —— 主角從兩個點變成一個點,`fit.length > 1` 不成立,06 變成以橫濱為中心 |
| `geocode-outside-and-notice` | K3(標題也算人工表候選)、K5(`pinOf` 同序)、K6(再查一次先查表)三條各自都對 | **三條合起來** = 「查無之後,人工表會從標題接手」,那顆 pin 換座標而不是消失 —— 上一輪的探針因此有一條必須改寫,而預期表裡沒有它 |

兩次都不是「哪一條寫錯了」,是**沒有人把逐條的結果乘起來**。
兩次都被抓到,也都是自己回頭發現的 —— 但**發現的時機都是「探針紅了」或「圖變了」之後**,
那已經是事後歸因了,而這一節開頭就寫著事前登記和事後歸因的差別。

> **寫法很便宜:逐條表格底下補一句「所以使用者會看到 ____」。**
> 答不出來,就是那幾條你其實只推到一半。

第 5 點是 `block-04` 那一輪自己撞到的:事前登記寫了「`:focus-visible` 這一輪驗不到
(截圖不對焦)」,**而 08 的第一個欄位在截圖裡就是對焦的** —— app 自己 focus 了它。
那一輪剛好在同一個地方發現一個真的回歸(對焦時圓角 8px→4px),
**如果當時信了自己那句「驗不到」,那張圖就不會被拿來看。**

> **登記不準有兩個方向,而低估的那個方向感覺起來像謙虛。**
> 「我驗不到」跟「我預期它會變」一樣是一個斷言,一樣要有根據 ——
> 最便宜的根據就是**把那張圖切出來看一眼**。

已知且無法消除的差異,要在**該輪開始之前**登記。事前登記和事後歸因看起來像同一件事,
但前者可以被檢驗、後者不能。

`block-01-chip.md` 是完整的範本:為什麼挑這塊、預測表、實際結果、偏離的解釋。

---

## 怎麼挑一塊

- **最小、最獨立、最容易單獨看到**
- **而且 design-system 裡要有語意對得上的組件**
- **而且不要卡在別人還沒修的東西上**
- **而且要問一句:它出現在哪一張截圖裡?**

第二個判準比第一個重要。挑一塊在 design-system 裡沒有對應組件的,就會變成
「用 utility 手工拼出一個外觀」—— 那驗不到我們要驗的東西(組件在真實頁面上長得對不對)。

第三個曾經擋掉三塊:`rm-chip`、`rm-overline`、`rm-day-pin` 都吃 `--font-mono`,
而當時 mono 沒有 CJK fallback。**那個阻礙已經解除**(`block-02-mono` 補在
**tokyo-trip 自己的 `:root`** 上,不是 design-system 那一份)。

### 動手之前還要做一件事:把要刪的那條規則**整條**盤完

**這四塊每一塊都是「刪掉一條舊規則、換上一個組件 class」,而舊規則幾乎都是
後代選擇器** —— `.fld label`、`.fld input`、之後還會有別的。
後代選擇器選到的東西**比你腦子裡那份清單多**,而且它**整條**的宣告都作用在那些東西上。

`block-05` 拿新截圖回頭驗第三塊的時候量到這件事:

> `.who label` 住在 `.fld` 裡,所以 `.fld label` **整條**都作用在十顆成員膠囊上。
> 第三塊只登記了 `color` 和 `letter-spacing` 兩項並且原樣補了回去 ——
> **結論是對的,但支撐它的推理不完整。**
> 沒出事是因為 `.fld label` 的 `font-weight`(`400`)和 `margin-bottom`(`0px`)
> **剛好等於瀏覽器預設**,規則刪掉之後落回原處。
> 舊規則要是寫了 `font-weight:600`,**十顆膠囊的字重會在無人登記的情況下被改掉,
> 而九張截圖一樣看不到。**

**所以做法是:把那條規則的每一個宣告列出來,逐個問「它作用在哪些元素上」,
而不是只盤你打算改的那幾個。** 差別在於前者會把「刪掉之後會落回什麼」
變成一個你答得出來的問題。

> 這是這個專案那條招牌教訓的又一次發作:**結論對和方法對是兩件事。**
> (同一頁上還有另外兩個:「同一狀態連跑兩次相同」證明不了工具沒壞、
> 「成功要回傳 0」驗過了不代表「失敗要回傳非零」也驗過了。)

#### 反過來的那一半:**加**一條規則也要問它連帶關掉了誰

上面那一段講的是「刪一條規則,會讓一堆被它遮住的宣告浮出來」。
`geocode-outside-and-notice` 那一輪撞到的是**同一件事的鏡像**,而且代價更直接:

> 規則一(「沒填地點就不查,標題不再是候選」)是對的 —— 拿標題去問一個
> 不知道自己不確定的服務,就是那顆雲南 pin 的成因。
> 但實作是把 `item.place` 傳給 `pinFor()`,而 `pinFor()` **同時**是
> `OUTSIDE` 那張人工確認過的表的入口。標題一從候選裡拿掉,
> **人工表也跟著比對不到了** —— 而那張表是整條路上最準的一環。
>
> 結果:`{ title:"桃園機場 第一航廈 報到", place:"樂桃 MM626 · 建議起飛前 2.5 小時" }`
> 掉到線上查詢,拿一句航班備註去問;而「再查一次」那條路**從來就沒查過那張表**,
> 按下去只是拿另一個線上結果蓋掉,錯得更確定。

**一條規則的正面效果,連帶砍掉了一個它沒看見的受益者。**
上一輪還為這件事寫了一條「✅ `OUTSIDE` 不受誤傷,而且驗過」——
它檢查的是「六個航班節點有沒有填地點」(有),該問的是
「**填的那個東西比對得到表嗎**」(有一筆比對不到)。**問錯的問題會拿到一個乾淨的綠燈。**

> **做法**:要收掉一個候選字串、一個選擇器、一個輸入來源之前,
> 先 `grep` 它**還通往哪裡**。這裡只要 `grep -n OUTSIDE` 就看得到三個消費者
> (地圖的 `pinFor`、天氣的 `resolvePlace` 和 `stopWx`),
> 而「地圖那條的入口同時是人工表的入口」在那一眼就看得出來。

**第六塊(`.who`)正好要面對同一個結構** —— 這次是 `.who input` 那四個
第四塊補回去的宣告,以及 `:537` 的 `.who label` 手機覆寫。見下面「第六塊」。

**第四個判準是 `block-03-label` 那一輪學到的。** 那一塊改了 32 個標籤,
**九張截圖只看得到 9 個**,其餘 23 個躲在沒有被打開過的表單和對話框裡。
不是不能做,但要**事前就知道自己驗得到多少**,並且在該輪的
「沒有驗到的事」裡寫清楚 —— 而不是做完才發現覆蓋率只有三成。
挑一塊之前,先對著九張截圖問:**這一塊會出現在哪幾張?**

可用的組件(`design-system/retro-modern.css` 的 `@layer components`):

```
rm-base  rm-btn(--action/--cta/--ghost/--lg/--secondary/--sm/--steel)
rm-card(--arch/--expressive/--raised/--surface)  rm-chip(--accent/--solid)
rm-day-pin  rm-divider(--dashed)  rm-input  rm-label  rm-overline
rm-row  rm-stack  rm-toast
```

### 第六塊:`.who` 那一整組 —— 擋了兩輪的理由已經消失

`#exp-form` 和 `#exp-edit-overlay` 現在有截圖了(`10-exp-form` / `11-exp-edit-dialog`),
所以「沒有畫面就驗不了」不再成立。**`.who` 是現在最該做的一塊,理由不只是輪到它:**

**十顆成員膠囊是壞的,而且看到圖才看得出來有多壞。**
`.who input` 的 `width:100%` 把 13px 的原生核取方塊撐成 **27.53px**,
在一顆 82px 寬的膠囊裡吃掉三分之一,**「阿輝」「佳瑜」「志偉」「雅倫」全部被擠成兩行**
(只有單字的「媽」沒事),膠囊因此高 **64.781px** —— 比旁邊 48px 的輸入欄位還高。

它的來源是被刪掉的那條 `.fld input{width:100%}`(本來是給文字欄位用的),
`.who` 住在 `.fld` 裡所以一起被選到,第四塊為了讓那一輪零改變又**原樣補了回去**。
`block-04` 當時寫「看起來就是個意外」—— 看到圖之後可以把「看起來」拿掉了。

動它之前要知道的(**先讀上面「把要刪的那條規則整條盤完」,這一塊正是那個結構**):

1. **`.who input` 有四個宣告是第四塊補回去的**(`width` / `background` /
   `font-size` / `line-height`)。`width:100%` 該走,另外三個要當成同一個決定一起想。
2. **`.who label` → `rm-chip`** 語意對得上(它就是一顆膠囊)。但 `:537` 有一條
   `.who label{padding:9px 13px; font-size:14px}` 的手機覆寫,特異度(0,1,1)
   **會贏過 `.rm-chip`(0,1,0)** —— 跟第四塊 `:521` 那題一模一樣的形狀,
   留不留要**事前**決定,不要做完才發現手機上沒換到。
3. `probes/who.js` 已經在那裡了,`WIDTH=390` 和 `WIDTH=1100` 兩個都要跑。
4. 這一塊**同時出現在 10 和 11 兩張圖上**,而且兩張都畫得到 ——
   **這是第一次「要改的東西」和「看得到它的截圖」完全對得上。**

細節和量到的每一個值見 `block-05-expform-shot.md`。

### 第五塊做了什麼,以及它為什麼算一塊

**它一個樣式都沒改。** 產出是兩張截圖,加上拿那兩張回頭驗證
第三塊和第四塊各自「刻意凍住」的 `.who`。結論:**凍住守住了** ——
膠囊和核取方塊的每一項 computed 值、連小數點後三位的幾何都相同,
唯一變的是第四塊自己登記過的 `min-width: 0px → auto`(零影響)。

兩件值得帶走的事:

- **第三塊沒有漏,但那是運氣。** 前兩個人報的結論是對的,支撐它的推理不完整。
  **已經提到這一節上面「把要刪的那條規則整條盤完」去了** —— 那條對之後每一塊都適用,
  不該只留在一份回顧裡。
- **「補一張截圖」這種一塊是有的。** 它不改外觀,驗收條件就是
  **九張逐位元組相同**;它的價值在於讓驗收規則第 4 點在一塊新的地方開始生效。

### 第四塊實際做了什麼(已完成,細節見 `block-04-input.md`)

Lulu 的決定是**保留 `:521` 的手機 16px 覆寫、刪掉 `:419` 的桌機基底規則**。
下面幾件事是那一輪量出來的,對之後的每一塊都還適用:

- **`.fld input`(0,1,1)高過 `.rm-input`(0,1,0)**,跟載入順序無關。
  同理 `:423` 的 time/date、`:430` 的 number 覆寫(都是 0,2,1)都活了下來,量過。
- **`padding` 在九張截圖裡一個像素都沒變。** `ADOPTION.md` 舊版寫「欄位會明顯變高」,
  那是把桌機的值當成了截圖的值 —— **08 / 09 都是 390px**,`:521` 贏。
  這一塊實際的高度變化只有框線 1px→2px 帶來的 **+2px**。
  > **教訓:寫「這一塊會怎麼變」之前,先確認那一張截圖是幾 px 寬。**
  > `probe.sh` 現在有 `WIDTH=`,不要再只量 1100。
- **刪一條規則,會讓一堆一直被它遮住的宣告浮出來。** 第三塊是 `.fld label` 漏給 `.who label`,
  第四塊有兩處:`.fld input` 也選到 `.who` 的核取方塊,
  以及 `:53` 的全域 `:focus-visible{border-radius:4px}` ——
  以前 `.fld input` 蓋得過它,換成同特異度的 `.rm-input` 之後,**對焦會把圓角從 8px 變 4px**。
  兩處都在那一輪補回去了。
  > **找這種東西的方法是量,不是讀。** `:focus-visible` 那條是探針的離群值報出來的,
  > 讀 CSS 的時候完全沒想到。

**排除掉的**:`.btn` → `rm-btn`(九張全會變、七個變體要對應,太大);
`.day` → `rm-day-pin`(語意不對 —— `rm-day-pin` 是 28×28 的圓形號碼牌,`.day` 是整張日卡。
真正對得上的是時間軸的數字圈和地圖 pin,但那會同時動到四張,而且 pin 尺寸一變
地圖標籤的碰撞讓位就整個重算);`.cloudbar`(最接近的 `rm-toast` 是浮在角落的提示,語意不對)。

---

## 約束

### 不要用 opacity modifier

`bg-paper/50`、`text-ink/70` 這一類。它們編出來是 `color-mix`,**在 iOS 16.3 以下
整條宣告無效** —— 不報錯,背景就是不上色。

Lulu 對 iOS 版本的答覆是「以最新版為主」,那是**假設不是事實**(她沒有去問那四個人)。
避開這類 utility 就避開了這個假設的主要暴露面。需要半透明就用明確色值或自己的 `:root` 變數。

### `<link>` 必須在 `<style>` 之前

不是保險,是**必要條件**。兩邊的組件都在 `components` 層,同層之內比特異度再比原始碼順序,
**後載入的贏**。design-system 先載入,`.card` 才蓋得過 `.rm-card`。

反過來的話它的組件會開始無聲蓋掉這邊寫好的東西。

### `:root` 留在 `@layer components` 外面

自訂屬性一旦進了層,任何無層級的 `:root` 都蓋得過它。留在外面就永遠是這邊說了算 ——
那也是之後想覆寫 design-system 某個 token 時唯一有效的位置。

> **這條同時是保護傘和阻礙,而後半段害過一次。**
> 「永遠是這邊說了算」的另一面是:**design-system 對同一個 token 的修正永遠到不了這裡。**
> 實際發生過 —— `--font-mono` 缺 CJK 後援,有人去修了 design-system 那一份,
> 重編之後九張零差異,因為 tokyo-trip 的 `:root` 擋著,那個修正從來沒到達任何元素。
>
> **要查一個 token 現在是誰說了算,不要讀 CSS 猜,直接在真實 DOM 上問:**
>
> ```js
> getComputedStyle(document.documentElement).getPropertyValue('--font-mono')
> ```
>
> 兩邊的 token **同名而且值長得很像**,分辨的方法是**只看第一個字體名**。
> 完整的辨識表和那次繞路的經過,寫在
> `block-01-chip.md` 的「怎麼分辨這個 computed 值是誰的 stack」。

### 改 markup 之後一定要重編

Tailwind 是 content-driven,掃 `tokyo-trip/` 決定編出哪些 utility。
用了新 class 沒重編 = 那個 class 不存在 = 樣式安靜消失。

**已經做進 `shoot.sh` 了**,截圖前自動重編,不是人要記得的事。

---

## 已登記的盲區

產出的 `utilities` 層有 8 個 class:

```
block  ease-out  filter  hidden  inline  resize  sticky  transform
```

**沒有一個是 markup 在用的。** 掃描器從 CSS 屬性值、JS 字串、**甚至中文註解**裡撿出來的:
`padding-block`、`position:sticky`、`cursor:col-resize`、`backdrop-filter`、
還有註解裡寫的「用 clip 不用 hidden」。

**後果:這 8 個名字不受「忘了重編」偵測保護。** 哪天想拿其中一個當 utility 用
(例如 `class="hidden"`),它早就在產出裡了,忘記重編也不會被抓到。

不修:為了 8 個常見字去調掃描規則,成本高於收益。詳見 `tokyo-trip/app.css` 的註解。

### 它曾經變成 11 個,而原因是這份文件自己

`9262c9a`(把這份文件寫出來的那個 commit)之後,重編一次會多出三個:

```
backdrop-filter   bg-paper\/50   text-ink\/70
```

因為當時這份文件住在 `tokyo-trip/`,而它的「不要用 opacity modifier」那段
字面寫著 `bg-paper/50` 和 `text-ink/70`、「已登記的盲區」那段字面寫著 `backdrop-filter`。
**掃描器把警告文字本身撿去編成了 utility。**

連帶的第二個症狀:`tokyo-trip/retro-modern.built.css` 是在這份文件進 repo **之前**編的,
所以從那時起 git 裡的產出就跟它的原始碼對不上了 —— **任何人跑一次 `shoot.sh`
都會讓它變成 modified**,而那看起來像是自己不小心改到的。

把文件搬到 `adopt-harness/` 之後重編,產出回到 8 個 class,而且跟 `9262c9a` 裡那份
**逐位元組相同**。這個故事比結論有價值:
**掃描範圍的邊界不只約束程式碼,也約束你寫在那個資料夾裡的每一個字。**

### 而上面那句「跑一次就會變 modified」害過一次,要更正

那個症狀當時是真的(產出跟原始碼對不上),搬走文件之後**已經不成立**了。
但那句話留在這裡,而且它做的事比過期更糟:

`design-system/` 一髒,`tokyo-trip/retro-modern.built.css` 就會跟著變。
那個檔在舊版出處紀錄的範圍內 —— 換句話說,**「凍結被打破了」這件事一直有一個訊號,
而這份文件親手把那個訊號標成了噪音。**

> **那比「紀錄範圍太窄」嚴重得多。範圍窄是遺漏;教人忽略,是主動拆掉一道還在運作的防線。**

現在的出處紀錄會單獨列出相依的狀態,不必再靠產出檔去推。
但這件事值得記住,因為它會再發生:**在文件裡寫「這個訊號可以忽略」之前,
先確認它真的只有噪音的意思。**

---

## 走鐘防護

`index.html` 裡有一段:檢查「頁面上用到、但沒有任何 CSS 規則提到的 class」,
也檢查產生檔有沒有載到。

**正式站完全不出聲**(只寫 console),localhost 和 `file://` 才跳橫幅 ——
那五個人不該看到我們的工程訊息。

`brand` 和 `panel` 在忽略名單裡,它們是純標記用的 class,沒有樣式也沒有 JS 用到。

---

## 多 session 協調規則

這個 repo 同時有別的 session 在動(`design-system/` 由另一隻負責),共用**同一個工作樹**。

- **提交前先問協調者**(`playground-8b`)。編輯可以並行,提交不行
- **逐檔 `git add`,不要用 `-A` 或 `.`** —— 會把別人手上的檔案掃進去。
  我實際遇過一次:協調者放行時說工作樹只有我的檔,我 add 之前重讀,
  發現另一隻已經落盤了兩個檔。**放行不等於工作樹狀態的保證,自己重讀。**
- **`DECISIONS.md` 一次只有一隻能寫**,要先拿鎖
- **一輪轉換進行中,design-system 凍結**。相依一動,差異就無法歸因。
  開始一塊之前跟協調者說,做完回報才解凍。
  **現在出處紀錄會單獨記相依的狀態**,凍結有沒有被打破,事後查得到
- **同一時間只有一隻能持有 harness**(`shoot.sh` / `probe.sh`)。
  它們共用 `.work/`、共用埠、共用產出檔 —— 腳本自己有鎖會擋,但排隊還是要先講。
  見「工具的陷阱 6」
- **開工前先問協調者「現在有誰在動哪裡」**。實際發生過:兩隻 session 被指派了
  **同一塊**,差四分鐘,兩邊都要改 `tokyo-trip/index.html` 和同一份 `block-03-label.md`。
  擋下來的不是工具,是**動手之前先回報**這個習慣

### 一塊一個人(Lulu 在第三塊之後定的)

**每完成一塊、提交之後,那個 worker 更新這份文件,然後換下一個人接。
不等 session 的上下文用完,在自然的邊界換手。**

理由:上一隻(Tokyo trip)的上下文用完時卡在半途,**而且不是它自己發現的**,
是 Lulu 看到才講的。

> **session 看不太到自己還剩多少**,所以「滿了就換」沒有可靠的觸發器 ——
> 那正是這個專案一路在拆的那種「靠人注意」的機制。
> 換成**事件觸發**(一塊做完)就不需要偵測狀態了。

附帶的好處:**這份文件每一塊都會被真的用一次。** 它的缺口就是這樣被挖出來的 ——
`block-02-mono` 那一輪挖出「量字體的四個陷阱」沒有寫下來,
`block-03-label` 那一輪挖出出處紀錄看不到相依、harness 不是併發安全的。
**沒有換手就沒有這些。**

接手的人要做的第一件事,見最上面「現在做到哪」的三行指令 —— 先問線上是什麼,
再讀這份文件,**然後自己重截一次基準用 `cmp` 驗過**再開工。
