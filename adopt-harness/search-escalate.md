# 找地點收成一顆按鈕：三段式

**Lulu 問的**：「這個再查一次不就等於搜尋嗎？」

從使用者站的位置看，它們就是同一件事。我一開始只看到程式面的差別
（不同服務、一個免費一個付費、一個回多筆一個回一筆），**而那個差別對按按鈕的人不存在**。

## 真正的理由：它的前提消失了

「再查一次」存在的理由是「**程式自己猜錯了，換一家重猜**」。

而兩欄合併之後（`merge-place-field`），**程式不再自己猜** —— 沒挑就沒有 pin。
那個理由整個沒了，剩下的真實需求是「免費那家找不到，我想換一家再找」，
而那是搜尋流程裡的一步，不是另一個功能。

> **一個概念因為前提消失而退場，跟介面簡化長得像，但不是同一件事。**
> 後者常常會把有用的東西一起收掉；前者收得乾淨。

## 三段

| 第幾次按（沒挑到候選） | 畫面 | 問誰 |
| --- | --- | --- |
| 1 | 候選清單 | 免費（Nominatim） |
| 2 | ＋「試試加入車站或地點名」 | 免費 |
| 3 | ＋「還是沒有找到嗎？」按鈕變鮮明、字變**強力搜** | **下一按**才走 Google |

**第 3 段只換按鈕，不會自己去花錢。** 要錢的那一下永遠是使用者看著一顆
長得不一樣的按鈕、再按一次 —— 那是「要錢的動作藏在刻意的操作後面」在這個流程裡的落點。

**畫面上不講「改用 Google」**（Lulu 的決定）：使用者不需要知道背後換了哪一家。

**第 1 次不給提示** —— 那時候還沒有「找不到」這件事，提早講等於預設他會失敗。

**挑到候選就歸零**，每個表單打開也歸零。不歸零的話，下一個地方會從花錢那一段開場。

## 後端那道窄路拆了，而它不是壞掉

`wishAsksForItsOwnPlace`：沒有通行碼的人也能打 geocode，條件是
「**查的字串必須就是那筆願望已經存著的地點**」。它不驗身分（驗不了），
改成限制**被問的是什麼**。

**它跟搜尋根本不相容**：

> 那道檢查只在「被問的東西是資料庫裡已經有的」時成立，
> 而**搜尋的本質是問一個還沒存進去的字**。

所以合併之後它會把那三個沒有通行碼的人**每一次都擋掉** ——
而「免費那家找不到，換一家再找」正是這個功能存在的理由，擋掉等於把功能拿掉。
**不是參數調一調的問題。**

成本改由 Google 金鑰自己擋：**`v3 requests per day = 500`**（Lulu 設的）。
那是 Google 強制執行的硬上限，比我們寫任何程式都可靠 —— 超過就是 Google 拒絕，
不是她的卡被刷。以牌價每千次約 US$5 估，最壞一天約 US$2.5。

**剩下的風險是額度被故意用光**（那天大家都搜不了），不是帳單失控。她拍板接受。

## 那顆按鈕的三層條件，逐一交代去了哪裡

`wish-edit` 那一輪的 `#we-again` 有三層。協調者提醒「不能只搬前兩層」：

| 原本的條件 | 去了哪裡 |
| --- | --- |
| 有 place 才畫 | **不再需要**。第 3 段的觸發是「連找兩次沒挑到」，跟有沒有地點無關 |
| 人工表命中就不畫 | **刻意拿掉**。它講的是「拿存著的地點去重查」；第 3 段送的是使用者剛打的字，他明擺著在找別的東西 |
| 快照模式不畫 | **搬進 `paintSeekBtn()` 和 `runSeek()`**，兩邊都有 `!onSnapshot()` |

**第三層一定要跟著搬**：在一份凍結的副本上開一個花錢的動作，是 `wish-edit` 剛關上的門。

## 驗收

- **`probes/escalate.js` 25 條全過**，拿去跑 main **7 條紅**
- `geofix` **158**、`seek` **53**、`drawer` 10 全過
- **截圖 `08`、`12`、`13` 三張變**，其餘九張與 `shots/sync-post` 逐位元組相同，
  `11` 落在已登記的雙穩態（8px／6 列）

> **上面三個數字第一次寫下來是 150 / 52 / 20，而其中兩個不是通過。**
> `seek` 和 `geofix` 都還在測 `#we-again` —— 這一輪拿掉的那顆按鈕 ——
> `q("#we-again").hidden` 讀到 null 就拋例外，後面的斷言一條都沒跑。
> 而 `out.結論` 當時只看 `沒過的`，**沒跑到的不會失敗**，於是印「全部通過」。
> 那兩個數字量到的是爆炸發生在第幾條。修好之後 `seek` 多回一條（取消關得掉）、
> `geofix` 多回八條（14f 整段）。判準已改成「`爆掉了` 有東西就不是通過」。
>
> **拿掉一個功能的時候，要跟著搜的是「誰在測它」，不只是「誰在用它」。**

> **截圖那一行原本寫的是「只有 13 變」，漏掉兩張。** `12-place-search` 是這一輪
> 重畫的畫面，漏記純屬失誤；`08-addstop-form` 差 34 列，是新增停留的表單裡
> **提示字**從「淺草寺」換成「地名/車站/建築物/活動名等」—— 跟第二段那句提示
> 是同一個念頭，只是沒有人寫下來。
>
> **而當時根本沒有資格下那個結論**：比對用的 `esc`–`esc5` 五組全部是在工作樹有
> 2–4 個檔未提交的狀態下截的，最新那組還落後分支頂端三個 commit。
> 乾淨的那一組是 `shots/esc-clean`（`7a8f42e`，14 張），已登記進 `ADOPTION.md`。

## 過程中三個錯，兩個是我的

### 一、我在網頁程式裡寫了 `d.querySelector`

`d` 是**探針的別名**，不是網頁裡的變數。於是 `seekBtn()` 一呼叫就拋例外，
查詢根本沒送出去 —— 而探針報的是「候選 0 筆」「按鈕沒變」，看起來像三段式的邏輯錯。

> **症狀出現的地方離原因很遠**，因為那個例外被 `runSeek()` 外面的 try 吃掉了。

### 二、我刪一段程式時多刪了 383 行

要拿掉 `#we-again` 的事件處理器，我用文字切割 —— 而 `rindex("  });")`
找到的是**整段最後一個**，不是那個 handler 的結尾。**抽屜、地圖開關、登入、匯出
全部跟著不見了**，而 `new Function` 的語法檢查**照樣通過**（刪掉完整的函式不會有語法錯）。

症狀是四支探針同時大面積退化、連「抽屜打得開」都紅。

> **語法檢查不會告訴你刪掉了什麼。** 用文字切割改程式的時候，
> 檢查的對象應該是「改動的行數是不是我預期的量」，不是「它還編得過嗎」。
> 這次 `git diff --stat` 一眼就看得出來（+123/−44 vs −383）。

### 三、我拿了過期的基準（第三次了）

`shots/we-final` 是 `be3e0ef` 截的，**而抽屜的修正在 `18b7525` 才合進來**。
於是 07 顯示差 14.5%，我花了四次量測（含兩個方向的位移搜尋）才發現是基準比程式舊。

對的基準是 `shots/snapguard`（`18b7525`）。**判準應該先看 `PROVENANCE.txt` 的 HEAD
是不是我要比的那個 commit**，而不是看哪一組時間最近。

## 沒有驗到的

| | |
| --- | --- |
| ~~**Google 真的回什麼**~~ | **2026-09-21 驗掉了。** Lulu 在 `feat/search-escalate` 的預覽版上按到第三段，強力搜回了合羽橋道具街（`〒111-0036 Tokyo, Taito City, Matsugaya…`），標定成功。那個地址的長相就是 Google 的 `formattedAddress`（`〒` 開頭、地名羅馬拼音、半形全形數字混用），不是 Nominatim。**擋住它的是金鑰，不是程式** —— 見下面那一則 |
| **是新版還是舊版答的** | 兩邊的回傳被整成同一個形狀，畫面看不出來、伺服器也沒記。現在不必知道；等舊版被收掉才會需要 |
| **第 3 段在真人手上的節奏** | 「連按兩次沒挑到」在合成事件裡是瞬間的，真人中間會讀、會改字、會猶豫。**那個節奏只有她試得出來** |
| **額度用完長什麼樣** | 文案寫了（「可能是今天的查詢次數用完了」），但**沒有人看過**，要真的打爆 500 次才會出現 |
| iOS Safari | 完全沒驗，跟每一輪一樣 |

## 金鑰那一關：擋住的是設定，不是程式

第一次真的問 Google 的時候兩家都拒絕，訊息裡有 **are blocked**。
程式碼的註解當時判讀成「金鑰的 API restrictions 沒放行」——**而那個判讀不完整**：

> 「are blocked」同時出現在兩種拒絕裡。**「Requests from referer … are blocked」
> 是應用程式限制**（HTTP referrer），**「Requests to this API … are blocked」
> 才是 API 限制**。只看這三個字分不出來是哪一層。

而強力搜是 **Vercel 的伺服器打出去的**，身上沒有來源網頁 ——
所以只要金鑰設了網址限制，它必然被擋，**跟 Places 有沒有開完全無關**。
Google 自己的文件也是這樣寫的：要給伺服器用的金鑰設 IP 限制或不設限制，
設成瀏覽器限制就不能用。

Lulu 查下來的實際狀態：**應用程式限制 =「無」**，
API 限制勾了 Places Aggregate API / Places API / Places UI Kit。
按下去就通了，所以是 API 限制那一格缺了 Places 而她補上的。

**留給下一個人的是那個判準**：`1e377ce` 把兩家的原話一起帶出來是對的方向，
但**光有原話不夠，還要知道原話裡哪個詞對應哪一層設定**。
訊息裡出現 `referer` → 應用程式限制；出現 `requests to this API` → API 限制；
出現 `not enabled` / `legacy` → 那個 API 根本沒開在這個專案上。
