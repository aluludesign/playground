# 決策日誌

這裡記的是**為什麼**。

| 要查什麼 | 去哪裡看 |
| --- | --- |
| 做了什麼、什麼時候做的 | `git log` |
| 現在怎麼運作、怎麼設定 | 各專案的 `README.md` |
| **當初為什麼這樣決定、後來為什麼改掉** | **這份檔案** |

每一則記四件事：**決定**（做了什麼選擇）、**為什麼**（不選另一邊的理由）、
**推翻了**（如果它取消了先前的某個決定）、**代價**（知道而且接受的缺點）。

代價那一行最重要。沒有代價的決定通常只是還沒發現代價。

每一則標題右邊是 commit hash —— 那是這份日誌和程式碼之間唯一的線，
`git show <hash>` 看得到當時的完整理由和實際改了哪幾行。

## 怎麼新增一則

改動有「本來可以那樣做，但選了這樣」的成分，就往對應專案的最上面加一則，
格式照既有的抄。純粹修 bug、改錯字、調文案不用記 —— 那些 `git log` 就夠了。

commit 之後回來把 hash 補上。還沒 commit 的先寫 `未進版控`。

---

# 改過主意的地方

回溯時最常要找的就是這幾條線：某個現在看起來奇怪的做法，
通常是某次推翻留下的。往下找對應的 hash 看完整理由。

| 主題 | 演變 |
| --- | --- |
| **資料放哪** | localStorage `85c041f` → Supabase `0be43f9` → Notion `35e099c` |
| **願望排進行程** | 長按拖曳 `46c630e` → 「加入」按鈕 `11cf284` |
| **怎麼抓起一列行程** | `⠿` 握把 `aeb95be` → 長按時間欄 `c4f86e5` → 長按整列 `3761a64` |
| **地名查詢的範圍** | viewbox 當硬界線（`bounded=1`）`863ee2a` → 只當加權 `57473b5` → **硬界線加回來，但框改成每次查詢自己選** `14121ee` → **而「人在挑」的那條不設界線** `b21fff2` |
| **地圖怎麼出現** | 行程頁內嵌 `<details>` + 許願池另一個 sheet `863ee2a` `3d69766` → 一張 sheet 兩種內容 `c663f5f` |
| **地圖打開時的背景** | 變暗、擋住點擊 `3d69766` → 不變暗、照樣點得到 `a5aad01` |
| **已排行程在願望地圖上** | 畫小、半透明，當背景 `79b8dec` → 同樣大小，用顏色分，各有開關 `2b7c53b` |
| **pin 的顏色從哪來** | 繼承容器的 `--dc` `863ee2a` → 每個點自帶顏色 `74db440` |
| **航班節點** | 寫死在程式裡 `85c041f` → 只當離線備援 `4d857ff` |
| **沒設通行碼的時候** | 跳過檢查，寫入全開 `35e099c` → 一律擋下寫入 `db1eca0` |
| **出東京的地點怎麼認** | 線上查詢服務 → 寫死對照表 `OUTSIDE`，查詢只當補漏 `275da9b` |
| **消費端產出落後怎麼辦** | 我判斷只能寫成限制（瀏覽器看不到 `design-system/`，沒有比較對象）→ 發現想錯了執行位置：檢查不必在使用者的瀏覽器裡跑，`build.sh` 掃給動手改的人聽 `未進版控` |
| **設計系統的 token 存哪** | Tailwind v3 preset 餵 hex `7390406` → 改存通道值 `channel-tokens-not-taken`（做完又拆掉）→ v4 `@theme` `5f8227d` |
| **icon 從哪來** | 手邊順眼的字元（`＋` `✕` `✎` `▾` `✈`）→ Font Awesome 內嵌 `e4a7fa7` |

---

# tokyo-trip

## 2026-09-24

### 一顆 AI 按鈕,確認後才存 · `263eff4`

- **決定**:頂列加 ✨ AI。話和圖交給 `api/ai.js`(Gemini 免費方案),回來的是「類型 + 欄位」,
  放進確認卡;按「確定」才走 addWish / addStop / 座位的寫入路徑。座位搬進 Notion 新的「座位」資料庫。
- **為什麼**:
  - 確認卡而不是直接存:AI 把第 3 天讀成第 8 天、把志偉的座位給了雅倫,存進去之後跟對的一模一樣。
    人按確定是唯一的偵測器(同「再查一次」的道理)。
  - 一顆按鈕而不是每張表單一顆:Lulu 要的是「丟東西進去,它自己知道放哪」。
  - 座位用航班號當標題:之後要開放給別團,航班不會只有兩班。
  - `/api/ai` 不要通行碼:沒通行碼的人也要能用它許願(Lulu 定的)。
- **推翻了**:座位寫死在 `SEATS`(`85c041f` 起)→ Notion 資料庫;`SEATS` 留著當備援。
  走道位置從「27D 前面」改成「C 和 D 之間」。
- **代價**:
  - 知道網址的人可以用掉免費額度。最壞是那天 AI 不能用,不會多花錢。
  - 免費方案的內容會被 Google 用來改善產品,訂位確認和座位表上有姓名。
  - 花費先不做。
  - 本機只用假的 Gemini 回應測過;真的模型讀座位表準不準,要上線拍第一張才知道。

## 2026-09-22

### 讀了述詞,沒追值從哪來 —— 同一天犯兩次 · `fix/one-baseline-name`

兩次都是**看了一個判斷式和一句註解就下結論**,沒有去追那個值什麼時候成立。
兩次都被別人抓到,兩次查證之後都是我錯。

| 我的結論 | 實際上 |
| --- | --- |
| 「`confirmPin()` 還有一處呼叫端,留著」 | 那一處是**註解**裡提到。真正的呼叫端只有 `#map-fix-ok` 的 handler,按鈕拆掉它就是孤兒 |
| 「航班列刪不掉,管理員也一樣」 | `movable = s => !s.anchor && editable()` 是真的,但 **`anchor: true` 只在離線時才設**(`const anchors = online ? [] : …`)。線上時航班是 Notion 來的一般 stop,管理員刪得掉 |

- **共同的形狀**:`grep` 找到一個名字、旁邊剛好有一句講得通的註解,於是停在那裡。
  **註解講的是設計意圖,不是此刻的事實**;而這個 repo 的註解密度高到
  `grep name(` 一次會撈出三種東西 —— 定義、呼叫、散文裡提到。
- **判準**:要判斷一個東西能不能刪,問的不是「哪裡讀到它」,是
  **「哪裡寫進它」** —— `s.anchor` 該搜的是 `anchor:`,不是 `s.anchor`。
- **第二次的代價比較實**:我已經把「航班列是例外」講給 Lulu 了,
  那是給她一個**不存在的限制**。

### 寫下那條規矩的同一個 commit 裡,又犯了一次 · `fix/one-baseline-name`

上面那一則修掉 `shots/adminwish` 的兩列相反判決,並寫下判準。
**而它自己在同一次改動裡,對下一個名字 `shots/adminwish-on-main` 做了一模一樣的事** ——
加一列 ⚠ 說它作廢,沒有撤回底下那列 ✅。被監督那個 session 掃出來的。

- **「寫下來」擋不住這個。** 規矩就寫在同一份檔案、同一個 commit 裡,
  而我在寫它的時候正在違反它。散文形式的判準防得了下一個人,防不了正在動手的那一個。
- **所以這一次做成一支會跑的檢查**:`adopt-harness/one-verdict.py`。
  掃基準表,同一個名字出現兩個不同的判決符號就 exit 1。
  修之前它紅、修之後它綠 —— **負向對照跑過了**,不是一支永遠回綠的東西。
- **這個 repo 對畫面有截圖、對行為有探針,而它一半的判斷寫在散文裡,
  沒有任何東西在看那些散文有沒有自相矛盾。** 這支補最便宜的那一半。

**還有一條是監督踩到、值得一起記的**:它第一次掃的時候用「這一行有沒有出現 ✅」
判斷,結果把一列 ⚠ 讀成 ✅ —— 因為那一列的**內文引用**了
「這一列以前是 ✅『最新的乾淨基準』」。差一點拿一個錯的量測來指正。

> **判準要看欄位,不要看整行有沒有出現那個字。**
> 而更上一層:**自己寫的檢查也會量錯,而量錯的檢查比沒有檢查更糟 —— 它會發綠燈。**
> `one-verdict.py` 只讀第二欄開頭那一個字元,理由寫在它的檔頭裡。

### 同一個基準名,同一張表裡判了兩次 · `fix/one-baseline-name`

- **發生的事**:`shots/adminwish` 在 rebase 之後作廢,我**加了一列說它作廢,
  沒有把原本那列「✅ 最新的乾淨基準」拿掉**。於是 `ADOPTION.md` 相鄰兩列
  對同一組圖給出相反的結論,而 `DECISIONS.md` 還指著作廢的那個。
- **加一列不等於撤回一列**:前者是補充,後者才是收回。我以為只漏了一個地方,
  實際上四個地方裡只修掉一個。
- **判準**:改一組圖的狀態時,搜的是那個**名字**出現過幾次,不是「我記得寫在哪」。
- **為什麼工具抓不到**:四句都是散文。探針量 DOM、截圖比像素,
  **沒有任何一件工具在看文件有沒有自相矛盾**,而這個 repo 的判斷有一半寫在文件裡。


### 通行碼改得動任何人的願望,而「刪掉」沒有跟著放寬 · `4c7a863` `53e09d9`

- **決定**:`mayEditWish()` 從 `mineWish(w)` 放寬成 `mineWish(w) || editable()`。
  **`updateWish()` 裡那道守門一起改** —— 只放寬畫按鈕那一邊的話,管理員點得開、
  填得完,按下「存起來」才被擋,而那時候字已經打完了。
- **為什麼**:通行碼本來就管得住「刪掉」「加行程」和其餘每一個寫入,
  唯獨改不動別人願望裡的一個錯字。
- **「刪掉」刻意不動**:改別人的願望留得下痕跡(改完還是他許的),刪掉不留。
- **原作者早就寫過這件事**:`updateWish()` 上面那段註解說「這一條的邊界是
  『wishes、而且不改歸屬』,**不是『只有本人能改』**……『誰許的誰能改』是介面上的
  規則,不是鎖」。所以這是介面層放寬,後端一個字都沒動。

### 做決定的地方看不到,就等於沒說 · `53e09d9`

- **決定**:改別人那一筆的時候,對話框標題改成「改別人的願望」,
  底下多一條暖色的 `.note`:「這是 佳瑜 許的。改完還是算他許的。」
- **我第一次寫下的理由是錯的**:我說別人的願望沒有標名字。**它有** ——
  清單每一列都印著「佳瑜 許的」(`renderWishes` 的 `.by`,還帶各人的顏色)。
- **真正的理由比較窄,而且成立**:**那張表蓋住了那一列**。打開之後畫面上只剩
  兩個欄位,跟自己那一筆長得一模一樣,而唯一說過歸屬的地方在遮罩後面。
- **名字走 `nameOf()`**:第一版直接印 `w.by`,畫面上跑出 `chang_chiayu` ——
  全站沒有第二個地方這樣叫她。

### 放寬一條規則,要搜的是「誰在測它」 · `4c7a863` `53e09d9`

- **上一輪的教訓在這一輪原封不動又來一次,只是方向相反。** 上次是拿掉功能
  (`#we-again`)忘了搜測它的人;這次是放寬規則,而**兩支工具都把舊規則寫進了結構裡**:
  `probes/seek.js` 和 `shoot.sh` 都用 `querySelector("[data-edit-wish]")` 抓第一顆,
  **靠的是「剛好只有一顆」**。
- **症狀差很多,而且都不像原因**:探針抓到別筆,後面每個 `until()` 逾時,
  最後**一條斷言都沒印**(連「爆掉了」都沒有);截圖那邊拍到一筆沒有地點的願望,
  紅的是「已標定那一行不存在」。
- **修法**:兩邊都改成指名 `[data-edit-wish="w2"]`,跟清單上有幾顆「改」無關。
  「只有自己那筆有『改』」那條斷言**搬到 `probes/geofix.js` 的 14e** ——
  那裡是唯讀(沒有通行碼)的情境,**規則的邊界要在邊界上量**。
- **`shoot.sh` 的新截圖放錯位置兩次**:先放在 `rm -rf .work` 之後,再放在
  `kill $SRV` 之後。**兩次的錯誤訊息都在講頁面載不到,沒有一次講到位置。**
  順序是 shot → `kill $SRV` → `rm -rf .work`。
- 數字:`seek` 53 → 59,其餘不變。**基準名不寫在這裡** ——
  `ADOPTION.md` 最下面那一行才是唯一該寫的地方(那份檔案自己也這樣規定)。
  這一則原本寫著 `shots/adminwish`(15 張),rebase 之後那組作廢、換成
  `shots/adminwish-on-main`(16 張),而**只有 ADOPTION 那邊跟著改**。


### 「再查一次」退場,找地點收成一顆會升級的按鈕 · `da6999f` `c5e332c` `467c60b`

Lulu 問:「這個再查一次不就等於搜尋嗎?」從按按鈕的人站的位置看,它們是同一件事。
程式面的差別(不同服務、一個免費一個付費、一個回多筆一個回一筆)**對她不存在**。

- **決定**:兩顆收成一顆。同一顆按鈕按第 3 次沒挑到候選,才換成「強力搜」;
  **再按一次**才真的去問 Google。
- **為什麼**:「再查一次」存在的理由是「程式自己猜錯了,換一家重猜」,
  而兩欄合併(`merge-place-field`)之後**程式不再自己猜** —— 沒挑就沒有 pin。
  前提消失,這個概念就沒有位置了。
- **這跟「介面簡化」不是同一件事**:簡化常常把有用的東西一起收掉;
  前提消失的東西收得乾淨。判斷標準是「它當初為了解決什麼而存在,那件事還在嗎」。
- **要錢的那一下永遠在使用者手上**:第 3 段只換按鈕的樣子,不查任何東西。
  探針有一條專門守這件事(「點『好』那一下不查任何東西」)。
- **畫面上不講「改用 Google」**(Lulu 的決定):使用者不需要知道背後換了哪一家。
- **代價**:後端那道窄路 `wishAsksForItsOwnPlace` 整個拆了 —— 它只在「被問的東西
  資料庫裡已經有」時成立,**而搜尋的本質是問一個還沒存進去的字**,
  留著會把那三個沒有通行碼的人每一次都擋掉。成本改由 Google 金鑰的
  `v3 requests per day = 500` 硬上限擋(Lulu 設的),最壞一天約 US$2.5。
  剩下的風險是額度被用光,不是帳單失控,她拍板接受。

### 探針的「全部通過」不看例外,所以它會說謊 · `7a8f42e`

- **決定**:`out.結論` 改成先看 `爆掉了`。有例外就印「✗ 中途爆掉,跑到第 N 條就停了」,
  不再只看 `沒過的` 是不是空的。四支探針(`seek` `escalate` `drawer` `geofix`)全部改。
- **為什麼**:**沒跑到的斷言不會失敗。** 中途拋例外的話後面一條都沒跑,
  `沒過的` 照樣是空的,於是印「全部通過」,而旁邊那個數字不是分數,
  是爆炸發生在第幾條。
- **真的發生了,而且是這一輪自己造成的**:`seek` 和 `geofix` 還在測 `#we-again` ——
  這一輪拿掉的那顆按鈕 —— `q("#we-again").hidden` 讀到 null 就拋。
  `seek` 因此少跑「取消關得掉」,`geofix` 少跑 14f 整段八條。
  **而 `search-escalate.md` 把 150 / 52 當成通過抄進了驗收欄。**
- **要記住的**:**拿掉一個功能的時候,要跟著搜的是「誰在測它」,不只是「誰在用它」。**
  這個 repo 記過很多次「工具說成功但其實沒做到」,這次工具說謊的原因在它自己的結論式子裡。
- **代價**:沒有。真實數字是 `seek` 53、`geofix` 158、`escalate` 25、`drawer` 10。

### 「are blocked」有兩種,而它們在不同的設定頁 · `1e377ce`

- **決定**:`1e377ce` 讓 endpoint 兩家 Places 都試、失敗時把**兩邊的原話都帶出去**,
  這個方向對。但判準要補一句:**原話裡哪個詞對應哪一層設定**。
- **為什麼**:Google 的兩種拒絕都含「are blocked」——
  `Requests from referer … are blocked` 是**應用程式限制**(HTTP referrer),
  `Requests to this API … are blocked` 才是 **API 限制**。
  程式註解當時只判讀成後者,而**強力搜是伺服器打出去的,身上沒有來源網頁**,
  所以設了網址限制必然被擋,跟 Places 開沒開無關。
- **實際的結果**:Lulu 查下來應用程式限制是「無」,補完 API 限制那一格就通了。
  第一筆真的走完全程的是合羽橋道具街。
- **對照表**:訊息含 `referer` → 應用程式限制;含 `requests to this API` → API 限制;
  含 `not enabled` / `legacy` → 那個 API 沒開在這個專案上。

### 基準重新登記:`shots/esc-clean` · `7a8f42e`

- **決定**:`ADOPTION.md` 那一行「下一塊的基準用 ___」從 `shots/sync-post` 換成
  `shots/esc-clean`(`7a8f42e`,14 張,工作樹乾淨)。
- **為什麼**:這一輪截過五組(`esc`–`esc5`),**五組全部在工作樹有 2–4 個檔
  未提交的狀態下截的**,最新那組還落後分支頂端三個 commit。
  「那一輪截過圖」不等於「那一輪留下了基準」。
- **順帶抓到的**:拿乾淨的一組重比,變的是 `08`、`12`、`13` 三張,不是筆記寫的「只有 13」。
  `08` 差 34 列是提示字從「淺草寺」換成「地名/車站/建築物/活動名等」——
  **預期內的改動,但沒有人寫下來**,而沒寫下來的預期改動跟回歸長得一模一樣。

## 2026-09-21

### 做成 PWA,而「離線看得到」跟 service worker 是兩件事 · `1f43e6e`

Lulu 問「改成 PWA 要多久,還是重做一個比較快」。先回答後面那半:重做不會比較快。
`<script>` 之後有 2975 行邏輯、119 次 commit,README 26KB 和這份檔案 82KB
幾乎都在寫「為什麼是這樣」—— 日光查到熊本、`bounded=1` 的兩難、標籤自動讓位、
最少轉帳次數。那些不會因為重寫一次就自動長回來。PWA 要加的是 4 個小檔加約 30 行。

**但最重要的發現跟 PWA 無關:離線模式本來就是空的。**

`pull()` 只把 Notion 的資料放進記憶體,`saveLocal()` 第一行是 `if (online) return`。
兩件事加起來 —— **只要連過 Notion,localStorage 裡永遠是 `SEED`**,而 `SEED` 是
一筆機票加 `stops: []`。在東京地鐵裡沒訊號打開,看到的是空行程。

現在這樣還算誠實(狀態列說「紀錄只存在這台裝置」,而確實只有這台的東西)。
但包成 PWA 之後,網頁打得開、渲染完整、裡面是空的 —— **「空的行程」和
「還沒有行程」長得一模一樣**,沒有任何一個地方會報錯。所以這一則的順序是:
先修資料,再包殼。不修的話 PWA 只是把空殼包得更像一個 app。

- **決定 1:網頁殼和資料分兩條路快取,不合併。**
  `sw.js` 只管「網頁本身打不打得開」;行程花費那些資料由 `index.html` 自己存成
  `tokyo5-snap`(內容 + 讀到的時間)。
- **為什麼**:失敗模式不一樣。網頁檔是固定的幾個,快取起來永遠正確;
  Notion 的資料會變,快取起來就多出「這是什麼時候的」這個問題,**而那件事一定要
  講給人聽**。合在一起的話,畫面上沒有任何地方說得出那份資料多舊。
- **代價**:兩套快取、兩個版本概念,要各自維護。

- **決定 2:離線唯讀 —— 但只在「有副本」的時候。**
  `editable()` 從 `!online || canEdit` 改成 `online ? canEdit : !snapAt`。
- **為什麼**:舊規則在舊世界是對的 —— 離線動的是自己的沙盒,跟別人無關,所以隨便改。
  **一旦離線顯示的是共用紀錄的副本,同一條規則就變成「沒有通行碼的人可以改那份
  看起來是共用的行程」**,而且改完傳不回 Notion,下次重讀就無聲消失。
  所以分岔:有副本 = 別人的資料 = 唯讀;沒副本 = 自己的沙盒 = 照舊能改
  (GitHub Pages 那份走的就是這條)。
- **推翻了**:`85c041f` 以來「離線就等於可以編輯」這個等號。
- **代價**:真的在沒訊號的地方記不了帳,要先記在別的地方。
  離線可寫要處理重送、重複、五個人的衝突,而 Notion 沒有防重複的機制 ——
  那是另一整輪,不是這一輪順手做得完的。

- **決定 3:service worker 走 network-first,不是 cache-first。**
- **為什麼**:這個專案的節奏是改完就 push、馬上要在手機上看到。cache-first 的話
  五個人的手機會停在舊版,而且**沒有任何跡象** —— 畫面完整、資料也新,
  只有版面和修好的 bug 是舊的。那正是這份 repo 記過太多次的形狀。
- **代價**:線上時完全不會變快,每個檔照樣走一趟網路。接受 —— 要的是離線打得開,不是快。

- **決定 4:只在 `https:` 下註冊 service worker。**
- **為什麼**:這不是保險,是正確性。`shoot.sh` / `probe.sh` 跑在
  `http://localhost` 上,而 localhost 算安全來源,service worker **裝得起來**。
  裝起來就會跨好幾輪活著,下一輪截到的可能是上一輪快取的 `index.html` ——
  **比對照樣全綠,而綠的是上一份程式**。這份 repo 的每一件工具都在防同一件事,
  不能讓工具自己變成源頭。
- **代價**:本機試不了 service worker,要用 `vercel dev` 或直接看正式網址。
  `probes/pwa.js` 會反過來確認那道擋板還在。

- **沒走的路:地圖圖磚離線。** 圖磚是跨網域抓的,回的是 opaque response ——
  **狀態碼讀不到,一個失敗的請求和一個成功的長得一模一樣**,存進快取等於把
  「載不到」凍起來反覆播放。而且預先抓整個東京量太大,OSM 的使用條款也不允許。
  留到下一輪,要做就得先處理 opaque 這件事,不是把那行 `return` 刪掉。

- **沒走的路:字型自己託管。** 離線會 fallback 成系統字,版面跟線上不一樣。
  Noto Sans TC 全字集要 subset 才能放,不是順手的事。已寫進 README 的表裡。

### 三件工具自己的坑,都是同一個形狀 · `39bc0b3` `1f43e6e`

這一輪沒有一件是「PWA 難做」,三件都是**工具說成功但其實沒做到**。

| 撞到的 | 真正發生的事 | 補了什麼 |
| --- | --- | --- |
| `make-icons.sh` 出了四張純底色的方塊 | `icon.svg` 的註解裡寫了 `--board-bg`,**XML 註解不准有連續兩個減號**,整個檔解析失敗。Chrome 畫了一個「圖片載不到」的小圖示照樣存檔 —— **尺寸完全正確、腳本回 0** | 渲染前先解析一次 XML;出完圖再數一次琥珀色的佔比,幾乎全是底色就擋下來 |
| `probe.sh` 說「探針沒有回傳任何東西(JS 可能在 iframe 裡就掛了)」 | 埠 8998/8999 上躺著兩隻**別輪留下來的孤兒 server**,服務的目錄早就被刪掉,Chrome 拿到 404。而診斷訊息**指向一支完全正常的探針** | 兩支腳本開跑前先 `lsof` 確認埠沒人佔,佔了就大聲拒絕並印出是誰 |
| 孤兒 server 為什麼會存在 | `cd X && python3 ... &` 的 `$!` 是**那個子 shell** 的 pid,不是 python 的 —— `&&` 逼 shell 留著子 shell 判斷前一句的結果。trap 裡 `kill "$SRV"` 殺掉子 shell,python 原地變孤兒繼續聽著那個埠,**而 kill 回 0** | 加一個 `exec`:子 shell 被 python 取代,`$!` 拿到的就是 python 自己 |

第二件特別值得記:**錯誤訊息本身把人送錯方向**。訊息有發、也讀到了,
但它講的是一個假設(「JS 掛了」),而真正的原因在它從來沒檢查過的地方。
`ADOPTION.md` 記過四次「訊號活著,沒有人站在它的頻道上」——
這一次是**訊號活著、有人在聽,但訊號說的是錯的**。

### 比對基準挑錯,差點把修好的東西當成壞掉 · `1f43e6e`

截完圖第一次比對,`13-wish-edit` 有 **73%** 的像素不同。看起來像大災難。

實際上是**基準挑錯了**:`shots/we-negctl` 是**負控制**,從 `be3e0ef`(許願編輯
這個功能還不存在的那個 commit)截的,所以它那張 13 根本沒有對話框。
換成 `shots/we-final`(同一個 HEAD 的乾淨基準)之後,13 張裡 12 張逐位元組相同,
剩下 `11-exp-edit-dialog` 是 8px,簽章跟 ADOPTION 登記的雙穩態完全吻合
(6 個單列群、全部落在 `x=86` 或 `x=88`、色差 1–5/255)。

- **要記住的**:`shots/` 底下有六十幾個標籤,**名字看不出哪個是乾淨基準、
  哪個是負控制**。挑錯的代價不對稱 —— 挑到負控制會冒出巨大的假警報(還算好,
  會去查);挑到更舊的基準則可能讓真的回歸躲在「本來就不一樣」裡面。
  `ADOPTION.md` 底下那張基準表就是為了這件事存在的,**比對前先查那張表**。

## 2026-09-20

### `bounded=1` 帶不帶,答案跟「誰在挑」綁在一起 · `14121ee` `b21fff2`

**先更正一件事:「改過主意的地方」那張表本來只記到第二步**(`57473b5` 拆掉硬界線),
而程式在 `14121ee` 就把它加回來了。照那張表回溯的人會以為現在沒有硬界線 ——
**一張給回溯用的表只記到一半,比不記更誤導。**

三步完整是這樣,而且第三步不是單純的反悔:

| | 做了什麼 | 為什麼不成立／為什麼成立 |
| --- | --- | --- |
| `863ee2a` | viewbox 當硬界線 | 以為這樣擋得住亂給的結果 |
| `57473b5` | 拆掉,只當加權 | 實測它連原本要擋的都沒擋住,而且會從框內硬挑一個交差:京都車站→東京車站大飯店、金閣寺→埼玉的日立金屬熊谷工場 |
| `14121ee` | **加回來,但框改成每次查詢自己選**(`regionFor`) | **兩件事是一起的。** 第二步真正的問題不是「有框」,是「框鎖死在這趟的城市」—— 所以「京都車站」被迫在東京框裡找。框會跟著字串走之後,硬界線就只擋它該擋的:不是地名的字串在全日本一定找得到「某個」東西(「泡溫泉」給鳥取的吉方温泉通り、「港灣未來」給福井的勝山丸岡線) |

- **決定(這一輪新增的那一半)**:地點搜尋的 `seekPlace()` **不帶** `bounded=1`,而 `pinFor()` 繼續帶。
- **為什麼不是不一致**:差別不在服務,在**誰在挑**。`pinFor` 是程式自己挑一筆直接標上去,
  框外的東西進來就是一顆沒有人看過的錯 pin;搜尋是人看著名字和地址挑,
  **硬界線只會把他要的那個藏起來**,而挑錯的那個他不會按。
  `57473b5` 當初拆掉的理由(「它不會回答查無,只會硬挑一個交差」)**在有人看著的時候不成立**。
- **代價**:同一個參數在兩條路上不一樣,而程式裡看起來像手滑。兩處都寫了註解指向這一則。

### 人工表留著,而理由跟「哪一家比較準」無關 · `未進版控`

Lulu 問:「人工表可以不用了吧,都可以用 google map 了。」在那之前這個 repo
對這件事**一筆量測都沒有** —— 表存在的理由(`275da9b`)全部是 **Nominatim** 的黑歷史,
不是 Google 的。所以先量:20 組字串 × 3 家,判準先訂再看結果。
完整數字見 `adopt-harness/table-vs-apis.md`,工具是 `table-vs-apis.js`。

- **決定**:人工表維持當預設,Google 維持只當「人看到錯的 pin 才按」的退路。
  **這份量測支持現在的架構,不推翻它。**
- **成績**:open-meteo ✅2 / Google ✅10(20 題)。Google 最高,但 ——
- **為什麼還是留著,三個理由都是量出來的**:
  1. **三家的錯不重疊**。輕井澤、迪士尼、富士電視台 Nominatim 對而 Google 錯 465／160／151 km;
     箱根湯本站、NRT 成田 T1 反過來。**沒有一家是另一家的超集,換一家只是換一批會錯的。**
  2. **有兩類問題是地名查詢結構上答不出來的**:同名地點(`草津` 三家全錯 297km ——
     群馬的溫泉 vs 滋賀的市,那是這趟旅行的事實不是服務的知識)、
     以及不是地名的字串(`TPE 桃園 T1` 是航班資訊)。`成田` 同理:表指機場,三家指市中心。
  3. **Google 是唯一會對非地名硬給答案的**:`樂桃 MM626 · 建議起飛前 2.5 小時`
     兩家免費的都誠實說查無,Google 回「日本」。**那正是雲南那顆 pin 的成因**,
     也正是這條退路要人按了才走的理由。
- **代價**:那張表是手維護的,而它現在有 13 條。加一個新的出東京目的地要記得加進去 ——
  忘了就掉到線上查詢,而線上查詢對這些地方最不準。**這個代價沒有變,只是現在有數字知道它值多少。**
- **花了多少**:Google 那一欄 20 次查詢,約 US$0.11,Lulu 的信用卡。免費那兩欄不用金鑰。

### `富士` 從人工表拆成 `富士山` · `未進版控`

- **決定**:`{ keys: ["河口湖", "富士"] }` 改成 `["河口湖", "富士山"]`。
- **為什麼**:表是**子字串比對**,所以「富士電視台」(台場)被拉到河口湖 ——
  量到差 151km,而同一個字串 Nominatim 只差 0.03km。**表在那一筆上是比較差的答案。**
  這是 `geocode-outside-and-notice.md`「決定 2」登記的危害,現在量到了。
- **推翻了** 那一則把危害範圍寫成「只發生在標題那條路」的說法。`pinFor()` 的表比對排在
  線上查詢之前,所以地點欄填「富士電視台」在任何冷快取的裝置上都會中。
- **連帶**:對帳多了反方向的一圈。**帶著 `via`、而表已經不認得的快取,`delete` 掉** ——
  不然拆 key 之後那些舊資料永遠不會再被碰到。用 `delete` 不是 `null`:
  `null` 的意思是「查過了,沒有」,會讓 `pinFor()` 再也不去問線上。
- **代價**:「富士急樂園」這種以前被表接住的字串,現在會掉到線上查詢(多一次請求)。
  接受 —— 表本來就不該替一個它只猜中兩個字的地方作答。

### 沒走的那條路:REGIONS 補台灣 · `未進版控`

做了,然後撤掉。理由留在原始碼那條註解上。

- **當初的問題**:量到「桃園」用 `cc=jp` 去問會拿到 1343km 外的東西(三家都是)。
- **為什麼撤掉**:`regionFor()` 的兩個呼叫點(`pinFor` 和 `regeo`)**都在查表之後**,
  而台灣那條的 key(桃園／TPE)跟人工表的 key 一模一樣 —— 會選到它的字串,
  表一定先答掉了。**那是死碼。**
- **真正的教訓**:那筆量測量的是**腳本裡獨立重現的 `regionFor`**,不是 app 走得到的路。
  **重現一段邏輯來量它,就等於拿掉了它在真實程式裡的所有前置條件。**
  數字是對的,結論是錯的。
- **什麼時候它會變成該做的**:key 放寬到「台北」「台灣」那種表答不出來的字。
  這是東京行,那種字串現在一筆都沒有。

### 舊快取跟人工表對帳,而不是換一個快取 key · `未進版控`

`via`(「這顆座標來自人工確認過的表」)以前只掛在函式的回傳值上,一寫進 `pins[place]`
就掉。修寫入端只救得了**新的**裝置 —— 五個人瀏覽器裡那批舊資料會被 `pinFor()` 開頭
那句 `if (place in pins) return pins[place]` 直接放行,錯的留著,而且長得跟對的一模一樣。

- **決定**:載入時掃一次 `tokyo5-pin3`,凡是 `OUTSIDE` 表答得出來的 key,
  一律換成表的值並補上 `via`。**不換快取 key。**
- **為什麼不換 key**:`57473b5` 那次換過(`pin2`),代價是把大家查好的、跟這張表
  無關的座標一起丟掉,每一顆重查、每次排隊 1.1 秒。這次要修的只有表答得出來的
  那幾個 key,其餘一個字都不該碰。
- **它順手還掉一筆被寫成人的待辦的債**:上一輪把「Lulu 要自己在桃園那一列再按一次
  『再查一次』」登記成 N7、註明「要當面講,程式不會自己講」。**那件事程式做得到。**
  對完帳之後快取裡的值就等於「這台裝置第一次載入會算出來的值」,沒有人需要按任何東西。
- **代價一**:有人曾經對「表答得出來的地點」按過「再查一次」拿到更準的線上座標
  (例如 `箱根湯本站` → 表只給箱根的概略中心),對帳會把它換回表的值。
  **但這不是新的單向門** —— 上一輪的 `regeoItem` 已經讓那種座標再也查不回來了
  (表命中就直接回、不打 API),這裡只是把已經做過的決定補套用到舊資料上。
- **代價二**:對帳會清掉那幾筆上的 `ok`(使用者按過「對」)。表的值不帶 `ap`,
  `paintFix` 的 `asking` 永遠是 false,所以那個確認沒有作用可以失去。
- **順帶清掉一顆看不見的錯東西**:修好之前,快取裡那個錯座標(東京車站)**在日本境內**,
  所以 `TPE 桃園 T1` 那兩列進得了可繪集合 —— 許願地圖上會有一顆標著「MM626 起飛」的
  pin 插在東京車站附近,而畫面上沒有任何東西說它是錯的。

### 人工表的範圍比「決定 2」登記的寬,而且不是這一輪造成的 · `未進版控`

- **查到的事**:`pinFor()` 的表比對排在線上查詢**之前**,所以地點欄填 `富士電視台`
  的那一筆,在任何一台冷快取的裝置上都直接拿到河口湖,差 100 公里 ——
  跟「標題也算候選」那條規則無關,一直都是這樣。
- **決定**:**這一輪不動它**,只把範圍記對。`geocode-outside-and-notice.md` 的
  「決定 2」把危害寫成「只發生在標題那條路」,那個範圍寫窄了。
- **為什麼不順手改**:那張表同時服務天氣(`resolvePlace`),改 key 或改比對方式是
  三個消費者一起變,而且會動到 01 / 02 兩張截圖。**要 Lulu 先拍板,不是修 bug。**
- **代價**:在她拍板之前,這個 100 公里的誤差是**已知而且放著的**。

## 2026-09-19

### 拿掉深色模式 · `ef364ce`

- **決定**：移除 `@media (prefers-color-scheme: dark)` 和 `:root[data-theme="dark"]` 兩塊 token 覆寫，`color-scheme` 從 `light dark` 改成 `light`。
- **為什麼**：要接 `design-system/`，而它**刻意只做淺色**（`7390406`）。先移除是讓這件事變成明講的決定，而不是接線那天才發現的副作用。
- **更正（同日）**：這則原本還寫了第二個理由 —— 「`dist` 整份輸出 `var(--rm-` 出現 0 次，所以接上去深色就救不回來」。**那個理由是錯的**，寫的人（我）沒查清楚就當成論據：`design-system/dist/` 被 `.gitignore` 排除、不在版本控制裡，我讀到的是一份**還沒重編的產物**。同一天稍後重編之後，`.bg-paper` 編出的是 `rgb(var(--rm-paper)/…)`。移除深色這個決定站得住，但它只站在上面那條「刻意只做淺色」上，跟 `dist` 當時長什麼樣無關。**別拿不在版控裡的 build 產物當論據。**
- **`color-scheme` 不是順手改的，是必要的**。它管的是**瀏覽器原生控制項**——日期/時間選擇器、捲軸、表單預設底色。留著 `light dark` 的話，使用者系統是深色時，那些原生元件仍然自己畫成深色，貼在淺色版面上。下一個人最容易漏的就是這層：CSS token 全改成淺色了，畫面看起來卻還是有東西是深的，而且怎麼翻樣式表都找不到。這剛好打到 `9d915eb` 才修過的時間欄位。
- **`--board-*` 沒有動**。那組（深底琥珀字的航班板）是**淺色模式裡的深色元件**，不是深色模式的一部分；只有深色覆寫區塊裡的 `--board-bg`、`--board-line` 兩行跟著走。
- **順帶清掉死碼**：`[data-theme]` 全檔只出現在那兩個選擇器自己的條件裡，程式沒有任何地方會去設它，所以手動切換主題從來沒有接上過。
- **代價**：在深色系統上這個站從此是亮的。已驗證（Chrome `--force-dark-mode`）：`prefers-color-scheme` 為 dark 時 `color-scheme` 仍解析成 `light`，`--paper`／`--ink` 維持淺色值，`--board-bg` 保留 `#12141A`。
- **套用前的基準點**：`pre-design-system` 這個 annotated tag 指著 `2b773b7`，也就是這次改動的前一狀態 —— 單檔內嵌樣式、281 處 `var(--*)`、完整深色模式、未引用 design-system。要比對套用前後的樣式層就跟它比（`git diff pre-design-system -- tokyo-trip/index.html`）。
- **還沒發生的**：這只是移除深色，**還沒有接上 design-system**。四步順序是「移除深色 → token 存法 → build → 換引用」，目前只走完第一步。
- **第二步（token 存法）是待決，不是定案**，正在評估要不要直接走 Tailwind v4（v4 的 `@theme` 原生就編成 `var(--color-*)`，不需要通道值那套體操）。
- **而且它不急，可以晚點再決定**：Tailwind 的 utility **class 名稱由 preset 的 key 決定，不是由值決定**。`colors` 裡 key 叫 `paper`，編出來就是 `.bg-paper`，不管值寫的是 `#F1EFE6` 還是 `rgb(var(--rm-paper)/<alpha-value>)`。所以先接、之後再換存法，**markup 一個字都不用動**，只要重編。這裡沒有單向門 —— 這一則早先的版本說過「晚改就要重改整個樣式層」，**那句也是錯的**，理由同上。

### 地名查詢的範圍只加權，不設界線 · `57473b5`

> **這一則已經被推翻了一半,不要照著它讀現況。** `14121ee` 把硬界線加回來了,
> 同時把框改成每次查詢自己選 —— 見 2026-09-20 那則「`bounded=1` 帶不帶,
> 答案跟『誰在挑』綁在一起」。下面這些理由本身仍然成立,它們解釋的是
> **為什麼「鎖死在這趟城市的框」不行**,不是「為什麼不要有框」。

兩個京都的願望落在東京兩個不相干的位置上。

- **決定**：Nominatim 查詢拿掉 `bounded=1`，viewbox 只當加權；真正擋離譜結果的是 `countrycodes`，那個留著。
- **為什麼**：硬界線的意思是「框外的一律不算」，所以它不會回答查無，只會從框內硬挑一個最像的交差 —— 京都車站給「東京車站大飯店」、伏見稻荷給荒川區一個同名小社、金閣寺給埼玉的「日立金屬熊谷工場」。名字對不上也照給，而且每次挑到的還不一樣，看起來像壞掉而不是查錯。
- **推翻了** `863ee2a` 加上 `bounded=1` 的理由。當初以為它擋得住亂給的結果，實測它連原本要擋的都沒擋住 ——「晴空塔」查到埼玉的「青空広場」，設不設都一樣，因為青空広場本來就在框內。
- **代價**：同名地點要打完整（東京行查「清水寺」給的是橫濱那間，要京都的就打「京都 清水寺」）。另外快取 key 得改成 `tokyo5-pin2` —— 錯的座標已經在大家的瀏覽器裡，而且長得跟對的一模一樣，不改名這個修正看不出來。

## 2026-09-18

### 地圖連結帶座標，查詢範圍由網頁標題決定 · `94634b6`

點「輕井澤」開出一堆台灣的火鍋店。

- **決定**：跳出去的 Google 地圖連結帶上地圖中心（`/@36.342,138.629,14z`），不只帶名字。查詢國家由 `REGIONS` 對照網頁標題決定 —— 標題有「東京」查日本、有「首爾」查韓國，認不出來當東京。
- **為什麼**：只丟名字過去，Google 會拿「使用者人在哪」來猜。座標在標 pin 的時候就已經查到了，本來就在手上。
- **代價**：`REGIONS` 是寫死的表，新城市要加一行。但換來的是把 `<title>` 改成「大阪京都自由行」，查詢範圍自己就跟著換，不用去翻程式碼裡的座標。

### 時間欄位不要長得像另一種控制項 · `9d915eb`

- **決定**：`input[type=time]` 和 `input[type=date]` 關掉原生外觀，清掉內層值的 margin 和置中。
- **為什麼**：iOS Safari 把它畫成一顆圓角厚按鈕、值置中，跟下面那排文字欄位完全不同調。點下去照樣升起系統的時間轉盤。
- **代價**：Chrome 上時間欄位還是 48px 對文字欄位的 46px —— 那兩像素來自控制項自己的 shadow DOM，要清掉只能藏起 picker 按鈕，那會讓桌機使用者沒得按。不划算，而且本來看得見的問題不是這個。
- **已驗證**（2026-09-19）：Lulu 在自己的 iPhone 上開 production 網址看過，時間欄位跟下面的文字欄位**一樣高**，原生外觀確實被關掉了。上面那 48px／46px 是 Chrome 專屬的,不影響實機。

  這一則從寫下到驗證中間隔了兩天,而卡住的不是難度 —— 是它需要一個機器做不到、人做起來三十秒的動作。中間還因為我們拿專案名去猜網址、打到受保護的別名,白以為整個站掛了,又多卡一輪。

  **所以:需要真人動手的驗證,在提出的當下就要講清楚「要誰、在哪台裝置、看什麼、怎樣算過」。** 少了這四樣它會被無限期拖著,而拖著的時候看起來跟「還在進行中」一模一樣。這個專案有 iOS Safari、五個真人使用者、只有 Lulu 進得去的 Vercel 後台 —— 這類尾巴是結構性的,不是這次的意外。

### 願望地圖的入口改成 icon，放進自己的標題列 · `77e13c0`

- **決定**：願望地圖從許願池裡一條整寬的文字連結，改成「大家的許願」標題列最右邊的 icon，位置在展開箭頭左邊。
- **為什麼**：同一張 sheet 有兩個入口，一個是 icon 在 DAY 標題列、一個是文字連結在區塊內容裡，形狀和位置都不一樣。現在兩個都是「那一列最右邊的 icon」。
- **代價**：按鈕在 `<summary>` 裡，點它得擋掉冒泡，不然會順手把整個許願池折起來。

### 顏色由「它是什麼」決定，不是由「你在看哪張圖」決定 · `74db440`

同一個願望在 DAY 3 的地圖上是綠的，在許願地圖上是藍的。

- **決定**：每個點自己帶顏色 —— 願望永遠是主色，行程永遠是它那天的 Day 色。不繼承任何容器。
- **為什麼**：原本靠繼承容器的 `--dc`，而 `--dc` 是「你現在看第幾天」。等於顏色在描述地圖，不是在描述地圖上的東西，同一個地方會隨著你在哪張圖上而變樣。
- **推翻了** `863ee2a` 讓 pin 繼承容器顏色的做法（那時候只有一張圖、只有一種點，看不出差別）。

### 地圖是兩個圖層，各有一顆開關 · `2b7c53b`

- **決定**：願望和已排行程畫成同樣大小、同樣份量，靠顏色分，兩顆開關決定看哪個。從哪顆按鈕進來就保證那一層是開的，最後一層不准關掉，選擇記在瀏覽器裡。
- **為什麼**：原本把行程畫小、畫淡當背景，那是在猜「你現在在看願望」—— 有一半的時候猜錯，行程才是主角。
- **推翻了** `79b8dec` 把行程當背景的前提。
- **順帶**：標籤改成一片一片排、佔走了就往下讓、下面也佔走就先藏起來（hover 或聚焦時再冒出來）。十一個點的實際情況下三個往下讓、兩個等著，沒有一個疊在一起。

### 換天的時候地圖跟著換色，hover 變雙向 · `838448a`

- **決定**：pin 的顏色和 sheet 的標題改成每次載入時重算，不是打開的當下設一次。滑過地圖上的 pin，清單那一列也亮起來。
- **為什麼**：開著地圖切換天數，pin 還留在前一天的顏色、標題還是前一天的日期。
- **順帶**：單獨一個點不再用街道級的縮放打開 —— 取景只看主角那一組，一個願望就把地圖塞滿，周圍的行程 pin 全被推出畫面，而那些正是用來判斷「這個願望離什麼近」的。

### 每個 pin 上面掛自己的名字 · `b357d8a`

- **決定**：pin 正上方掛一小片「做什麼／想去哪」那欄填的字，hover 時跟著 pin 一起放大。
- **為什麼**：一個編號只說得出「在哪」，說不出「是什麼」，讀地圖還是得回清單查號碼。
- **代價**：許願模式下背景可能有十幾個行程 pin，十幾片標籤會讓地圖變成一面文字牆 —— 正是標籤想省掉的事。所以已排行程的標籤預設藏著，hover 或聚焦才出現。
- **另外**：標籤 `pointer-events:none`。它蓋在圖磚上，吃掉拖曳的話，只要手指落在 pin 附近地圖就像壞掉。

### 滑過清單那一列，地圖上對應的 pin 跳出來 · `bf1f3d1`

- **決定**：hover 時在對應的 pin 上切一個 class，不重畫地圖。只裝在有指標的裝置上。
- **為什麼**：每列重畫一次會把整片圖磚重建，一路滑下來整張圖都在閃。
- **代價**：一列對到哪顆 pin 跟模式有關（行程在行程模式下是主角、在許願模式下是背景），所以 key 要照當下的模式算，不能寫死。
- **不做觸控**：手機沒有 hover 這回事，碰一下就等於點下去。

### 願望地圖上畫出已經排好的行程 · `79b8dec`

- **決定**：許願模式的地圖把六天所有行程都畫上去，畫小一點、半透明，帶它那天的顏色和編號。從 DAY 標題列進來的那張不變，只有那天。
- **為什麼**：要決定一個願望排哪一天，得知道它附近已經排了什麼。
- **代價**：取景刻意不把行程算進去 —— 算進去的話，一個成田機場就把視野拉到蓋住整個千葉，東京那幾個願望擠成一團，而那團正是你打開地圖要看的。
- **順帶**：pin 開始帶自己的身分（class、顏色、key），因為兩組點共用一張圖，而「願望 3」和「DAY 3」會互相點亮對方。

### 使用者打的字不准把版面撐寬 · `181c077`

- **決定**：`overflow-wrap:anywhere` + grid／flex 子項 `min-width:0`，最後補 `body{overflow-x:clip}`。
- **為什麼**：一個民宿地址打進去就把整頁撐出去。grid／flex 子項預設 `min-width:auto`，也就是「不肯縮到比內容窄」—— 只有 `overflow-wrap` 沒有 `min-width:0` 是沒用的，兩個要一起。
- **為什麼是 `clip` 不是 `hidden`**：`hidden` 會讓 body 變成捲動容器，頂上那條 sticky 標題列就壞了。
- **驗證**：390px 下放進真實地址加一條完整的 Maps 網址加一串 48 字不斷行的文字，document／body／`.wrap` 都回報 390/390，`.stop`／`.pl`／`.nt`／`.ti` 沒有一個溢出自己的框 —— 是斷行，不是被裁掉。

### 從網頁貼過來的文字要留住連結 · `fb2589c`

- **決定**：自己接 `paste`，剪貼簿 HTML 裡的 `<a>` 寫成 `[文字](網址)` 存進欄位，顯示時再變回連結。
- **為什麼**：從網頁複製一段字，連結藏在格式裡，而 `<input>` 只收純文字 —— 貼進去的當下網址就沒了，只剩看得見的那幾個字。
- **代價**：編輯的時候會看到那串標記。這是兩邊都是純文字的必然結果（Notion 欄位存字串、`<input>` 也存字串），要留住網址就得把它寫進文字裡。票數存成 `[+1:…]` 是同一套做法。
- **邊界**：文字本身就是網址的 `<a>` 不包第二層；`mailto:`、相對路徑那類非 http 的丟掉只留文字，反正從這裡也開不起來。

### 一張地圖，兩種內容；入口只決定它裝什麼 · `c663f5f`

- **決定**：合成同一個 sheet，兩個入口決定它裝哪一天／裝願望，標題和 pin 顏色跟著變。入口固定在 `DAY n · 10/0n` 那一列最右邊。
- **為什麼**：同一件事（看這些地方在哪）長成兩種形狀 —— 行程頁是內嵌的 `<details>`，許願池是滑上來的 sheet。而且入口原本在時間軸下面，時間軸長度每天不一樣，等於每天都要重找。
- **推翻了** `863ee2a` 的內嵌地圖和 `3d69766` 的獨立 sheet。
- **順帶拿掉**地圖下面那幾行說明。數量在標題列已經講了（「3／3」），其餘在講的事 —— 可以拖、有個點很遠 —— 地圖被拖一下就自己說明了。

### 時間軸的號碼牌就是地圖上的那顆 pin · `7df38e2`

- **決定**：時間軸每列前面的圓點換成跟 pin 一模一樣的號碼牌，同編號同樣式同顏色。點那一列，地圖打開並移到那個點，pin 和列一起亮。
- **為什麼**：本來兩邊各排各的，而兩個分開的排序在「同一分鐘的兩筆」上會給出不同順序，編號就對不起來。現在共用 `dayList()`。
- **修掉**：點 pin 沒反應。拖曳用的 pointer capture 裝在容器上（好讓手指滑出去也還在拖），這會把 click 的目標從 `<a>` 上搶走，連結永遠不會觸發 —— 改成在 `pointerdown` 記下手指下的 pin，沒有移動的那一下直接開。
- **順帶**：桌機的抽屜改成把版面推開（`body` 加 `padding-right`）而不是蓋住，左邊緣的握把拖得動，吸到螢幕的 1/4、2/4、3/4、4/4。存的是「第幾檔」不是像素，換螢幕還是同一個比例。

### 地圖開著的時候，頁面要照樣能用 · `a5aad01`

- **決定**：拿掉遮罩。容器 `pointer-events:none`，只有卡片本身收事件；背景不變暗、不模糊，照樣捲得動點得到。桌機（≥641px）改從右邊滑進來當抽屜。
- **為什麼**：地圖存在的理由就是拿來跟清單對照 —— 看兩個願望誰在誰旁邊，然後捲下去對兩個都按「加入」。原本變暗加擋住點擊，等於要先關掉地圖才能動手，那時候已經在憑記憶做事了。
- **推翻了** `3d69766` 的遮罩。
- **代價**：沒有遮罩可以點，關閉只剩 ✕（桌機加 Esc）。

### 拿真實資料跑一遍，修掉三件事 · `c06ecb5`

從 Notion 把真的資料讀出來，不是拿編的資料測 —— 三個問題只有真實資料才看得到。

- **地點欄常常是空的，或根本不是地名**（實際資料裡有一筆填「🦞」），但標題本身往往就是地名。兩張地圖改成地點欄查不到就拿標題再查一次，許願地圖從 1/7 變 6/7。剩下沒標的是「泡溫泉」—— 那本來就不是地點，標不出來才對。
- **氣溫掉了**：Open-Meteo 的降雨機率比氣溫多預報一天。改成兩個都有才存 —— 半個讀數旁邊空一塊看起來像壞掉，而且那天永遠是預報最遠最不準的一天，隔天就補齊了。溫度計還得補 U+FE0F，`U+1F321` 預設是文字呈現，iOS 上直接空白。
- **取景改成只遷就中位數那一群（60 公里內）**，不是框住每一個點。一個 128 公里外的輕井澤就把整張圖縮到看得見群馬，東京那幾個願望擠成一團 —— 而那團正是你打開它要看的。離群的點照樣畫，在說明裡點名。
- **順帶**：兩張地圖都可以拖、捏、雙擊、按 `+` `−`。手勢只動圖層的 transform，放開才重畫 —— 每次 `pointermove` 都重畫的話，手指還沒放開整片圖磚就一直在閃。

### 所有願望標在同一張圖上，從底部滑上來 · `3d69766`

- **決定**：許願池加一張地圖，所有有地點的願望標在上面，編號對應清單。用 bottom sheet，不是內嵌。
- **為什麼**：要把附近的願望排同一天，得先知道哪些在附近，一串地名看不出來。而地圖是「看一眼就收起來」的東西，不該長期佔著清單 260px。
- **代價**：沒填地點的願望標不出來，所以清單上也不給它編號 —— 給了編號就會跳號，而編號存在的意義就是兩邊對得起來。

## 2026-09-17

### 編輯對話框可以把行程搬到別天 · `240d251`

- **決定**：編輯對話框最上面加「哪一天」，六天加上 10/09 凌晨（紅眼班機那段掛在最後一天底下）。
- **為什麼**：一筆行程什麼都改得動，就是改不了它屬於哪一天 —— 排錯天只能刪掉重打一次。
- **代價**：改了天，它會從你正在看的那一天消失，讀起來像被刪掉了。所以狀態列要講一聲它去了哪（「『明治神宮』移到 DAY 5 · 10/07 了」）。但**不會跳過去** —— 你八成還在排現在這一天。
- **坑**：舊的日期要先複製成字串再存。`updateStop` 是直接寫進原本那個物件的，留著 reference 的話，等到要比對的時候它早就是新值了。

### 把願望排進行程改用按鈕，不是拖曳 · `11cf284`

- **決定**：願望右邊加一顆「加入」，一下就好。排進**你現在看的那一天**，問幾點，預設是那天最後一筆再加 30 分鐘。
- **為什麼**：拖曳對拇指要求太多 —— 長按、等卡片浮起來、手指不能放、一路拖過整份清單。而且按鈕看得出來可以按，長按看不出來。
- **推翻了** `46c630e` 的拖曳（連同 `b1761d1` 為它做的跟手動畫）。
- **順帶**：時間不設上下界。按鈕不像放下那樣瞄準兩個特定行程之間的縫，而時間軸本來就照時間排，選什麼都會落到該去的位置。
- **移除**：`initWishDrag` 和只有它在用的 `.wish.lifted`、`.dropline`、願望清單的 `can-sort` 游標、`neighboursForInsert`。行程之間的拖曳保留，沒動。

### 存檔的當下就查地點，查不到馬上講 · `0f8c0c2`

- **決定**：存行程的時候立刻查一次地點，查不到就在狀態列說「『晴空塔』查不到，地圖上不會顯示 —— 試試把地名打完整」。新增、編輯、把願望排進去，三個入口都查。
- **為什麼**：查不到就只是默默少一顆 pin，等你打開地圖發現少一個點，已經想不起來是哪一筆打錯了。
- **成本是零**：查詢本來就有快取，看過的地點不用再問。

### 當天的行程上地圖，不引地圖函式庫 · `863ee2a`

- **決定**：行程頁加一張可收合的地圖，有地點的行程照順序編號、帶那天的顏色，點編號跳 Google 地圖。圖磚直接排 OpenStreetMap 的，投影自己算。
- **為什麼不用函式庫**：目的是看相對位置，不是導航，不需要一套完整的可拖可縮地圖 —— 而這個檔案一直沒長出 CDN 依賴。
- **為什麼不用 Google 嵌入**：要 API 金鑰、要綁信用卡。OSM 兩個都不用，而點出去跳 Google 地圖也不用金鑰。
- **座標怎麼來**：Nominatim，它讀繁體比天氣用的那個準很多（淺草寺、阿美橫町、二重橋都查得到）。三層，由準到不準：手工確認過的 `OUTSIDE` 表 → 整串去查 → 去掉第一個詞再查（「銀座線 淺草站」要的是「淺草站」）。
- **只拆一層是刻意的**：拆到最後一段會查到「樂桃 MM626 · 建議起飛前 2.5 小時」裡的「小時」，然後命中船橋市一個真的叫「小室」的地方。
- **日本境外的點不標**：桃園查得到（天氣要用），但一張圖同時裝台灣和成田就得縮到看得見鹿兒島，兩個點都被擠到角落。Day 1 只畫成田，跑道級的縮放。
- **重畫的判斷是「哪一天＋有哪些地點」**，不是只看哪一天 —— 不然管理員新增一筆要重整才看得到。
- **後來推翻**：這裡加的 `bounded=1` 在 `57473b5` 拿掉了。

### 重新整理要連天氣一起抓 · `06a2e4a`

- **決定**：狀態列的「重新整理」連天氣一起重抓，不等那三小時的快取。兩件事並行跑，天氣失敗在 `refreshWeather` 裡面就吞掉。
- **為什麼**：本來只拉 Notion，天氣自己走三小時的計時器 —— 按下去看到「已更新」會以為全部都是最新的，其實氣溫可能是三小時前的。
- **為什麼並行**：天氣掛掉不該擋住 Notion 那半邊完成。

### Day 方塊顯示天氣，出東京的行程才另外標 · `275da9b`

- **決定**：每個 Day 方塊顯示當天東京的下雨機率和氣溫；行程那一列只有**離開東京都**的地點才顯示。
- **為什麼都內不顯示**：淺草、豐洲、新宿的數字跟上面 Day 方塊那個是同一個，每列重印一次只是噪音。箱根跟東京當天差 2 度和幾個百分點 —— 那個差別才是這一行存在的理由。
- **哪些算「出東京」是寫死的表，不是查詢** —— 線上查詢對日文地名爛得很具體：「日光」給熊本縣的日光、「横浜」給青森縣的横浜、「箱根」「輕井澤」直接查無。拿那種座標抓回來的天氣**看起來完全正常**，其實是九百公里外的數字，比空白還糟。
- **表裡沒有的還是去查**，但回來要落在關東到中部的框裡、而且 `admin1` 不是東京都才採用 —— 所以之後手動加的地點走同一條規則，不用回來改程式。
- **預報只到 16 天**：出發前太早看，那幾天根本沒資料，就顯示空白，不拿鄰近日期的數字頂替。

## 2026-09-16

### 拖曳中的卡片要跟著手指，並標出會落在哪 · `b1761d1`

- **決定**：浮起來的卡片跟著指標移動，一條線標出它會插進去的位置。
- **為什麼**：本來只加個陰影，卡片留在原地不動，只有時間軸在亮 —— 手勢跟被拖的東西感覺是分開的。
- **坑**：那條線要先從版面裡拿掉才能量測。留在裡面會把下面的列往下推，而算 index 用的中點就跟著移動，手指停著不動 marker 也會在兩個位置之間跳。
- **後來推翻**：拖曳本身在 `11cf284` 換成按鈕了。

### 許願池跟行程共用一個資料庫，差別只在有沒有日期 · `46c630e`

- **決定**：願望就是行程資料庫裡沒填「日期」的列（網站本來就會丟掉那些），所以 Notion 那邊什麼都不用設定，而「排進行程」實際上就是幫那一列補上日期和時間。
- **票數存在備註裡**（`[+1:名字,名字]`）：在 Notion 表格裡看很醜，但為了一個計數器去改資料庫結構更不值得。
- **這是寫入鎖上開的第一個洞**：願望的 `POST`（許願）和 `PATCH`（+1）不用通行碼，不然非管理員就許不了願。洞開得很窄 —— `DELETE` 還是要管理員，排進行程走的是行程那條、也要管理員，而且沒通行碼的 `PATCH` 會把原本的標題、地點、備註從 Notion 讀回來蓋上去，所以路過的人動得了票數，改不了別人願望的內容。

### 花費要能改，不是只能刪掉重打 · `cca7279`

- **決定**：每個欄位都能改 —— 日期、項目、分類、金額、幣別、付款人、分攤者、備註。
- **為什麼**：最容易打錯的是「誰分攤」，而那偏偏是原本唯一只能整筆刪掉重打的東西。
- **順帶**：改了分攤者，共同基金立刻跟著重算 —— 從一筆花費裡拿掉兩個人，那兩個人的進度條會降、剩下的人會升。

### 共同基金算的是「分攤到多少」，不是「掏了多少現金」 · `8c6be83`

- **決定**：每人 NT$30,000 的基金放在分帳頁最上面，一條總的加每人一條。「花了多少」算的是**他分攤到的金額**。
- **為什麼**：旅程中間的問題不是「誰欠誰」（那是回程飛機上的事），是「我那三萬還剩多少」。誰先墊的錢是下面「每個人的帳」在管的，兩件事分開看才不會混。
- **結果**：只有兩個人合買的東西只扣那兩個人的額度，進度條會長得不一樣 —— 那才是誠實的畫面。
- **`30000` 放 `config.js`**，跟匯率一起 —— 同一種數字：五個人共用、改一個地方。

### 每個人兩個名字：畫面上的和 Notion 裡的 · `95be68f`

- **決定**：`def` 是畫面上顯示的（阿輝、佳瑜、志偉、雅倫、媽），`key` 是 Notion「付款人／分攤者」select 真正存的字串（Chinhui、Chiayu…）。讀的時候兩種都認，寫出去一律用 `key`。
- **為什麼**：混成一個的話，改一次顯示名就要去 Notion 把 select 選項改掉、再把每一筆既有紀錄重選一遍。
- **讀兩種都認**是為了就算哪天真的在 Notion 把選項改成中文，舊紀錄也不會變成「未指定付款人」。**寫只用 key** 是免得 Notion 長出第二組選項。
- **順帶**：重新整理的按鈕改成原地轉圈，至少轉 450ms —— 40 毫秒就消失的 spinner 讀起來像閃了一下的故障，不像「好了」。

### 座位表，回程換位的三個人標出來 · `5589921`

- **決定**：航班看板列出誰坐哪個位子；回程 MM631 換位子的志偉、雅倫、媽用琥珀色框起來。
- **為什麼**：本來「27A 27B 27D 27E 27F」印兩次，既沒告訴任何人該坐哪，也藏住了回程根本不是同一張訂單這件事。框起來是為了不要有人憑肌肉記憶登機。

### icon 改用 Font Awesome，內嵌不連 CDN · `e4a7fa7`

- **決定**：Font Awesome Free 6.7.2 solid，SVG 直接內嵌成 `<symbol>`。底部導覽要 icon，順手把散在各處的字元替身（`＋` `✕` `✎`）也換掉。
- **為什麼內嵌**：這個網站是在東京用手機打開的，而且有離線模式。外部樣式表好一點是漫遊時多一趟來回，糟一點是圖全都不見。六個 icon 內嵌 3.4KB，webfont 約 100KB。
- **為什麼一次換完**：一套 icon，不是「當時看起來夠像的那幾個字元」。

### 長按的目標是整列，不是時間那一欄 · `3761a64`

- **決定**：長按行程列的任何地方都能把它抓起來。按 ✎ 或 ✕ 還是點擊，不是長按。
- **為什麼**：時間欄太小，這個手勢屬於整列。
- **坑**：原本的做法是給 54px 寬的時間欄釘上 `touch-action:none`，那不花什麼成本；釘在整列上就會讓整份行程在拇指下捲不動。所以改成捲動先不動，等長按真的成立，再用一個非 passive 的 `touchmove` 從那一刻起取消捲動 —— 這是 CSS 在手勢中途做不到的事。

### 用長按取代握把，時間欄的位置讓給 ✎ · `c4f86e5`

- **決定**：拿掉 `⠿` 握把。按住時間欄 380ms，整列浮成一張帶陰影的卡片跟著你走。手還沒按滿就移動，判定成捲動並取消。原本握把的位置放 ✎，開一個能改時間、名稱、地點、備註的對話框。
- **為什麼**：長按是手機上本來就會的手勢，握把是桌面時代的東西。
- **順帶**：編輯一筆行程和拖完之後改時間，底下是同一個寫入。
- **順帶修掉**：標題的天數跟 Day 列對不起來 —— 它算的是「離 10:50 起飛過了幾個 24 小時」，所以 10/05 下午寫「第 2 天」而 Day 列寫 DAY 3。改成算日曆日。
- **後來推翻**：目標範圍在 `3761a64` 從時間欄擴大到整列。

### 開在今天、過去反灰、拖曳的本質是改時間 · `aeb95be`

- **決定**：Day 列開在今天那一格（紅眼回程期間 10/09 還算最後一天）；過去的日子掉色但照樣點得進去；管理員可以拖行程。
- **為什麼過去的還能點**：回頭看禮拜二花了多少，正是記帳本存在的意義。
- **拖曳的本質**：時間軸是照時間排的，所以「換順序」真正的意思是「改時間」。放下之後看新的左右鄰居，時間真的排得進去就不動它；只有真的衝突才開對話框，預設填一個排得進去的時間，而且會拒絕再次衝突的輸入。航班節點沒有握把，當牆用。
- **連帶**：改一筆行程需要 API 本來沒有的 `PATCH`，它跟著 `POST`、`DELETE` 一起進了通行碼後面。

### 讀開放，寫要通行碼 · `db1eca0`

- **決定**：看行程和記帳本不用通行碼，新增和刪除要。進站的對話框給兩扇門，唯讀那邊乾脆沒有編輯按鈕可以按。
- **真正的拒絕在後端**：`GET` 開放，`POST`／`DELETE` 一定要對得上的 `x-trip-key`。前端把按鈕藏起來只是順手。
- **推翻了**：原本 `TRIP_KEY` 沒設就跳過檢查，等於寫入全開。現在改成沒設一律擋下寫入 —— **沒設定通行碼不等於沒有鎖**。

### 讓網站在手機上單手可用 · `22b0387`

- **決定**：航班看板在 640px 以下收成一行摘要；分頁列變成固定在底部、拇指構得到；表單控制項一律 16px；按鈕最小 38–42px。
- **為什麼**：版面本來就塞得進 390px，但體驗不是 —— 航班看板每次進來都佔滿第一屏，換分頁要捲回最上面，而 iOS 在每個表單欄位上都自動放大整頁（那是 16px 以下才會發生的事）。

### 拿掉 Notion 已經刪掉的那兩欄 · `57f1501`

- **決定**：程式裡不再送「已確認」和「類型」這兩個欄位。
- **為什麼**：它們已經從「行程規劃」資料庫移除，而 Notion 收到一個它沒有的屬性名稱會整筆拒絕 —— 從網站新增行程會每一次都失敗。
- **記在這裡是因為**：Notion 那邊的欄位是人手改的，改完網站就會壞，而錯誤訊息不會告訴你是哪一欄。以後在 Notion 動欄位，記得回來看 `api/notion.js` 的屬性對應。

### 航班節點改成離線備援，並讓紅眼班機的隔夜抵達現身 · `4d857ff`

- **決定**：六個航班行程既然進了 Notion，程式裡寫死的那份就只當離線備援。
- **為什麼**：兩份一起畫，線上時 Day 1 和 Day 6 每一筆都列兩次。
- **順帶**：10/09 00:40 落地落在 Day 1–6 之外，根本看不見。最後一天改成涵蓋紅眼那段隔夜的行程，那幾列標 +1。

### 只有後端真的要求時才問通行碼 · `1d54f2f`

- **決定**：開站時拿存著的 key（或空的）去探一次 API，只有真的回 401 才跳輸入框。
- **為什麼**：本來只要本機沒存 key 就跳，但那時候後端如果沒設 `TRIP_KEY` 是完全跳過檢查的 —— 於是打什麼都會過，讀起來像一把壞掉的鎖，而不是一把不存在的鎖。

### 改用 Notion，讓網站和表格是同一份資料的兩個視圖 · `35e099c`

- **決定**：資料搬進共用頁面底下的兩個 Notion 資料庫。手機上用網站記帳，Notion 裡看表格、排序、做視圖。
- **為什麼需要一支 serverless function**：Notion API 不收瀏覽器直接呼叫（沒有 CORS），而且要密鑰 —— 密鑰放前端等於公開。所以 `api/notion.js` 擋在中間，它拿著密鑰，網站只跟它說話。
- **推翻了** `0be43f9` 的 Supabase。
- **順帶**：匯率變成 `config.js` 裡一個共用常數，跟 Notion 那邊的公式對齊。寫入走樂觀更新，Notion 拒絕就回捲。

### 五個人要看同一份資料，所以搬上 Supabase · `0be43f9`

- **決定**：網站留在 GitHub Pages，資料搬到 Supabase，靠 RLS 擋掉匿名讀取 —— 要用共用帳號登入才看得到。
- **為什麼**：五個人各記各的 localStorage，等於五份對不起來的帳。
- **保留**：沒設定或使用者選離線時，退回原本的 localStorage 模式。
- **後來推翻**：`35e099c` 改成 Notion。

### 單一檔案的靜態網站，資料存在瀏覽器裡 · `85c041f`

起點。2026/10/03–10/09 東京六日遊的行程、花費與五人分帳。

- **決定**：一支 `index.html`（樣式、邏輯、icon 全在裡面），紀錄存 localStorage，可以匯出／匯入 JSON。
- **為什麼**：沒有 build step、沒有相依套件，丟到任何靜態空間就會動。
- **後來推翻**：資料層在 `0be43f9` 搬上雲端；寫死的航班節點在 `4d857ff` 降級成離線備援。單一檔案這件事到今天還成立。

### 只調了文案和圖示的幾筆

這些沒有取捨，列在這裡只是為了不用懷疑它們是不是漏記了：
`f23b027` 拿掉進站對話框的說明句（兩顆按鈕自己說得清楚）、
`109ba29` 唯讀按鈕縮短成「直接進入」、
`df37b45` 唯讀狀態列縮成「已連上 Notion」、
`25bd278` 不再每一屏都提醒唯讀訪客去登入（登入按鈕就在旁邊）、
`dd504f7` README 拿掉 Notion 公開連結（公開分享從來不是網站讀得到的原因，
Connections 授權才是）、
`9640ffc` 許願按鈕改用 `+`（它是在新增東西，星星留在標題上當標籤）、
`7c87929` 許願按鈕從有框的按鈕改成置中的連結、
`462e8e1` 航班看板的展開箭頭換成真的 icon（`▾` 在黑底上又細又小）。

---

# 在那之前

## 2026-06-15

`909f4b7` 開了這個 repo，`cd581f0` 放進韓國行的 `code_artifact.html`。
兩筆都沒有取捨可記 —— 列在這裡只是為了說明這份日誌從 `85c041f` 才開始有東西。

---

# design-system

## 2026-09-19

### 移植規格書時要比對「值變了沒」，不是只看「有沒有新名字」 · `未進版控`

Lulu 打開 demo 的第一句話是「怎麼跟我傳給你的 HTML 不一樣，少了很多東西」。
量下來她是對的，而且比回報的多。

- **我上次移植 9/24 版的方法是錯的**：我 diff 兩份規格書，然後用
  `grep -oE '\.[a-z-]+\s*\{|id="..."'` 找「新出現的名字」。
  **那只看得到新增的規則，看不到既有規則的值被改掉。**
- **實際漏掉 8 條改了值的規則**（協調者發現其中 2 條，完整比對才找出全部）：
  `gradient-matte-steel`（冷藍灰 → **純中性灰**，停點 5 → 7）、
  `gradient-crisp-steel-dark`（整體變暗）、`btn-steel-hybrid`（靜止狀態從
  Version B 換成 Version A）、三層虹光的光譜色（全面提高飽和）、
  `text-steel-engraved-title`（刻痕變柔、顏色回到系統的 ink）、
  以及三處 `filter: saturate(--iri-saturate)`。
- **正確的做法**：把兩份規格書的 `<style>` 解析成「選擇器 → 宣告內容」，
  比對三件事 —— **改了值的、新增的、移除的**。三種都要看。
  一次就列出 8 改 8 增 1 移，而且不會漏。
- **她說「少了」其實是兩件事**：一是值真的變了（上面這些），
  二是 demo 的章節編號跟她那份對不起來 —— 她照自己那份的目錄去找東西。
  **同樣六個主題，但她找「04. 核心組件庫」會看到「04. 仿霧面不鏽鋼」。**
  已經照她那份重排並補回英文副標。
- **一條沒照抄**：規格書給 `.btn-steel-hybrid` 換了新的內陰影，我維持 `box-shadow: none`
  —— 那是 Lulu 當初明確要求移除的。**規格書改的是那個值，不是那個決定。**

### JS 跟 CSS 走同一條路：編出去、蓋指紋 · `未進版控`

- **背景**：tokyo-trip 的金屬漸層不會動，查下來是**它根本沒載 `retro-modern.js`**。
  CSS 有 `build.sh` 編到消費端目錄那條路，JS 一直沒有對應的路。
- **決定**：`build.sh` 編出 CSS 的同時，把 `retro-modern.js` 複製一份到同一個目錄
  並蓋指紋。落後掃描的副檔名也放寬到 `.js`。
- **為什麼不讓消費端寫 `../design-system/retro-modern.js`**：部署時的 root 通常
  只有消費端自己的資料夾（Vercel 的 Root Directory），那條路徑在線上不存在。
- **為什麼不讓消費端自己抄**：抄的那份沒有指紋，**走鐘不會有人講** ——
  這正是我們一路在拆的東西。
- **JS 副本過期時不印重編指令，改成描述**：它不是自己編出來的，是跟著 CSS 複製過去的。
  印一個 `./build.sh 舊.js 新.js` 看起來很合理，但那條指令是錯的 ——
  **一個錯的指令比沒有指令糟**，所以說「重編同目錄那份 CSS 就會一起更新它」。

### 陀螺儀是加分項，不要為它跳權限框 · `未進版控`

- **決定**：正式站不呼叫 `RetroModern.requestMotion()`。demo 頁保留那顆按鈕。
- **為什麼**：iOS 要使用者手勢觸發才給動作權限，而且會跳系統對話框。
  **為了讓一個漸層會動而要求權限是不成比例的** —— 這是
  `Playground 監督` 那條線提出來的，我同意。
- **而且不需要**：`init()` 在沒有任何權限的情況下就綁了 `scroll` 和 `touchmove`
  （桌機另外綁 `mousemove`）。所以**手機上捲動頁面、手指拖曳，高光本來就會動** ——
  陀螺儀只是多一種輸入，不是唯一那種。
- **這件事我當初設計對了但沒寫下來**，所以對方查「為什麼漸層不動」時，
  合理的猜測是「沒有權限」，而真正的原因是「沒載 JS」。
  **降級行為如果沒有寫在文件裡，它就等於不存在。**

### `@source` 指到畫面檔，不是整個資料夾 · `未進版控`

- **決定**：`demo.entry.css` 從 `@source "./"` 改成 `@source "./demo.html"`，
  README 給消費端的範例也一起改。
- **為什麼**：Tailwind 的掃描器不分辨字出現在什麼語法位置，指到資料夾就會把
  跑在伺服器上的程式、建置腳本、鎖定檔全部當成 markup 掃。實際踩過兩次 ——
  `api/ai.js` 裡 Gemini 的 `contents: [{ parts }]` 編出 `.contents`（由
  `Playground 監督` 那條線發現，而且它讓每次驗收的工作樹都是髒的）；
  我自己的 `shadowed-declarations.js` 裡一個 `transform` 字樣編出 `.transform`
  加五個 `@property`。
- **浪費體積是小事**（收窄後 demo 少 2.2KB）。真正的問題是**產出跟畫面對不起來**：
  一條沒人用的規則存在，代表「產出裡有這個 class」不能當成「有人用了它」的證據；
  而且非畫面的程式一改，產出就跟著變。
- **附帶收穫**：掃描集合變成已知的單一檔案之後，下一則那個洞才補得起來。

### 指紋涵蓋被掃描的內容，涵蓋不到就說 · `未進版控`

- **背景**：先前的指紋只涵蓋入口 CSS 和它 import 的 CSS。但 Tailwind 是
  content-driven 的，**被掃描的內容也是輸入** —— 所以 `demo.html` 改了、沒重編，
  落後掃描照樣說「最新」。那是我自己寫的「從不失敗的檢查要問它檢查了什麼」那條的實例。
- **決定**：指紋把指向**單一檔案**的 `@source` 也收進來。指向資料夾或 glob 的話
  列舉不可靠，就印一行說「涵蓋不到這些，那些檔案改了偵測不到」。
- **為什麼不硬把資料夾也算進去**：要嘛漏（跟不上 Tailwind 的實際規則），
  要嘛過度（每次改 README 都報過期，而重編出來一模一樣）。**能涵蓋就涵蓋，
  不能就說清楚** —— 這比兩種錯法都好。
- **驗過**：只改 `demo.html` 不重編 → 抓到；`@source` 改回 `"./"` → 印出涵蓋不到的警告。

### 逐像素比對證明不了「零差異」 · `未進版控`

這則是 `Playground 監督` 那條線量出來的，記在這裡是因為它改變了我驗收自己改動的方式。

- **現象**：我改完 design-system，請對方用逐像素比對確認 tokyo-trip 沒受影響。
  它照做了，但同時做了對照組：**同一份 CSS 連拍兩組，七張裡有兩張照樣有差**
  （地圖取景會讀到還沒安定的高度）。
- **所以那個數字答不了問題**：差異裡混著已知的不穩，而**再多拍幾輪也只是把機率
  往下壓，不會變成證明**。
- **它換了問法**：把消費端 HTML 真正用到的 class 全抓出來（`class="…"` 加上 JS 裡的
  `classList.add/toggle/remove`），逐條比較它們在新舊產出裡的規則。
  **202 個 class，零條規則不同** —— 那是證明，不是機率。工具收在
  `adopt-harness/ds-impact.py`。
- **給我自己的教訓**：我當時說「預期差異零」，理由寫成「只新增了新 class，
  沒動 `.rm-chip` 本身」。**後半對，前半錯** —— 我還在 `.steel-iridescent-overlay`
  加了一行 `filter: saturate(...)`，產出從 15623 漲到 18872 bytes。結論碰巧成立
  （tokyo-trip 完全沒用到 steel 那一族），但**我給的理由不是我實際做過的事**。
  下次更新完先自己跑一次 `ds-impact.py`，不要用回憶當根據。

### 標籤的狀態寫在屬性上，不是靠 JS 抽換 class · `未進版控`

規格書的可切換標籤是 `toggleTag()` 一次增刪十個 Tailwind class。我沒有照抄。

- **決定**：`.rm-chip--selectable` 的外觀由 `[aria-pressed="true"]` 或
  `:has(input:checked)` 決定，JS 只需要翻一個屬性。兩種都支援 ——
  button 版是純互動，label + checkbox 版送得進表單（tokyo-trip 那十顆用的是後者）。
- **為什麼**：抽換 class 要記住十個名字的增減順序，少刪一個就卡在半途，
  而那個半途狀態**在畫面上可能看起來是對的**。而且 `aria-pressed` 之外沒有東西
  告訴 screen reader 它被按下了。
- **選取時不只換顏色**：邊框由 1px 虛實變成 2px 實線、加上硬派投影。
  灰階列印和色盲情境下都分得出來 —— 這是 Day 色那則「絕不單靠顏色辨識」的同一條規則，
  只是這次用形狀而不是標籤來補。
- **代價**：`:has()` 需要 Safari 15.4+。我們的下限已經是 16.4（`@property`），所以沒有新增限制。

### 掃掠的兩段要認 animationName · `未進版控`

- **決定**：`RetroModern.sweep()` 等 `animationend` 時比對 `e.animationName`，
  只有自己那一段的名字才算數。
- **為什麼**：動畫跑在子層（`.directional-sheen`），事件是**冒泡**上來的 ——
  所以按鈕裡任何動畫結束都會觸發那個監聽。使用者在掃掠途中按一下，
  `steel-press-flash` 結束時冒上來的事件會把這一段提早收掉，而掃掠會看起來「跳了一下」。
- **怎麼發現的**：不是測出來的，是想清楚「我監聽的是誰」的時候發現的 ——
  當時我正在查為什麼 headless 測不到動畫事件。
- **驗法**：用合成事件驅動。先丟一個 `steel-press-flash` 的 `animationend`，
  序列**不前進**；再丟 `rm-sweep-sheen` 才前進；跑完零殘留。

### headless 的虛擬時間不觸發動畫事件 · `未進版控`

- **現象**：`--virtual-time-budget` 下，`animationstart` 和 `animationend`
  **一個都不觸發**。我用一個 100ms 的最小動畫單獨驗過。
- **所以**：這個環境驗得了「動畫有沒有接上」（`getComputedStyle(...).animationName`
  解析得出來）、驗不了「動畫跑完會怎樣」。後者要用合成事件，或者在真的瀏覽器裡看。
- **這是「我的量法量不到」而不是「量不出來」** —— 跟字型那次同一類，差別是
  這次我一開始就往環境的方向查，沒有先去懷疑自己的程式碼。

### 一道檢查要通過的三個條件 · `498fbb9`

這三條是分三次學到的，但它們是一個序列，拆開看會失去力量：

```
1. 沒看過它失敗的檢查不算檢查      防護壞掉了 —— 它永遠不會叫
2. 只在失敗時出聲的檢查不算檢查    防護沉默 —— 成功和「沒跑」長得一模一樣
3. 從不失敗的檢查要問它檢查了什麼  防護範圍被誤解 —— 它做得很對，只是不管那件事
```

- **前兩條問的是「它會不會叫」，第三條問的是「它在看哪裡」。** 那不是並列關係，
  是兩個不同的軸，所以過了前兩條不代表過第三條。
- **第三條為什麼最難**：那種防護**每天都在正確地做它該做的事**。它會如你所願地
  失敗、也會如你所願地通過 —— **只是範圍不是你以為的那個**。所以第 1 條
  （看它失敗過沒）對它無效：它從不失敗，它只是不管。
- **能抓到它的只有一個問題：「它到底檢查了什麼？」** 而那個問題**只有工具自己
  講得出來**，因為讀的人不會去看實作。所以**限制要由工具印出來，不是寫在文件裡等人查**。
- 三條各自的來源：第 1 條是撞名檢查第一版自己就是它要防的那種事；第 2 條是
  落後掃描成功時一個字都不印（`playground-b8` 抓到）；第 3 條是
  `shadowed-declarations.js` —— 它掃得到「一個檔案自己打自己」，掃不到
  「兩個檔案在同一個元素上打架」，而後者才是實際咬到人的那種。
- **代價**：第 3 條沒有機械化的驗法。前兩條可以用「種一個錯看它叫不叫」驗，
  第 3 條只能靠寫的人誠實描述範圍。目前唯一的補償是把限制印在輸出裡，
  讓讀的人至少有機會發現自己誤解了。

### 訊號有兩個獨立的軸：頻道和時機 · `498fbb9`

- **頻道**：同一則訊息該出現在哪裡。建置腳本的 stdout 給動手的人看，所以報數字
  沒問題；展示頁的畫面給看設計的人看，同樣的訊息要換到 `console`。
  這一軸這個專案踩過好幾次 —— 寫進頁尾的送出提示、被 `| grep` 濾掉的錯誤訊息、
  demo 頁該用 console 不該用橫幅。
- **時機**：同一則訊息該在什麼狀態下出現。`shadowed-declarations.js` 在**乾淨的時候**
  也印那句「這只代表這個檔案沒有自己跟自己打架」—— 因為**那正是人會把「沒有項目」
  讀成「這個軸過了」的時刻**。
- **兩者的差別**：一句限制寫在 README 裡，是人要主動去讀的；同一句在綠燈那一秒
  出現在螢幕上，是送到他面前的。內容一樣，效果不一樣。
- **誠實的註記**：**時機這一軸目前只有一個實例。** 頻道那一軸被踩過好幾次所以
  站得住，時機只有這一次。一個實例夠不夠成為規則，下一個人自己判斷 ——
  寫在這裡是為了讓他知道那是一個實例，不是一條被驗證過很多次的規律。

### 我立的標準，被我自己的工具違反了兩次 · `未進版控`

標準是前一則那兩句：沒看過它失敗的檢查不算檢查、只在失敗時出聲的檢查不算檢查。
**同一天，同一支腳本，兩道檢查各違反一次。**

- **第一次（撞名檢查，我自己抓到）**：`@import` 的比對只吃開頭是 `.` 的相對路徑，
  拿絕對路徑一測就抓不到匯入目標、token 名單是空的、`exit 0` 靜靜通過。
  我是照標準去種假撞名想看它失敗，才發現它不會失敗。
- **第二次（落後掃描，`playground-b8` 抓到）**：整段只有兩個印警告的迴圈，
  **成功時一個字都不印**。所以「沒聲音」有兩個意思 ——「兩份都驗過都最新」和
  「一份帶指紋的產出都沒找到」，而它們長得一模一樣。
  b8 是把那段邏輯唯讀重跑、讓它把每個檔講出來，才知道是哪一個。
- **決定**：成功時也印，而且**印數字不印「OK」**。
  ```
  產出檢查: 2 份帶指紋的產出全部最新（掃過 5 個 .css）
  ```
  b8 那句理由最關鍵：**數字從 2 掉到 0 的時候看得出來，「OK」不會。**
  而且「0 份全部最新」這種句子不該存在 —— 一份都沒找到時改說「這次等於沒檢查」。
- **為什麼第二次是別人抓到的**：我在撞名檢查上做對了這件事（報「113 個 token，
  無衝突」而不是報「通過」），但**沒有回頭把同一條標準套到隔壁二十行的另一道檢查上**。
  立一條標準跟把它套完是兩件事，而漏掉的那個通常就在旁邊。
- **然後我照那句話回頭掃了一遍，又找到第三處。** `demo.html` 的兩道執行期檢查
  （樣式表有沒有生效、產出有不有過期）也是只在失敗時出聲，而且
  `if (!m) return` 那行是無聲的「這次沒檢查」。四條路徑現在都會講。
  **用 `console` 不用橫幅** —— 展示頁的觀眾不該一直看著一條狀態列，
  而開發者看得到 console。訊號要活著，但也要挑對頻道。
- **代價**：每次建置多兩行輸出、開 demo 多兩行 console。可以接受 ——
  它們帶數字，而數字變了就看得出來，不是那種會退化成背景的「OK」。
- **可操作的部分**：立一條標準之後，**第一件事是回頭掃自己手上還有哪些東西
  該套上它**，不是等下次有機會用。這次回頭掃就找到了第三處。

### 兩種會讓人停止讀訊號的方式 · `未進版控`

`playground-b8` 把兩條接起來了，記成一則：

```
一個永遠掛著的警告，會被當成噪音        （b8 上一輪挖出來的）
一個永遠不出聲的檢查，會被當成沒問題    （這一輪）
```

**兩種都讓人停止讀它。** 所以一個訊號要活著，它得同時滿足兩件事：
**平常不叫**（否則變噪音），**而且平常講得出自己檢查了什麼**（否則沒人知道它還活著）。
這兩件事聽起來矛盾，解法是：**平常報事實和數字，不報評價。**

### 命名空間前綴是共存的必要條件，不要為了好寫拿掉 · `未進版控`

`--font-mono` 那件事查下來，真正生效的是 tokyo-trip 自己定義的那個 ——
它的 `:root` 是無層級的，蓋過 `@theme`。順著這條線量了整個撞名面：

```
設計系統 @theme 宣告的 token   108 個
tokyo-trip 自己定義的變數       24 個
真正撞名的                       1 個 → --font-mono
```

- **反直覺的地方**：`tokyo-trip` 有 `--paper`、`--ink`、`--accent`、`--surface`
  這些，名字跟我的語意別名一模一樣 —— 但**不撞**，因為 v4 把 token 放在命名空間裡，
  我的是 `--color-paper`、`--color-ink`。不同的變數。
- **所以如果當初用裸的語意名，現在會撞五個以上。** 前綴是共存的必要條件，
  而它是 v4 順手給的，不是我設計出來的 —— 這種「靠運氣成立的正確」要寫下來，
  否則下一個人只會覺得 `--color-ink` 太長。
- **代價**：寫起來長。**不要簡化。** 落在 v4 命名空間裡的消費端變數只有 3 個
  （`--font-body`、`--font-sign`、`--font-mono`），其餘 21 個永遠不會撞 ——
  那個安全邊際整個來自前綴。

### 撞名與落後都改成建置時講，不做 runtime 檢查 · `未進版控`

兩個症狀同一個形狀：**「改了沒反應」，而且沒有任何東西會講。**

- **決定**：`build.sh` 編譯前掃消費端原始碼比對 `@theme` 名單（撞名），
  編譯後掃整個 repo 裡帶指紋的產出比對來源（落後）。兩者都只是提示不是錯誤。
- **為什麼不做 runtime console 檢查**：撞名面是 1 個現存、3 個有風險，為此在每次
  頁面載入多背 108 個影子變數的體積不成比例。而且覆寫 token 是合法的（那就是
  主題化），所以它只能是提示 —— 一個「通常沒事」的 console 提示，跑幾次就沒人看了。
- **落後那個為什麼能自己講**：它講給**動手改設計系統的那個人**聽。改了就會編、
  編了就會掃 —— 不需要 git hook，也不依賴任何人記得跑什麼。
  我原本以為這件事只能寫成限制（瀏覽器看不到 `design-system/`，沒有比較對象），
  **漏掉的是「檢查不必在使用者的瀏覽器裡跑」**。
- **兩個檢查都會在「這次沒檢查」的時候出聲**：撞名檢查取不到 token 名單時說
  「等於沒檢查」；落後掃描找不到來源檔時說「驗不出」而不是「是最新的」。
  沒撞到時也報「113 個 token，無衝突」。
- **「沒叫」有兩種意思，而其中一種是沒跑。** 這句話是被自己的工具教出來的：
  撞名檢查**第一版就是它要防的那種事** —— `@import` 的比對只吃開頭是 `.` 的
  相對路徑，拿絕對路徑測就抓不到匯入目標、token 名單是空的、`exit 0` 靜靜通過。
  我是照「看過它失敗一次」的標準去種假撞名，才發現它不會失敗。
  **造了一個防這種事的工具，而它第一版就是這種事。**
- 所以這裡的檢查標準有兩半，缺一半都不算：
  **沒看過它失敗的檢查不算檢查**（它可能永遠不會叫）、
  **只在失敗時出聲的檢查不算檢查**（成功和沒跑長得一模一樣）。
- **代價**：只在有人跑 `build.sh` 時才掃。改了原始碼卻什麼都不編的話沒人會講 ——
  但那種狀態下也還沒有人受影響。

### 指紋存的是相對於產出的路徑 · `未進版控`

- **決定**：產出尾巴那枚指紋，檔案清單存「相對於產出」的路徑。
- **為什麼**：原本存檔名。demo 剛好沒事（入口和匯入都在同層），但消費端的入口
  匯入的是 `../design-system/retro-modern.css` —— 只存檔名會記成
  `./retro-modern.css`，解析到 `tokyo-trip/retro-modern.css`，**一個不存在的位置**。
- **為什麼基準是產出而不是入口**：讀指紋的人手上只有產出（demo 的比對、落後掃描
  都是），所以基準必須是產出的位置。
- **這個 bug 是落後掃描抓出來的** —— 它掃到 `tokyo-trip` 那份產出時說「驗不出，
  它記的來源檔找不到」。加檢查的當天就抓到一個既存的錯，而那個錯原本會讓
  重算指紋的人算出一個對不上的值卻不知道為什麼。

### 驗證不可以重用被驗證的那段邏輯 · `6dc9226`

這則不是某次 bug 的修法記錄，是一條工作準則。這個專案一路上最常遇到的失敗
不是「壞掉」，是**機制看起來在運作、保證卻是空的**。三句話，適用於這裡每一道防呆：

- **驗證不能重用生成用的那段邏輯。** 我修 layer 名單漏掉裸宣告的 bug 時，改好
  正規表示式又加了驗證 —— 但驗證用同一個 pattern。看不見的東西，驗證時一樣
  看不見。那個檢查**印了「已驗證無遺漏」然後放過了眼前的 bug**。
- **寬鬆的驗證勝過精確的驗證。** 改成找出每一處字面上的 `@layer`、把到 `{` 或 `;`
  之間的字全當層名候選。會誤報，但**誤報看得見，漏報看不見**。
- **沒看過它失敗的檢查，不算檢查。** 我是刻意把正規表示式退回舊版、想看它失敗，
  才發現它不會失敗。這個動作要當成加檢查的必要步驟，不是額外的謹慎。

代價：寬鬆的驗證日後可能因為無關的字串誤報，到時要放寬白名單而不是收緊掃描。

### layer 有兩種註冊形式，只數一種會讓那層跑到最前面 · `6dc9226`

- **決定**：`build.sh` 推導 layer 名單時，`@layer x { }`（帶區塊）和 `@layer x;`
  （裸宣告，也可能是逗號清單）兩種都要數。
- **為什麼**：原本只數帶區塊的，所以 `base` 被漏掉 —— 它是原始碼自己那行順序宣告
  被 minify 之後的殘留，以裸宣告的形式出現。漏掉的後果**不是「少宣告一個」，
  是那個層被接在名單之後、變成優先序最高的** —— 本該墊在底下的層跑到最上面。
- **只在不帶 preflight 的建置裡顯現**：demo 匯入 preflight，它的 `base` 有區塊、
  數得到；沒有 preflight 的那份才會漏 —— 而那正是要給消費端用的。
  **測試用的建置跟出貨用的建置不同，bug 就藏在差集裡。**
- **當時無害**（那層是空的），但任何人往 `base` 放東西、或在下游把 preflight
  接回來，拿到的就是相反的結果，而且沒有任何錯誤訊息。

### 掃描範圍在設計系統這端關掉，不要求消費端記得 · `59ba010`

- **決定**：`retro-modern.css` 的 utilities 匯入掛 `source(none)`，掃描範圍改由
  每個入口自己宣告 `@source`。
- **為什麼**：不關的話，消費端匯入這支的同時 Tailwind 會連 `design-system/`
  一起掃，把 `demo.html` 用到的 utility 全編進他的產出（量過：**206 個，其中
  只有 58 個是他的**）。
- **體積不是重點。** 真正的傷害是**「忘了重編」偵測不出來** —— 他要寫的 utility
  幾乎一定已經在裡面了（demo 用過），content-driven 的前提整個失效，
  而失效的樣子是「一切正常」。
- **為什麼不讓消費端自己寫 `source(none)`**：那是一個「必須記得，忘了就安靜失效」
  的要求，正是這個專案一路在拆的東西。關在上游，下游就不必記得。
- **順帶記一個掃描器的實情**（由 Tokyo trip session 查出）：它不分辨字出現在
  什麼語法位置，CSS 屬性名、註解、連中文散文都撿 —— 註解裡寫「用 clip 不用
  hidden」，`hidden` 就進產出。**推論：不能拿「產出裡有某個 class」當作
  「有人用了它」的證據。**

### `--font-mono` 補上 CJK 後援 · `未進版控`

`.rm-chip` 裝中文分類標籤上了正式站，回報說那幾個字跟頁面其他中文不是同一套。

- **決定**：`--font-mono` 在拉丁 mono 之後接上跟 `--font-sans` 同一組 CJK
  後援（`Noto Sans TC` → `PingFang TC` → `Microsoft JhengHei`）。**不改動任何
  組件的字族指派。**
- **為什麼改 token 而不是改組件**：`--font-mono` 原本零個 CJK 後援，那是一套
  中文介面的設計系統的缺陷，跟哪些組件在用它無關。改 token 一次修好現在的四個
  （`.rm-chip` `.rm-overline` `.rm-day-pin` `.rm-toast`）和以後所有的；改組件
  則是每加一個組件就要再判斷一次。
- **為什麼不把 chip 改成 sans**：mono 在這些組件上是刻意的 —— 色碼、金額、時間、
  token 名靠它對齊，那是 70s 技術標籤的調性。補後援之後拉丁字仍然等寬、中文
  落到頁面本來那套，兩邊都拿到。
- **「中文不再等寬」不成立**：漢字在任何 CJK 字體裡都是全角 1em。真正對不齊的是
  半角數字跟全角漢字混排，那件事換哪套字都一樣。
- **我在本機環境驗不出這個修正**，見下一則。

### 那個修正驗得出來，是我測錯了三次 · `7214658`

上一則原本寫成「一個驗不出來的修正」。**那個結論是錯的**，而且錯在方法上，
所以留著記錄怎麼錯的，比留著錯誤結論有用。

三次都不是同一種錯：

1. **把字重看成字體。** 第一張對照圖裡 `.rm-chip` 的中文跟本文不同，我當成字體
   差異 —— 其實 `.rm-chip` 是 `font-weight: 700`、本文是常規。**一個真實存在的
   差異，被歸到錯的原因上。**
2. **`document.fonts.check()` 不帶文字。** 不帶的話它測的是拉丁測試字，而 CJK
   網頁字體是按 `unicode-range` 分片送的 —— 拉丁那片根本不會載，所以回 `false`。
   帶上中文就回 `true`。我拿那個 `false` 當成「這個環境沒有那套字體」。
3. **拿高度當指標，但那是混淆的。** 行盒高度由「第一個可用字體」的度量決定，
   不是由實際畫出漢字的那套字。所以兩個堆疊高度不同，可能只是因為第一個字體
   不同，跟 CJK 後援有沒有生效無關。

**有效的做法**：把第一個字體固定，只差 CJK 名字在不在，然後**比對算圖的位元組**。

```
'JetBrains Mono',…,Menlo,monospace                      041d9e7c
'JetBrains Mono',…,Menlo,'Noto Sans TC','PingFang TC',… e08f96fd   ← 不同
只有 'Noto Sans TC'                                      e08f96fd   ← 與上面相同
```

第三行是關鍵：**出貨的堆疊畫出來的漢字，跟純 Noto Sans TC 逐位元組相同** ——
所以 CJK 名字確實被讀到了，而且 mono 的中文現在跟 sans 會給的一樣。

順帶推翻我自己的一個猜測：我以為 `ui-monospace` / `SFMono-Regular` 會透過
macOS 的複合字體把漢字接走、讓後面的 CJK 名字永遠到不了。**沒有這回事**，
CJK 名字排在 `Menlo` 之後照樣生效。

**準則**：「量不出來」跟「我的量法量不到」是兩件事，而後者常見得多。
宣告前者之前，先確認量的東西沒有混淆、工具的參數給對了。

### 不帶 preflight，改讓組件自己自足 · `未進版控`

接線前發現 tokyo-trip 從來沒有過 CSS reset。

- **決定**：`retro-modern.css` 只匯入 `theme.css` 和 `utilities.css`，**不帶 preflight**。改成組件在 `@layer components` 裡自己宣告 `box-sizing: border-box` 和必要的 margin 歸零。
- **為什麼**：preflight 是替全新專案抹平瀏覽器差異，不是去改一個已經長好的網站。tokyo-trip 明確設過的樣式在 `components` 層贏得過 `base`，但凡是它**依賴瀏覽器預設**的地方（標題級距、清單縮排、`b` 的粗細、各種預設 margin）都會在接線那一刻改變 —— 第一步就從「接上去、畫面不變」變成「整站到處有細微差異，而且分不清哪些是預期的」，逐塊驗收的乾淨基準就沒了。
- **但傾向要成立，組件就不能靠它。** 實測三份對照，不帶 preflight 時組件真的會壞：`.rm-input` 的 `width:100%` 加 padding 加邊框撐出容器（348 → **380**），`.rm-day-pin` 不再是圓的（28×28 → **44×32**）—— 而那顆號碼牌的全部意義就是跟地圖圖釘長得一模一樣，變形等於功能壞掉。所以先拆掉這個依賴，才談得上不帶。
- **只套組件本身，不套後代**：`:where(組件) :where(*)` 那種寫法會把消費端既有的子元素也從 content-box 翻過去。量過之後確認**後代那條對組件幾何毫無影響**（兩份數字一模一樣，差別只在消費端的子元素 372 vs 348），所以拿掉。用 `:where()` 讓特異度維持 0。
- **行高也不再靠繼承**：preflight 的 `html { line-height: 1.5 }` 撐著六個組件（`.rm-card` `.rm-chip` `.rm-input` `.rm-label` `.rm-overline` `.rm-toast`），不帶就縮。各自寫明 `1.5` —— 就是它們原本繼承到的值，所以外觀零變化。**不要在 `.rm-base` 上設**，那會改掉消費端的整站行距，正是要避免的事。
- **代價／要講準的一句話**：接上 CSS 本身不動任何既有東西，但**把既有元素套上 `rm-*` class，那個元素會變成 border-box**。這不是副作用，是組件保證自己版面正確的必要條件。會變的只有親手加了 `rm-*` 的那幾個元素本身，後代不受影響。
- **全新專案的 preflight 是 opt-in**：`demo.entry.css` 加一行 `@import "tailwindcss/preflight.css" layer(base);`，它同時是「全新專案怎麼寫」的活範本。
- **順序**：這件事必須在接線之前做完。否則對方換的第一塊如果含輸入框或號碼牌，會看到版面溢出，而那跟它自己改的東西無關 —— 診斷起來很浪費時間。
- **「不帶」這個決定的另一半證據來自 tokyo-trip 那邊的量測**（由 Tokyo trip session 做）：逐項對照 preflight 的九項職責，**它自己已經做了八項**（全域 `box-sizing`、body margin 歸零、`img max-width`、h1~h6 字級字重、`list-style`、`a` 的顏色裝飾、表單字型、button 外觀重設），缺的 `border-collapse` 因為整頁沒有 `<table>` 而無關。所以不是「它沒有 reset」，是**它有一套自己的，preflight 對它買不到東西** —— 那比「不要去改一個已經長好的網站」這個原則更硬，因為是量出來的。
- **順帶記一句方法上的教訓**（同一份量測）：如果硬要帶 preflight，625 個元素裡有 11 個尺寸會變，全部是按鈕，成因是 `button,input,select,textarea{font:inherit}` 把簡寫裡的 `line-height` 和 `font-family` 一起重設掉。試著補兩條宣告修掉，**結果變成 20 個**。**remedy 不是看程式碼推得出來的，要量著改。** 哪天有人決定要帶 preflight，從這裡開始。
- 產出順帶從 19.5KB 降到 15.5KB，但那是附帶好處，不是理由。

### 明確宣告 layer 順序，不吃「誰先出現」的運氣 · `9801091`

`458291b` 把組件包進 `@layer components` 之後，`rounded-arch` 是對的了 —— 但理由還是錯的。

- **決定**：`build.sh` 從產出本身讀回 layer 名單，依序在檔案開頭補一行 `@layer properties,theme,base,components,utilities;`。
- **為什麼**：**層的優先序由「名字第一次出現的順序」決定，不是由名字本身。** utilities 排在最後只是剛好，任何人在前面插一層就翻盤。這跟 `458291b` 抓到的是同一種病 —— 一直是對的，但理由是錯的 —— 只是換了層皮。
- **為什麼不寫在原始碼裡**：`--minify` 會把 `@layer a,b,c;` 這種宣告拿掉。Tailwind 自己的 `index.css` 開頭就有一行，編出來一樣不見 —— 所以只能在 build 之後補。名單從產出推導而不是寫死，Tailwind 之後多一層也不會漏掉。
- **它買到什麼、買不到什麼**（拿編好的檔案實測）：

  | 消費端的樣式 | 結果 |
  | --- | --- |
  | 包進 `@layer components` | utility 贏，而且 `<link>`／`<style>` 的前後順序不再有影響 |
  | 沒有包 layer | **消費端贏** |
  | 用了我們沒宣告過的層名（例如 `@layer site`） | **消費端贏** |

- **代價／交接條件**：只有第一種情況被修好。**沒有包 layer 的 CSS 贏過所有的層**，而 tokyo-trip 現在正是單檔內嵌、完全沒有 layer。所以接線時它必須把自己的 `<style>` 包進上面宣告過的層名之一 —— 這不是建議，是接上去能不能照預期運作的前提。

### 產生檔進版控就要有防呆 · `f0b3921`

- **決定**：`build.sh` 在產出尾巴蓋一枚原始碼指紋（FNV-1a），`demo.html` 自己算一次來比對，對不上就在頁面最上面跳警告。
- **為什麼**：`demo.css` 進了版控（見 `e1737cb`），就有跟原始碼走鐘的可能 —— 有人改了 `retro-modern.css` 忘了重編，git 裡的 demo 就開始說謊，而那正是這套系統被審閱的方式。與其在 README 寫「記得重編」，不如讓頁面自己講。跟「問畫面 body 是什麼顏色」是同一個路子：**驗證，不要相信別人記得**。
- **代價**：demo 多兩次 fetch。`file://` 下 fetch 會被擋，那種情況查不出來 —— 刻意選擇不報，而不是亂報。

### 走 Tailwind v4，token 搬進 CSS · `5f8227d` `135a8a4` `458291b` `e1737cb`

- **決定**：升到 Tailwind v4，token 從 `tailwind.preset.js` 搬進 `retro-modern.css` 的 `@theme`。三支檔案變兩支，`build.sh` 砍掉產生暫時 config 那整段。
- **為什麼**：v4 的 `@theme` 同時做兩件事 —— 產生 utility，以及把每個 token 輸出成**真正的** CSS 變數。所以 `bg-paper` 編成 `background-color: var(--color-paper)`（保留變數，可以重新指向），而手寫 CSS 照樣可以寫 `color: var(--color-ink)` 拿到顏色。v3 只能二選一，見下一則。
- **順帶消失的**：不再有 config 檔；要掃哪些檔案也不用指定（v4 掃入口 CSS 所在資料夾）。消費端自己寫一支一行的入口 `@import`，設計系統就不必知道誰在用它。
- **代價一：多了 node_modules。** v4 的 CLI 必須解析得到 `tailwindcss` 套件，`npx` 從任意目錄做不到，所以變成本機 devDependency。這個 repo 本來一個 node 相依都沒有。
- **代價二：產出從 14KB 變 34KB**（minified）。多出來的是 `@theme` 輸出的全部 token 變數和 `@property` 宣告 —— 那正是「token 還活著」的體積，不是浪費。
- **代價三：瀏覽器下限拉到 iOS Safari 16.4 / Chrome 111 / Firefox 128**（`@property`）。低於這條線顏色照常，但 `bg-paper/50` 這類半透明會**整條宣告失效**（編成 `color-mix()`，連 `@supports` 後備也是）—— 安靜失效，不是退化成難看。這道關卡真正咬人的時間點是 tokyo-trip 改引設計系統那一刻，在那之前只影響 `demo.html`。
- **`retro-modern.css` 從此是原始碼，不是成品**：開頭是 `@import "tailwindcss"`，瀏覽器讀不懂，一定要編。`demo.css` 因此進版控（demo 是這套系統被審閱的方式，要先 build 才看得到的展示頁比一個產生檔進 git 更糟）；`dist/` 維持不進版控，那是各網站自己的產出。

### 沒走的那條路：token 存成通道值 · tag `channel-tokens-not-taken`

做完、提交、然後整個拆掉。**留下 tag 才找得回來** —— `git show channel-tokens-not-taken`。

- **當初的問題**：v3 餵 hex 會把顏色烘死進每一個 utility（`bg-surface` 編成 `rgb(249 232 212 / …)`），輸出裡一個 token 變數都不剩，執行期沒辦法重新指向。
- **當時的解法**：token 改存空格分隔的通道值（`--rm-surface: 249 232 212`），preset 餵 `rgb(var(--rm-x) / <alpha-value>)`。有效，判準也都過了（輸出從 0 個變數變成 27 個）。
- **為什麼拆掉**：v4 不必付這個代價就拿到同樣結果。通道值的代價是 **CSS 變數不再是能直接用的顏色** —— `var(--rm-ink)` 變成三個數字，丟進顏色欄位會被瀏覽器安靜丟掉，元素變透明而且不報錯。光是 `retro-modern.css` 裡就有 90 處要改寫成 `rgb(var(--rm-x))`，devtools 也不再顯示色塊。而我們的消費端正是 tokyo-trip 那種手寫 CSS 的單檔網站。
- **為什麼可以安心來回**：兩種存法編出來的 **utility class 名稱完全相同**，所以 markup 一個字都不用動 —— 沒有單向門，晚改不會比較貴。這個判斷是實測的（改 preset 的值重編，`.bg-paper` 就變，probe 的 markup 沒動）。
- **為什麼刻意不推上遠端**：沒推過的 commit `git reset` 就沒了，遠端歷史裡看不到這輪來回；推出去就只能 revert，疤留著。所以它在本機待到 v4 拍板才拆。
- **順手學到的**：`grep -c` 在 minified 檔上會騙人（整份壓成一行，不管幾個都回 1）。要數就用 `grep -o | wc -l`。

### 三組不鏽鋼色階維持字面色碼，不走 token · `5f8227d`

- **決定**：`steel`、`steel-deep`、`steel-iri` 共 17 個色維持寫死的十六進位，不進 `@theme` 的語意 token。
- **為什麼**：它們是三條固定漸層上的停點，不是有人會想重新指向的語意角色。而那三條漸層本身就是寫死的 —— 只讓色階可變會讓 utility 跟漸層對不上，看起來像壞掉。
- **代價**：下一個人只會看到「有 17 個色沒走 token」而看不出是刻意的。這一則就是為了那個人寫的。

### 方格紋理改成參數化，不為每種配色各寫一個 class · `2b49093`

- **決定**：`.pattern-grid` 由四個 CSS 變數驅動 —— `--grid-fill-color`（預設透明）、`--grid-border-color`（預設 8% 丁香褐）、`--grid-border-width`（`1px`）、`--grid-size`（`20px`）。行內給值就好。
- **為什麼**：方格的變化維度是連續的（格寬、線粗、兩個顏色），不是離散的幾種款式。寫成 `pattern-grid--sky-sun`、`pattern-grid--earth-tide` 這種具名變體，每多一種配色就多一個 class，而且命名會愈來愈難取 —— 「這個叫什麼」本身就是設計在抗議這個做法。
- **變數名沒有 `rm-` 前綴**：沿用規格書原稿，那份 HTML 的 markup 可以直接貼過來。查過 `tokyo-trip` 沒有任何 `--grid-*`，短期不會撞。
- **代價**：四個變數都是全域名稱，消費端如果自己也用 `--grid-size` 就會互相影響。真的撞了再加前綴，屆時要同步改規格書原稿的 markup。

### 按鈕的 hover 和 click 不能都閃白光 · `7390406`

滑過去閃一下、按下去也閃一下，使用者分不出自己到底按到了沒有。

- **決定**：`.btn-steel-hybrid` 靜止是 Version B 冷鋼，hover／鍵盤 focus 時 Version C 的薄膜虹光在 0.3 秒內淡入，白光嚴格保留給點擊（`steel-press-flash`）。hover 那兩層裡一滴純白都沒有 —— 沒有白光掃掠條，也沒有循環呼吸亮度。
- **為什麼**：「滑過去有回應」和「真的被按下」是兩件事，用同一種視覺講會混成一件。虹彩是色相變化、白閃是亮度變化，兩者分得開。
- **底色那條選擇器必須把 `:hover` 也列進去**：`.rm-btn:hover` 的特異度跟 `.btn-steel-hybrid` 一樣（各一個 class 加一個 pseudo-class），少了它滑過去整顆會被換成砂岩色。
- **代價**：hover 那層只在有指標的裝置上出現，手機完全看不到 —— 見下一則。

### hover 的虹光包在 `@media (hover: hover)` 裡 · `7390406`

- **決定**：虹光那層只掛在真的有指標的裝置上。鍵盤的 `:focus-visible` 不分裝置，放在 media query 外面。
- **為什麼**：手機點一下之後 `:hover` 會黏住不放，虹光會永遠亮著、再也回不去 Version B。行程網站主要就是在手機上用的，那不是這個效果的用意。
- **跟規格書不同**：原稿沒有這層保護，它預設在桌機上看。
- **代價**：手機使用者看不到虹光，只看得到點擊的白閃。這套系統裡 hover 一律當成加分項，不承載任何必要資訊。

### Primary 按鈕換成混合鋼，而且不要陰影 · `7390406`

- **決定**：「06. 核心組件」的 Primary 從 Version A 柔霧鋼換成混合按鈕（B ⇄ C），並且 `box-shadow: none`。
- **為什麼**：Primary 該是最有存在感的那一顆，而混合按鈕會對互動有反應，柔霧鋼不會。
- **`none` 是必要的，不是省略**：這顆同時帶著 `.rm-btn` 基底類別，單純把 `box-shadow` 那行刪掉的話，`.rm-btn` 的 `--rm-shadow-soft` 會補上來，反而多一層外投影 —— 結果跟想要的相反。
- **跟規格書不同**：原稿的 `.btn-steel-hybrid` 有內凹倒角陰影（第 220 行），每顆按鈕的 class 上還另外掛著 `shadow-soft`。兩處都拿掉了。

### 三支檔案分開，不合成一支 · `7390406`

- **決定**：`tailwind.preset.js`（token）、`retro-modern.css`（材質與組件）、`retro-modern.js`（動態反光）各自獨立。
- **為什麼**：需求層次不同 —— 只要配色就引 preset，要金屬質感才加 CSS，要高光會跟著手機轉才加 JS。不引 JS 不會壞，高光只是不會動。

### CSS 裡不寫 `@import` 拉字型 · `7390406`

- **決定**：字型用 `<link>` 放在 HTML，CSS 不做 `@import`。
- **為什麼**：`@import` 會多一次往返而且擋住渲染。

### 用語意別名，不要在版面裡點名顏料 · `7390406`

- **決定**：寫 `bg-paper`、`bg-surface`、`border-line`、`text-ink`，不要寫 `bg-foundation-muslin`、`border-foundation-sandstone`。顏料名留給真的需要指名某個色相的場合（裝飾線、圖表）。
- **為什麼**：之後要調色票，改 preset 裡的別名一行就好，不用全站搜尋 `foundation-linen`。

### `rounded-*` 整組取代，不是 extend · `7390406`

- **決定**：`rounded-sm/md/lg/xl` 直接改寫成 8／16／24／32px，不保留 Tailwind 原值。`rounded-2xl`、`rounded-3xl` 留成 md／lg 的別名。
- **為什麼**：留著原值會讓 `rounded-lg` 同名不同值，翻程式碼時無法確定看到的是哪一套。
- **代價**：從別的 Tailwind 專案貼 markup 過來圓角會變 —— 但規格書原稿的 markup 貼過來不會變形，那才是這裡要顧的。

### Day 色一律搭數字或文字標籤，絕不單靠顏色 · `7390406`

- **決定**：六個 Day 色（`day-1`…`day-6`）從核心色相延伸、用 CVD 驗證器調過彩度，但用的時候一定要配編號或標籤。
- **為什麼**：相鄰配對都過得了門檻（最差 CVD ΔE 10.7 deutan），但 day-2 和 day-3 對底色的對比低於 3:1 —— 標籤是必要的補償，不是裝飾。
- **六天同時出現在一張圖上**（地圖、散佈圖、小倍數）屬於 all-pairs 情境，**只有前三個 slot 過得了門檻**。橘與綠在 deutan 下必然靠近，換什麼顏色都一樣。所以那種圖上編號是強制的，`.rm-day-pin` 就是為此存在 —— 時間軸的號碼牌和地圖圖釘共用同一個樣式，兩邊看起來才是同一個東西。

### 沒有深色模式 · `7390406`

- **決定**：不做。
- **為什麼**：這套系統的靈魂是溫潤的奶油底色與暖色光影，翻成深色等於重新設計一套色票。
- **之後真的要做**：在 preset 裡新增一組深色 token，**不要用濾鏡反轉** —— 那會把 70s 的暖調變成髒的藍灰。

### `dist/` 不進版控 · `7390406`

- **決定**：`build.sh` 編出來的靜態 CSS 不 commit，`.gitignore` 掉。
- **為什麼**：Tailwind 是 content-driven 的，只產出你真的寫在 HTML 裡的 class。所以沒有「全部 utility 的預編版本」可以共用（那會是好幾 MB），每個網站得編自己的那一份。
- **但正式上線一定要編**：`cdn.tailwindcss.com` 會把整個編譯器（約 120KB JS）送到瀏覽器，在使用者裝置上即時算樣式。行程網站是在國外用手機開的，這點差很多。`demo.html` 整頁編出來 14KB（minified）。

### 動態反光的光源是全域的 · `7390406`

- **決定**：所有金屬面共用同一盞燈，變數寫在 `:root` 上讓它繼承。追到定位就停掉 `requestAnimationFrame`。`prefers-reduced-motion: reduce` 下整套不啟動。
- **為什麼**：每幀只改 4 個變數，而不是走訪 N 個元件各改 4 個。行程網站是在外面邊走邊看的，不能讓它一直空轉耗電池。

### 組件 class 一律低特異度 · `7390406`

- **決定**：`.rm-btn`、`.rm-card` 這些都是單一 class，Tailwind utility 蓋得過去 —— `class="rm-btn bg-info"` 會如你所想。
- **順帶**：`rm-` 前綴是這套系統新增的；沒有前綴的（`gradient-*`、`texture-*`、`steel-*`、`pattern-*`、`sheen-*`）沿用規格書原稿的名字，那份 HTML 可以直接貼過來。

### `demo.html` 要把話講完，不要整段 script 停掉 · `7390406`

- **決定**：preset 沒載到時，頁面最上面出現一條紅色提示，其餘部分照常運作。
- **為什麼**：本來是直接丟 `ReferenceError: retroModern is not defined`，整段 script 就停了 —— 使用者只看到一頁壞掉的東西，不知道原因是 `demo.html` 被單獨拖進預覽窗格、抓不到同層的兄弟檔案。

### 保留了兩組重複的顏色值 · `7390406`

- **決定**：`foundation.chocolate` 和 `earth.chocolate` 是同一個值（`#663924`），兩個都留；`sun.tangelo` 等於 `earth.tangelo`，一併保留。
- **為什麼**：它在兩組色階裡各自說得通，刪掉哪一個都會讓那組色階缺一階。

### 願望那一排放不下就換行,不收成「⋯」 · `5bd9b20`

- **決定**：管理員看自己的願望時那一排有「改」和「刪掉」，窄的地方裝不下就**換到第二行**。不做溢出選單。
- **為什麼**：兩顆按鈕都是一眼要看到、一下要按到的東西。收進「⋯」等於把「刪掉」藏在一個要先點開才知道有什麼的地方 —— 為了省一行高度，換來每次都多一次點擊和一次猜測。那張卡高一階是可以接受的代價。
- **這是決定，不是沒人改**：`geofix` 那條「窄視窗那一排會換行」的斷言已經退成只記數字（它原本的根據是舊版 05 的 +51px，而許願改成滿版 sheet 之後那個前提沒了）。**看到它換行不要當成缺陷去修。**
